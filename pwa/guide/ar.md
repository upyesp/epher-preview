# دليل مستخدم epher

مرحباً! epher آلة حاسبة قابلة للبرمجة والكتابة النصية. يمكنك استخدامها لحساب سريع، أو لبناء دوالك وبرامجك الصغيرة. كل شيء متاح بثماني لغات.

هذا الدليل موجَّه للمبتدئين تماماً. يبدأ بأبسط حساب ممكن ثم يتدرج حتى القوة الكاملة للغة. كل مثال يبيّن ما تكتبه وما يجيبه epher.

هناك خمس طرق لاستخدام epher. اختر ما يناسبك:

| الإصدار | ما هو | متى تختاره |
|---|---|---|
| **سطر الأوامر** (CLI) | أوامر نصية في الطرفية | تعيش في الطرفية وتحب النصوص البرمجية |
| **REPL** | جلسة `epher` تفاعلية عند الموجه `epher>` | تريد تبادلاً سريعاً دون مغادرة الطرفية |
| **واجهة الطرفية** (TUI) | برنامج بملء الشاشة داخل الطرفية | تريد تطبيق طرفية برسوم بيانية وسجل على الشاشة |
| **تطبيق سطح المكتب** | برنامج مكتبي عادي بنافذته الخاصة | تريد تطبيقاً عادياً |
| **تطبيق الويب** (PWA) | يعمل في متصفحك، قابل للتثبيت، ويعمل دون اتصال | تريد أسرع بداية؛ دون تثبيت |

تطبيق سطح المكتب وسطر الأوامر وREPL وواجهة الطرفية برنامج واحد: تنزيل واحد يثبِّت أمر `epher` الذي يفعل الأربعة. تطبيق الويب هو الاستثناء: لا يحتاج أي تنزيل.

جميع الإصدارات الخمسة تفهم اللغة نفسها تماماً. تعلّمها مرة واحدة، واستخدمها في أي مكان.

## 1. لغة epher

يعلم هذا الفصل اللغة المشتركة بين جميع إصدارات epher. في تطبيق الويب أو تطبيق سطح المكتب، اكتب تعبيراً واضغط **Enter** (أو انقر زر **=**). في CLI، ابدأ الجلسة بـ `epher repl` واكتب بعد الموجه `epher>`. في TUI (`epher tui`)، اكتب فقط واضغط **Enter**. في CLI يمكنك أيضاً كتابة `epher "تعبير"` لتقييم تعبير واحد مباشرة.

### 1.1 أول عملية حساب لك

اكتب هذا:

```epher
2 + 3 * 4
```

يجيب epher:

```text
14
```

الضرب يتم قبل الجمع، تماماً كما في الرياضيات. تسمى هذه القاعدة *أسبقية العوامل*.

### 1.2 ترتيب العمليات

ترتيب الأسبقية الكامل، من الأقوى إلى الأضعف:

1. `!` المضروب و`%` النسبة المئوية (كلاهما لاحق)
2. `^` الأس
3. `*` و`/` الضرب والقسمة
4. `+` و`-` الجمع والطرح

استخدم الأقواس لتغيير الترتيب:

```epher
(2 + 3) * 4
```

```text
20
```

يحسب العامل `^` الأسس ويعمل من اليمين إلى اليسار:

```epher
2 ^ 10
```

```text
1024
```

```epher
2 ^ 3 ^ 2
```

```text
512
```

(`2 ^ 3 ^ 2` تعني `2 ^ (3 ^ 2)`، أي `2 ^ 9` = 512.)

يمكن أن يكون الأس كسراً. `2 ^ 0.5` هو الجذر التربيعي للعدد 2:

```epher
2 ^ 0.5
```

```text
1.41421356237
```

الطرح والقسمة يعملان من اليسار إلى اليمين:

```epher
10 - 3 - 2
```

```text
5
```

علامة `%` عامل لاحق يعني "مقسوماً على 100": `5%` تساوي 0.05. ولا ينظر أبداً إلى العوامل من حوله، لذا `200 + 10%` تساوي 200.1. ولزيادة 200 بنسبة 10%، اكتب الضرب:

```epher
200 * (1 + 10%)
```

```text
220
```


### 1.3 الأعداد الخاصة pi وe وtau وphi

الثوابت الشهيرة مدمجة:

```epher
pi
```

```text
3.14159265359
```

```epher
2 * pi
```

```text
6.28318530718
```

```epher
e
```

```text
2.71828182846
```

اثنان آخران: `tau` هو دورة كاملة (2 pi) و`phi` هو النسبة الذهبية:

```epher
tau
```

```text
6.28318530718
```

```epher
phi
```

```text
1.61803398875
```

### 1.4 المقارنة والمنطق

يمكنك مقارنة الأرقام. النتيجة `true` (صحيح) أو `false` (خطأ):

| المقارنة | المعنى |
|---|---|
| `a > b` | a أكبر من b |
| `a < b` | a أصغر من b |
| `a >= b` | a أكبر من أو يساوي b |
| `a <= b` | a أصغر من أو يساوي b |
| `a == b` | a يساوي b (لاحظ الـ `=` المزدوج) |
| `a != b` | a لا يساوي b |

```epher
3 > 2
```

```text
true
```

```epher
1 != 2
```

```text
true
```

اجمع المقارنات بـ `and` و`or` و`not`:

```epher
3 > 2 and 2 < 3
```

```text
true
```

```epher
not 3 > 2
```

```text
false
```

### 1.5 المتغيرات

أعطِ اسماً لقيمة باستخدام `=` واحد:

```epher
x = 5
```

```text
5
```

يعيد epher إليك القيمة. من الآن فصاعداً يمكن استخدام `x` في أي مكان:

```epher
x ^ 2
```

```text
25
```

يمكنك تغيير متغير متى شئت. يحتفظ بقيمته حتى تغيّره:

```epher
x = x + 1
```

```text
6
```

> يمكن أن تحتوي الأسماء على حروف وشرطات سفلية، مثل `radius` أو `my_total`. لا يمكن أن تحتوي على مسافات ولا أن تبدأ برقم.

اسم واحد محجوز: `i`، الوحدة التخيلية (القسم 1.18).

يحمل المتغير الخاص `ans` دائمًا الإجابة السابقة، مثل مفتاح `Ans` في
الآلة الحاسبة الجيبية، مفيد للحسابات المتسلسلة:

```epher
2 + 3
ans * 2
```

```text
5
10
```

قائمة واحدة يمكن أن تملأ عدة أسماء دفعة واحدة — `{a, b} = list` تفكك
القائمة من اليسار إلى اليمين (القسم 1.11 يعرض دوال تعيد بهذه الطريقة
أكثر من إجابة):

```epher
{a, b} = {10, 20}; a + b
```

```text
{10, 20}
30
```

الموضع الذي لا تريده يُكتب `_`:

```epher
{x, _} = {7, 8}; x
```

```text
{7, 8}
7
```

### 1.6 الثوابت: أسماء لا تتغير أبدًا

*الثابت* اسم لا تتغير قيمته أبدًا، مثل `pi` المدمج، لكن باختيارك. عرّفه
بـ `const`:

```epher
const tax = 0.2
```

```text
0.2
```

استخدمه في أي مكان يصح فيه رقم:

```epher
100 * (1 + tax)
```

```text
120
```

القيمة ثابتة: تغييرها بـ `=` خطأ،

```epher
tax = 0.25
```

```text
error: cannot assign to constant tax
```

وتعريف الثابت نفسه بقيمة مختلفة كذلك:

```epher
const tax = 0.25
```

```text
error: constant already defined: tax
```

تختلف الثوابت عن المتغيرات في نقطة أخرى: مثل `pi`، تعمل داخل دوالك
الخاصة.

```epher
const g = 9.81
```

```text
9.81
```

```epher
def weight(m) = m * g
```

```epher
weight(80)
```

```text
784.8
```

احفظ ثابتًا للجلسات القادمة بـ `save tax`، تمامًا كدالة (الفصل 4.4).

> لا يمكن أن يتشارك متغير وثابت الاسم نفسه: بعد `const tax = 0.2`، يظل
> `tax = ...` خطأً دائمًا. اختر اسمًا جديدًا أو ابدأ جلسة جديدة.

### 1.7 السلاسل النصية و print

السلسلة النصية نص بين علامتي اقتباس مزدوجتين: `"hello"`. تتسلسل السلاسل بـ `+`، وتقارن بـ `==` و `!=`، وترتب بـ `<` و `>` (ترتيب المعجم)، وتُعدّ بـ `len`، وتُفهرس من 1 مثل القوائم:

```epher
"hello" + " " + "world"
len("hello")
"hello"[1]
"abc" == "abd"
"apple" < "banana"
```

```text
hello world
5
h
false
true
```

الشرطة المائلة للخلف داخل السلسلة تبدأ **تسلسل هروب**: `\n` سطر جديد،
`\t` مسافة جدولة، و`\\` و`\"` هما الشرطة المائلة للخلف وعلامة الاقتباس
نفسهما — وهكذا يمكن أن تحوي السلسلة علامة اقتباس مزدوجة:

```epher
s = "say \"hi\""
len("a\nb")
```

```text
say "hi"
3
```

**str(x)** يكتب قيمة كما يكتبها لوحة الإجابة، و**print(a, b, …)** يصل
حججته بمسافات في سطر واحد:

```epher
print("x =", 42)
```

```text
x = 42
```

مكتبة صغيرة تغطي بقية كتابة التقارير. **upper** و**lower** يغيّران حالة
الأحرف، **trim** يجرد المسافات من الطرفين، **substr** يأخذ قطعة (تبدأ
من 1 مثل كل فهرس)، **find** يخبر أين يظهر نص (0 حين لا يظهر)،
**replace** يستبدل كل نسخة من نص بنص آخر، **split** يكسر نصًا إلى قائمة
عند فاصل، **join** يلصق قائمة في نص واحد، و**fixed** يكتب عددًا بذلك
العدد بالضبط من المنازل العشرية الذي تطلبه — الصفر الذي يريده التقرير،
محفوظ:

```epher
upper("hello")
substr("2026-09-17", 1, 4)
find("hello world", "world")
replace("2026-09-17", "-", "/")
split("a,b,c", ",")
join({1, 2, 3}, "-")
fixed(3.1, 2)
```

```text
HELLO
2026
7
2026/09/17
{a, b, c}
1-2-3
3.10
```

### 1.8 اتخاذ القرارات بـ if

`if` يختار بين قيمتين:

```epher
if 3 > 2 then 10 else 20
```

```text
10
```

الصيغة دائماً `if شرط then القيمة_عند_الصحة else القيمة_عند_الخطأ`. الجزء `else` إلزامي.

مثال أكثر فائدة مع متغير:

```epher
price = 100
if price > 50 then 2 else 1
```

```text
100
2
```

> يستطيع `if` الآن مقارنة السلاسل النصية أيضًا (القسم 1.7): كل المطلوب أن
> تكون قيمتا الفرعين من النوع نفسه، أرقام مع أرقام وسلاسل مع سلاسل.

### 1.9 الحلقات بـ while

يكرر `while` عبارة ما ما دام الشرط محققاً:

```epher
x = 0; while x < 5 do x = x + 1; x
```

```text
0
5
```

اقرأ هذا النص البرمجي هكذا: *ابدأ x عند 0؛ ما دام x أصغر من 5، أضف 1 إلى x؛ ثم اعرض x.* النتيجة 5 لأن الحلقة دارت خمس مرات.

> **شبكة أمان:** يوقف epher أي حلقة بعد 100 000 خطوة ويعرض `error: step limit exceeded`. هذا يحميك من الحلقات التي لا تنتهي أبداً. إذا رأيته، فشرطك على الأرجح لم يصر خطأً أبداً.

كما يمكن الخروج من الحلقة عمدًا: القسم التالي يضيف `break` و`continue`،
والدالة تضيف `return` (القسم 1.11).

### 1.10 الحلقات باستخدام for

يكرر `for` جملة مرة واحدة عن كل قيمة، ويجمع قيم المتن في قائمة، على مدى `start to end` (شامل) مع `step` اختياري، أو على عناصر قائمة:

```epher
for i in 1 to 5 do i^2
for x in {2, 3, 4} do 10*x
for i in 0 to 1 step 0.5 do i
```

```text
{1, 4, 9, 16, 25}
{20, 30, 40}
{0, 0.5, 1}
```

نطاق متغير الحلقة هو الحلقة نفسها: بعد الانتهاء يعود الاسم إلى معناه السابق (فالحلقة على `i` لا تمس الوحدة التخيلية)، بينما تبقى الإسناد إلى أسماء أخرى داخل الجسم. ومع print، تكتب الحلقة أسطرًا مقروءة:

```epher
for i in 1 to 3 do print("line", i)
```

```text
{line 1, line 2, line 3}
```

الشبكة الأمان نفسها ذات 100,000 خطوة تحدّ حلقة for كما تحدّ while.

**مغادرة الحلقة مبكرًا.** `break` يوقف الحلقة في الحال؛ القيم المجمعة
حتى تلك اللحظة هي جواب الحلقة. `continue` يتخطى باقي الدورة وينتقل إلى
القيمة التالية. كلاهما عبارتان، لذا يوضعان خلف `if`:

```epher
for k in 1 to 6 do if mod(k, 2) == 1 then k
for k in 1 to 10 do if k == 4 then break else k
total = 0
for k in 1 to 6 do if mod(k, 2) == 0 then continue else total = total + k
total
```

```text
{1, 3, 5}
{1, 2, 3}
0
{1, 4, 9}
9
```

اقرأ الوسطى هكذا: لِـ k من 1 إلى 10، إن كان k يساوي 4 فتوقف؛ وإلا فأعِد
k. جواب الحلقة هو القيم التي رأتها قبل أن تتوقف. الزوج الأخير يُظهر
`continue` وهو يعدّ: القائمة المجمعة من سطر for هي المجموع الجاري - 1
ثم 4 ثم 9 - وقد تُخطيت الدورات الزوجية فلم تضف شيئًا. `total` النهائي،
9، هو مجموع القيم الفردية. `if` بلا `else` لا يضيف شيئًا حين يكون شرطه
خاطئًا، وهكذا تحتفظ الحلقة الأولى بالقيم الفردية فقط، وهكذا تستطيع
الحلقة أن يُصفّي قائمة كما تحوّلها.


### 1.11 دوالك الخاصة بـ def

الدالة عملية حسابية لها اسم ومعاملات:

```epher
def f(x) = x ^ 2
```

ثم استخدمها:

```epher
f(7)
```

```text
49
```

يمكن أن تأخذ الدوال عدة معاملات:

```epher
def area(w, h) = w * h
area(3, 4)
```

```text
12
```

يمكنك أيضاً تعريف دالة دون معاملات:

```epher
def answer() = 42
answer()
```

```text
42
```

**جسم من عدة خطوات.** حين لا يكفي تعبير واحد، امنح الدالة جسمًا بـ
`do … end`. تُنفَّذ العبارات واحدة تلو الأخرى بالترتيب، وقيمة الأخيرة
هي الجواب:

```epher
def hyp(a, b) do
  c = a ^ 2 + b ^ 2
  sqrt(c)
end
hyp(3, 4)
```

```text
5
```

الاسم الوسيط `c` يعيش داخل الاستدعاء؛ لا يُرى من خارجه، ولا يرى
استدعاءان `c` أحدهما للآخر.

**return: الجواب الآن.** `return value` يجيب فورًا ويتخطى باقي الجسم —
الشكل الطبيعي لاختيار بمخرج مبكر:

```epher
def grade(s) do
  if s >= 90 then return "A"
  if s >= 80 then return "B"
  "C"
end
grade(95); grade(85); grade(40)
```

```text
A
B
C
```

`return` يترك أيضًا حلقة تبحث، في اللحظة التي تجد فيها:

```epher
def firstsq(xs) do
  for x in xs do
    if x ^ 0.5 == floor(x ^ 0.5) then return x
  0
end
firstsq({3, 5, 9, 11}); firstsq({3, 5, 7})
```

```text
9
0
```

قاعدة واحدة تُبقي الكتل مقروءة: `end` يغلق دومًا do الدالة — `if` أو
`for` أو `while` لا يأخذ `end`. الجسم الذي ينتهي بلا جواب (قولًا، كل
المسارات لم تعِد شيئًا) خطأ، لا صمت: epher يقول ذلك ويسمّي الدالة.

> **أكثر من جواب؟** أعِد قائمة — وسمّها بحركة واحدة بالتفكيك من القسم
> 1.5: `{mean, sd} = {4, 1.6}`.

### 1.12 العودية: دالة تستدعي نفسها

المثال الأشهر هو أرقام فيبوناتشي:

```epher
def fib(n) = if n <= 1 then n else fib(n - 1) + fib(n - 2)
```

```epher
fib(10)
```

```text
55
```

`fib(10)` هو رقم فيبوناتشي العاشر. تستدعي الدالة نفسها بوسائط أصغر حتى تصل إلى `n <= 1`. ينجح هذا لأن صيغة `if ... then ... else ...` تحسب الفرع الذي تحتاجه فقط.

> جسم الدالة تعبير واحد بعد `=`، أو كتلة `do … end` من عدة عبارات مع
> `return` للمخارج المبكرة (القسم 1.11). ادمج عدة عمليات حسابية في نص
> برمجي بدلاً من ذلك (القسم التالي) حين لا تحتاج إلى دالة.

### 1.13 النصوص البرمجية: عدة عبارات دفعة واحدة

*النص البرمجي* هو عدة عبارات مربوطة بـ `;` أو بأسطر جديدة، تعني الشيء نفسه تماماً، تُنفَّذ واحدة تلو الأخرى:

```epher
x = 10; y = x + 5; x + y
```

```text
10
15
25
```

النصوص البرمجية هي طريقة بناء البرامج الصغيرة: جهّز المتغيرات، وأدر الحلقات، واعرض النتيجة النهائية.

الأسطر الجديدة و`;` هما الفاصل نفسه، ويمكنك خلطهما بحرية. زر **نسخ** الموجود فوق المثال ينسخ النص البرمجي كاملاً، ويمكنك لصقه مباشرة في epher: حقل الإدخال في تطبيق الويب وتطبيق سطح المكتب، وواجهة الطرفية، و`epher repl` جميعها تشغّل كل سطر بالترتيب، تماماً كما لو كنت كتبتها واحدة تلو الأخرى. ربط عدة عبارات بـ `;` في سطر واحد يعمل في كل مكان أيضاً، بما في ذلك وضع لمرة واحدة (القسم 4.1).


يمكن أن تحمل البرامج النصية **تعليقات** - ملاحظات لك يتخطاها epher، مكتوبة بأسلوب PHP. الرمز `//` أو `#` يعلّق حتى نهاية السطر، و`/* ... */` يعلّق على كتلة. الملاحظة الخاصة بعبارة واحدة تقع في نهايتها بعد `//`، وليس داخل العبارة أبداً:

```epher
// a small script with notes
r = 3 # radius in metres
area = pi * r ^ 2 // pi r squared
area
```

A block comment may also span several lines. A script file or a pasted
program arrives as one whole program, so its block comments may span
lines:

```epher
/* the area of a circle
of radius 3 */
pi * 3 ^ 2
```

Input that arrives line by line - typing into the REPL or the terminal
UI, or a piped script - takes one line at a time, so there a block
comment opens and closes on the same line.
### 1.14 نتائج دقيقة: frac وdec وbig

عادة يحسب epher بأرقام عشرية مثل الآلة الحاسبة الجيبية، وتُقرَّب النتائج إلى اثني عشر رقمًا معنويًا كما تعرضها الآلة الحاسبة: `0.1 + 0.2` هو `0.3`، وليس `0.30000000000000004` أبدًا. الكسور الدقيقة مفعّلة افتراضيًا — النتيجة ذات الكسر الجيد بمقام صغير الذي يتكرر عشريها تظهر ككسر. `1 / 3` يظهر `1/3` دون طلب:

```epher
1 / 3
```

```text
1/3
```

مع **إيقاف الكسور الدقيقة** في إعدادات النتائج (الفصل 2.2)، تُظهر القسمة نفسها `0.333333333333`. **frac(n, d)** ينشئ كسرًا دقيقًا يبقى دقيقًا عبر العمليات الحسابية:

```epher
frac(1, 3)
```

```text
1/3
```

تبقى الكسور دقيقة عبر العمليات الحسابية:

```epher
frac(1, 3) * 3
```

```text
1
```

**dec(x)** ينشئ عددًا عشريًا دقيقًا. `0.1 + 0.2` يظهر `0.3` في
الحالتين — الفرق حسابي:

```epher
0.1 * 3 - 0.3
dec(0.1) * 3 - dec(0.3)
```

```text
0.0000000000000000555111512313
0.0
```

النتيجة العائمة تحمل خطأ التقريب الصغير الذي يرتكبه كل حاسوب مع
الأعداد العشرية؛ `dec()` يُبقي الحساب دقيقًا.

**big(x)** ينشئ عدداً صحيحاً دقيقاً، للقيم الأكبر من نطاق الآلة الحاسبة الجيبية:

```epher
big(10 ^ 20)
```

```text
100000000000000000000
```

تكتب **الأنظمة العددية** الأعداد الصحيحة كما يكتبها مجتمع الرياضيات:
`0b` للثنائي، `0o` للثماني، `0x` للسداسي عشري (البادئة تغير التهجئة
فقط، وليس القيمة أبدًا):

```epher
0b1010 + 0xFF
```

```text
265
```

حوّل بالعكس باستخدام **bin(x)** و**oct(x)** و**hex(x)**. تُعطي التهجئة
ذات البادئة لعدد صحيح، جاهزة للاستخدام مباشرة من جديد:

```epher
hex(255)
bin(10)
```

```text
0xff
```
0b1010
```

**exact(x)** يعيد بناء الكسر الدقيق وراء نتيجة عشرية: أي قيمة لها كسر جيد بمقام صغير تُعرض هكذا. هذا هو نفس إعادة البناء التي تستخدمها التطبيقات افتراضياً، لذلك يظهر `1 / 3` عادة مباشرة `1/3`:

```epher
exact(0.3333333333333333)
exact(0.30000000000000004)
```

```text
1/3
3/10
```

القيمة غير النسبية مثل `pi` لا تملك كسراً جيداً، لذا يتركها `exact()` كما هي.

أفعال العرض تكتب الرقم بترميز آخر. **scientific(x)** يستخدم رقماً واحداً قبل الأس، **engineering(x)** أسساً بخطوات ثلاث (يظل الجزء العشري بين 1 و1000)، و**grouped(x)** يدرج مسافات رفيعة كفواصل للآلاف:

```epher
scientific(12345)
engineering(12345)
engineering(0.5)
grouped(1234567.89)
```

```text
1.2345e4
12.345e3
500e-3
1 234 567.89
```

تقدم تطبيق الويب وTUI هذه الخيارات أيضاً كإعدادات عرض (انظر الفصلين 2.2 و5.2): كسور مضبوطة مفعلة أو معطلة، ترميز تلقائي/علمي/هندسي، وفواصل الآلاف. الإعدادات تغير طريقة عرض النتائج فقط؛ تبقى القيم تحتها أعداداً عشرية عادية.

### 1.15 الدوال المدمجة

لدى epher دوال آلة حاسبة علمية، مجمّعة حسب العائلة.

حساب المثلثات بالراديان. استخدم `deg` و`rad` للتحويل:

| الدالة | المعنى | مثال | النتيجة |
|---|---|---|---|
| `sin(x)`، `cos(x)`، `tan(x)` | الدوال المثلثية | `sin(pi / 2)` | `1` |
| `asin(x)`، `acos(x)`، `atan(x)` | الدوال المثلثية العكسية | `atan(1)` | `0.7853981633974483` |
| `atan2(y, x)` | زاوية النقطة (x, y) | `atan2(1, 1)` | `0.7853981633974483` |
| `deg(x)` | راديان ← درجات | `deg(pi)` | `180` |
| `rad(x)` | درجات ← راديان | `rad(180)` | `3.14159265359` |
| `sinh(x)`، `cosh(x)`، `tanh(x)` | الدوال الزائدية | `sinh(1)` | `1.1752011936438014` |
| `asinh(x)`، `acosh(x)`، `atanh(x)` | الزائدية العكسية | `acosh(1)` | `0` |

الأسس والجذور واللوغاريتمات (على الآلات الحاسبة `log` أساسه 10):

| الدالة | المعنى | مثال | النتيجة |
|---|---|---|---|
| `sqrt(x)` | الجذر التربيعي | `sqrt(16)` | `4` |
| `cbrt(x)` | الجذر التكعيبي | `cbrt(-27)` | `-3` |
| `root(n, x)` | الجذر النوني | `root(3, 8)` | `2` |
| `exp(x)` | e مرفوعة للقوة x | `exp(1)` | `2.71828182846` |
| `ln(x)` | اللوغاريتم الطبيعي | `ln(e)` | `1` |
| `log(x)` | لوغاريتم أساس 10 | `log(100)` | `2` |
| `log2(x)` | لوغاريتم أساس 2 | `log2(8)` | `3` |
| `logb(b, x)` | لوغاريتم بالأساس b | `logb(2, 8)` | `3` |
| `hypot(a, b)` | الوتر | `hypot(3, 4)` | `5` |
| `5!` (وأيضاً `fact(n)`) | مضروب | `5!` | `120` |

التقريب والإشارات والأعداد الصحيحة:

| الدالة | المعنى | مثال | النتيجة |
|---|---|---|---|
| `abs(x)` | القيمة المطلقة | `abs(-3)` | `3` |
| `floor(x)` / `ceil(x)` | تقريب لأسفل / لأعلى | `floor(2.7)` | `2` |
| `round(x)` | الأقرب، والنصف يبتعد عن الصفر | `round(2.5)` | `3` |
| `trunc(x)` | حذف الجزء العشري | `trunc(-2.9)` | `-2` |
| `sign(x)` | -1 أو 0 أو 1 | `sign(-5)` | `-1` |
| `ncr(n, r)` | التوافيق | `ncr(52, 5)` | `2598960` |
| `npr(n, r)` | التباديل | `npr(5, 2)` | `20` |
| `gcd(a, b)` / `lcm(a, b)` | القواسم والمضاعفات المشتركة | `gcd(12, 18)` | `6` |
| `mod(a, b)` | الباقي | `mod(7, 3)` | `1` |

الأعداد الأولية والقواسم تعمل على الأعداد الصحيحة:

| الدالة | المعنى | مثال | النتيجة |
|---|---|---|---|
| `isprime(n)` | صحيح إذا كان n أوليًا | `isprime(97)` | `true` |
| `nextprime(n)` / `prevprime(n)` | أقرب الأعداد الأولية | `nextprime(10)` | `11` |
| `factors(n)` | تحليل إلى عوامل أولية | `factors(360)` | `2^3 * 3^2 * 5` |
| `totient(n)` | دالة أويلر | `totient(12)` | `4` |
| `ndivisors(n)` | عدد القواسم | `ndivisors(360)` | `24` |
| `modpow(b, e, m)` | b مرفوعًا للأس e، والباقي قسمة m، بدقة | `modpow(2, 10, 1000)` | `24` |


الإحصاء يقبل أي عدد من الوسائط:

| الدالة | المعنى | مثال | النتيجة |
|---|---|---|---|
| `sum(...)` / `product(...)` | المجاميع | `sum(1, 2, 3)` | `6` |
| `mean(...)` | المتوسط | `mean(1, 2, 3)` | `2` |
| `median(...)` | القيمة الوسطى | `median(1, 2, 3, 4)` | `2.5` |
| `min(...)` / `max(...)` | الأصغر / الأكبر | `max(4, 1, 3)` | `4` |
| `variance(...)` / `stdev(...)` | تشتت القيم | `stdev(2, 4)` | `1` |

طبقات القسم 1.14 الدقيقة تبقى كما هي:

| الدالة | المعنى | مثال | النتيجة |
|---|---|---|---|
| `frac(n, d)` | كسر دقيق | `frac(1, 3)` | `1/3` |
| `dec(x)` | عدد عشري دقيق | `dec(0.1)` | `0.1` |
| `big(x)` | عدد صحيح دقيق | `big(10 ^ 20)` | `100000000000000000000` |
| ثنائي، ثماني، سداسي عشري | `0b…`, `0o…`, `0x…` | `0xFF + 0b1` |
| تهجئة الأساس | `bin(x)`, `oct(x)`, `hex(x)` | `hex(255)` |
| الأعداد الأولية | `isprime(n)` و`factors(n)` وغيرها | `factors(360)` |
| `bin(x)` / `oct(x)` / `hex(x)` | تهجئة ببادئة في الأساس 2 / 8 / 16 | `hex(255)` | `0xff` |

تتحد معاً مثل أي شيء آخر:

```epher
min(sqrt(16), 5)
```

```text
4
```

الثوابت الفيزيائية تستخدم وحدات النظام الدولي، مثل الثوابت الفلكية في القسم 1.29:

| الاسم | المعنى | القيمة |
|---|---|---|
| `G` | ثابت الجذب النيوتوني | 6.6743e-11 |
| `gamma` | ثابت أويلر-ماسكيروني | 0.577215664902 |
| `q_e` | الشحنة الأولية | 1.602176634e-19 |
| `ev` | إلكترون فولت بالجول | 1.602176634e-19 |
| `eps_0` | سماحية الفراغ | 8.8541878128e-12 |
| `mu_0` | نفاذية الفراغ المغناطيسية | 1.25663706212e-6 |
| `z_0` | معاوقة الفراغ | 376.730313668 |
| `m_e` | كتلة الإلكترون | 9.1093837139e-31 |
| `m_p` | كتلة البروتون | 1.67262192595e-27 |
| `m_n` | كتلة النيوترون | 1.67492750056e-27 |
| `m_u` | وحدة الكتلة الذرية | 1.66053906892e-27 |
| `a_0` | نصف قطر بور | 5.29177210544e-11 |
| `alpha` | ثابت البناء الدقيق | 0.0072973525643 |
| `r_inf` | ثابت ريدبرغ | 10973731.568160 |
| `mu_b` | مغنطون بور | 9.2740100783e-24 |
| `n_a` | ثابت أفوجادرو | 6.02214076e23 |
| `faraday` | ثابت فاراداي، C/mol | 96485.33212 |
| `r_gas` | ثابت الغازات العام | 8.31446261815 |
| `atm` | جو قياسي بالباسكال | 101325 |
| `wien` | ثابت طول موجة فين | 0.002897771955 |
| `phi_0` | كمية الفيض المغناطيسي | 2.067833848e-15 |
| `m_P` | Planck-Masse | 2.176434e-8 |
| `l_P` | Planck-Länge | 1.616255e-35 |
| `t_P` | Planck-Zeit | 5.391247e-44 |
| `r_e` | klassischer Elektronenradius | 2.8179403205e-15 |
| `lambda_c` | Compton-Wellenlänge | 2.42631023867e-12 |
| `mu_n` | Kernmagneton | 5.050783699e-27 |


### 1.16 قراءة الأخطاء

عندما يحدث خطأ، يخبرك epher بدلاً من التخمين:

```epher
1 / 0
```

```text
error: division by zero
```

```epher
sqrt(-4)
```

```text
2i
```

```epher
unknown_name
```

```text
error: unknown name: unknown_name
```

```epher
foo(1)
```

```text
error: unknown name: foo
```

المثال الأخير مهم: يخبرك epher بالضبط بالاسم الذي لا يعرفه، لتتمكن من إصلاح تعبيرك.

### 1.17 مرجع سريع

| الوظيفة | الصيغة | مثال |
|---|---|---|
| جمع، طرح، ضرب، قسمة | `+ - * /` | `7 / 2` |
| الأس | `^` (من اليمين إلى اليسار) | `2 ^ 10` |
| المضروب | `!` (لاحقة) | `5!` |
| النسبة المئوية | `%` (لاحقة) | `200 * (1 + 10%)` |
| الأقواس | `( )` | `(2 + 3) * 4` |
| الثوابت | `pi`, `e`, `tau`, `phi` | `2 * pi` |
| الترميز العلمي | `2.5e-3` | `6.02e23` |
| المقارنة | `> < >= <= == !=` | `3 >= 2` |
| المنطق | `and or not` | `a > 1 and a < 10` |
| متغير | `name = value` | `x = 5` |
| ثابت | `const name = value` | `const tax = 0.2` |
| قرار | `if c then a else b` | `if x > 0 then 1 else -1` |
| اختيار عبارات | `if c then جملة [else جملة]` | `if k == 4 then break` |
| حلقة | `while c do statement` | `while x < 5 do x = x + 1` |
| حلقة for | `for i in a to b step s do جملة` | `for i in 1 to 5 do i^2` |
| مغادرة حلقة / تخطيها | `break`, `continue` | `if k == 4 then break` |
| دالة | `def name(params) = expr` أو `do … end` | `def f(x) = x ^ 2` |
| الجواب الآن | `return value` | `if ok then return x` |
| عدة أسماء | `{a, b} = list` (`_` يتخطى) | `{m, sd} = stats(d)` |
| السلاسل النصية | `"..."`، `+` يصل، `s[i]`، `==`، `<` | `"a" + "b"` |
| مكتبة السلاسل | `upper` `lower` `trim` `substr` `split` `join` `find` `replace` `fixed` | `split("a,b", ",")` |
| Print | `print(a, b, ...)` | `print("x =", 42)` |
| نص برمجي | عبارات مربوطة بـ `;` أو أسطر جديدة | `x = 1; x + 1` |
| كسر دقيق | `frac(n, d)` | `frac(1, 3)` |
| عدد عشري دقيق | `dec(x)` | `dec(0.1) + dec(0.2)` |
| عدد صحيح دقيق | `big(x)` | `big(10 ^ 20)` |
| إعادة بناء كسر | `exact(x)` | `exact(0.3333333333333333)` |
| علمي، هندسي، مجمّع | `scientific(x)` `engineering(x)` `grouped(x)` | `engineering(12345)` |
| الوحدة التخيلية | `i`، أو صيغة `4i` | `sqrt(-1)` |
| أجزاء عدد مركب | `re(z)` `im(z)` `arg(z)` `conj(z)` `abs(z)` | `re(3 + 4i)` |
| حل معادلة | `solve lhs == rhs` | `solve x^2 == 9` |
| مشتقة عددية | `derivative(expr, x)` | `derivative(x^2, 3)` |
| تكامل محدد | `integral(expr, a, b)` | `integral(x^2, 0, 3)` |
| ثنائي، ثماني، سداسي عشري | `0b…`, `0o…`, `0x…` | `0xFF + 0b1` |
| تهجئة الأساس | `bin(x)`, `oct(x)`, `hex(x)` | `hex(255)` |
| الأعداد الأولية | `isprime(n)` و`factors(n)` وغيرها | `factors(360)` |
| حرفية قائمة | `{…}` | `{1, 2, 3}` |
| عنصر قائمة | `list[i]` (يبدأ من 1) | `{5, 6}[2]` |
| إحصاء قائمة | `mean(قائمة)`, `median(قائمة)`, … | `stdev(d)` |
| شكل قائمة | `len(s)`, `sort(s)`, `mode(s)`, `range(s)`, `quartile(s, k)` | `quartile(d, 1)` |
| انحدار خطي | `linreg(xs, ys)` | `linreg(x, y)` |
| عائلة الانحدار | `quadreg` `expreg` `powreg` `logreg` | `quadreg(xs, ys)` |
| عائلة طبيعية | `normpdf` `normcdf` `invnorm` | `invnorm(0.975)` |
| عائلة t | `tpdf` `tcdf` `invt` | `invt(0.975, 10)` |
| عائلة كاي تربيع | `chi2pdf` `chi2cdf` `invchi2` | `chi2cdf(3.84, 1)` |
| عائلات متقطعة | `binompdf` `binomcdf` `poissonpdf` `poissoncdf` | `binomcdf(2, 10, 0.5)` |
| اختبارات وفترات | `ztest` `ttest` `zinterval` `tinterval` `chisq_gof` | `tinterval(d, 0.95)` |
| ANOVA و t المزدوج | `anova(قوائم...)`، `ttestpaired(a, b)` | `anova(g1, g2, g3)` |
| مخططات بيانات | `graph scatter(xs, ys)` `histogram(data)` `boxplot(data)` | `graph boxplot(d)` |
| أعداد عشوائية | `random()`, `random(a, b)`, `randint(a, b)`, `randseed(n)` | `randint(1, 6)` |
| سحبات طبيعية | `randn(mu, sigma)` | `randn(0, 1)` |
| مستعرض الثوابت | المساعدة → الثوابت: كل ثابت مدمج، مجمّعًا | المساعدة → الثوابت |
| كمية | `5 m`, `60 mile/hr`, `1 km` | `2 m^2` |
| تحويل | `expr in وحدة` أو `expr -> وحدة` | `72 km/hr in m/s` |
| سوابق | `k M G T m µ n p` تدرّج أي وحدة | `5 km`, `3 MPa`, `1 GHz` |
| و، أو على مستوى البت | `a & b`, `a \| b` | `0xFF & 0x0F` |
| أو الحصري على مستوى البت | `a xor b` | `5 xor 3` |
| نفي على مستوى البت | `~a` | `~0` |
| إزاحات | `a << n`, `a >> n` | `1 << 8` |
| حجم الكلمة | `bits(n)` لـ 8 و16 و32 و64 | `bits(8)` |
| علاقة ضمنية | `graph lhs == rhs` | `graph x^2 + y^2 == 1` |
| حرفية مصفوفة | `[[1, 2], [3, 4]]` | `[[1, 2], [3, 4]] * [[5, 6], [7, 8]]` |
| دوال مصفوفات | `det` `inv` `transpose` `trace` `dim` `ref` `rref` | `rref([[2, 1, 5], [1, -1, 1]])` |
| حالّ TVM | `tvm_n` `tvm_i` `tvm_pv` `tvm_pmt` `tvm_fv` | `tvm_pmt(360, 0.08/12, -100000, 0)` |
| صافي القيمة الحالية ومعدل العائد الداخلي | `npv(rate, flows)` `irr(flows)` | `irr({-100, 60, 60})` |
| استهلاك | `amort(p, r, n, k)` | `amort(1000, 0.01, 12, 6)` |
| فائدة | `simple_interest` `compound_interest` | `compound_interest(1000, 0.05, 2)` |

### 1.18 الأعداد المركبة

يحسب epher بالأعداد المركبة تلقائياً. الوحدة التخيلية هي **i**، تماماً مثل `pi`:

```epher
i ^ 2
sqrt(-1)
```

```text
-1
i
```

بخلاف كل اسم مدمج آخر، لا يمكن إعادة استخدام `i`: يتم رفض `i = 5`، فلا يمكن أن تطال الوحدة التخيلية بالظل أبدًا - لا بإسناد ولا بجلسة قديمة محفوظة.

اكتب عدداً مركباً باللاحقة `i` دون علامة ضرب: `3 + 4i` صيغة واحدة، و`2.5i` تعمل، وكذلك الصيغ ذات الأساس (`0xFFi`). الحساب المعتاد يمتد: الجمع والطرح والضرب والقسمة والقوى كلها تعمل، وتتبع `i` أسبقية العمليات العادية (`i ^ 2` يرتبط مثل أي قوة).

تمتد الدوال الحقيقية أيضاً. مع وسيط مركب تحسب في المستوى المركب؛ ومع وسيط حقيقي خارج نطاقها الحقيقي تعيد النتيجة المركبة الرئيسية بدلاً من خطأ:

```epher
ln(-1)
asin(2)
exp(i * pi)
```

```text
3.14159265359i
1.57079632679-1.31695789692i
-1+0.000000000000000122464679915i
```

(`exp(i * pi)` تساوي بالضبط `-1`؛ الأرقام الأخيرة هي ضجيج `sin(pi)` في حساب الحاسوب.)

أربع دوال تقرأ أجزاء العدد المركب، و`abs()` هي قيمته المطلقة (الطول):

```epher
re(3 + 4i)
im(3 + 4i)
arg(-1)
conj(3 - 4i)
abs(3 + 4i)
```

```text
3
4
3.14159265359
3+4i
5
```

الدوال الخاصة بالأعداد الصحيحة فقط (`fact`, `gcd`, `floor`, `isprime`, ...) ترفض الوسائط المركبة بخطأ نوع.

### 1.19 حل المعادلات

**solve** يجد جذور معادلة في متغير واحد. تستخدم المعادلة `==`:

```epher
solve x^2 == 5*x + 6
```

```text
x = -1, x = 6
```

المعادلات متعددة الحدود (المبنية من `+ - * ^` والثوابت) تعطي كل جذر، حقيقياً ومركباً:

```epher
solve x^2 == -1
solve x^2 + 2*x + 5 == 0
solve (x - 1)^2 == 0
```

```text
x = -i, x = i
x = -1-2i, x = -1+2i
x = 1
```

المتغير المطلوب حله هو `x` عند ظهوره، وإلا المتغير الوحيد الآخر. تعمل الثوابت والمتغيرات المرتبطة كمعاملات:

```epher
const k = 3
solve k*x == 12
```

```text
3
x = 4
```

أي معادلة أخرى تُمسح عددياً على -100..100: تُحصر الجذور بتغيرات الإشارة، لذلك يسرد `solve sin(x) == 0.5` كل جذر في ذلك النطاق. حدان صريحان: جذر تلمس فيه الدالة الصفر فقط (مثل `x^2 == 0` عبر المسار العددي) قد يُفوَّت، والمعادلات ذات متغيرات متعددة غير مرتبطة خطأ.

### 1.20 التفاضل والتكامل: المشتقة والتكامل

**derivative(expr, p)** هي المشتقة العددية لـ`expr` عند `p`. يبقى الوسيط الأول تعبيراً، ومتغيره الحر هو الذي يُفاضَل:

```epher
derivative(x^2, 3)
derivative(sin(t), 0)
```

```text
6
1
```

لأن الوسيط يبقى تعبيراً، يمكن رسم المشتقة: `graph derivative(x^3 - x, x)` يرسم منحنى الميل.

**integral(expr, a, b)** هو التكامل المحدد من `a` إلى `b`، محسوباً بتربيع سيمبسون التكيفي:

```epher
integral(x^2, 0, 3)
integral(sin(x), 0, pi)
```

```text
9
2
```

`integral(x^2, 3, 0)` تساوي `-9` (التكامل بإشارته)، وحد علوي قابل للرسم يعمل: `graph integral(x^2, 0, x)`.

كلاهما عددي؛ يجب أن تكون التعبيرات حقيقية القيمة عبر النطاق، والتعبير ذو متغيرات متعددة خطأ.

### 1.21 البيانات: القوائم والإحصاء والانحدار

القائمة هي عمود من الأرقام بين قوسين معقوفين: `{1, 2, 3}`. العناصر هي
تعبيرات، والقائمة الفارغة `{}` مسموحة، والقائمة ترتبط باسم مثل أي قيمة:

```epher
d = {12, 15, 14, 16, 13, 15, 14, 17}
d[2]
len(d)
```

`list[i]` هو العنصر رقم i، يبدأ العد من 1 كما في الآلة الحاسبة؛
المؤشر خارج القائمة خطأ. القوس المربع يلتصق أقوى من `^`، لذا `d[2]^2`
هي `(d[2])^2`.

العمليات الحسابية على القائمة عنصرًا بعنصر، ويُطبَّق الرقم المفرد على
كل عنصر:

```epher
{1, 2, 3} * 2
{1, 2, 3} + 10
```

يجب أن يكون طولا القائمتين متساويين في `+ - * / ^`. يقارن `==` و
`!=` القائمتين كاملتين؛ ومقارنات الترتيب ترفض القوائم.

تأخذ دوال الإحصاء قائمة واحدة كوسيط وحيد (الصيغة متعددة الوسائط باقية —
`mean(1, 2, 3)` ما زالت تعمل): `sum product mean median mode variance
stdev min max range`. دوال الشكل الجديدة هي `len(قائمة)` و`sort(قائمة)`
(نسخة تصاعدية) و`mode(قائمة)` (القيمة الأكثر تكرارًا، والأصغر عند
التعادل) و`range(قائمة)` (أكبر ناقص أصغر قيمة) و`quartile(قائمة, k)`
حيث k من 1 إلى 3 (ربيعيات بأسلوب TI، وسيط النصفين):

```epher
mean(d)
median(d)
quartile(d, 1)
```

**linreg(xs, ys)** يلائم خط المربعات الصغرى لقائمتين متساويتي الطول
ويُبلغ عنه مع معامل الارتباط r:

```epher
linreg({1, 2, 3, 4}, {2.1, 4.2, 5.8, 8.1})
```

الخط الملائم عرضٌ، مثل جذور solve؛ صورة الملاءمة تعيش في مخطط
التشتت (القسم 1.23).

بقية عائلة الانحدار يُلائم بقية عائلة الانحدار النماذج التي تكبر إليها الآلات الحاسبة: **quadreg** يُلائم `y = a*x^2 + b*x + c` (ثلاث نقاط على الأقل)، و**expreg** يُلائم `y = a*e^(b*x)`‏ (y > 0)، و**powreg** يُلائم `y = a*x^b`‏ (x و y > 0)، و**logreg** يُلائم `y = a + b*ln(x)`‏ (x > 0). كلٌّ منها يبلغ عن النموذج مع معامل الارتباط r الخاص به:

```epher
quadreg({1, 2, 3, 4}, {1, 4.1, 8.9, 16.2})
expreg({1, 2, 3}, {2.7, 7.4, 20.1})
```

```text
y = 1.05*x^2 + -0.21*x + 0.2 (r = 0.9999)
y = 0.9911*e^(1.0037*x) (r = 1)
```

قيمة r لملاءمة محوَّلة هي ارتباط الزوج المُخطَّط خطيًا، الرقم نفسه الذي يبلغ عنه TI وNumWorks. وكل نموذج يستطيع أيضًا رسم نفسه فوق مخطط انتشاري: `graph scatter(xs, ys, quadreg)` (أو expreg أو powreg أو logreg) يرسم النقاط مع منحنى ذلك النموذج.

### 1.22 التوزيعات واختبارات الفرضيات

تغطي دوال الاحتمال التوزيع الطبيعي المعياري و t لستودنت وكاي تربيع
وذات الحدين وبوزون. تأخذ عائلة التوزيع الطبيعي وسيطًا واحدًا أو
ثلاثة — الوسيط الواحد هو الطبيعي المعياري:

```epher
normcdf(1.96)
invnorm(0.975)
normcdf(12, 10, 2)
```

`normpdf(x[, mu, sigma])` و`normcdf(x[, mu, sigma])` و`invnorm(p[,
mu, sigma])`؛ و`tpdf(x, df)` و`tcdf(x, df)` و`invt(p, df)`؛
و`chi2pdf(x, df)` و`chi2cdf(x, df)` و`invchi2(p, df)`؛
و`binompdf(k, n, p)` و`binomcdf(k, n, p)`؛ و`poissonpdf(k, lambda)`
و`poissoncdf(k, lambda)`. تجيب دوال `inv*` عن السؤال المعاكس:
`invt(0.975, 10)` هي قيمة t التي تحتها 97.5% من الكتلة.

تأخذ الاختبارات قائمة بيانات وتُبلغ عن الإحصاء وقيمة p ثنائية الجانب
كنص عرض؛ وتُبلغ الفترات عن `(أسفل, أعلى)` عند المستوى الذي تسميه:

```epher
d = {12, 15, 14, 16, 13, 15, 14, 17}
ttest(d, 14)
tinterval(d, 0.95)
ztest(d, 14, 1.5)
chisq_gof({20, 30, 25, 25}, {25, 25, 25, 25})
```

```text
{12, 15, 14, 16, 13, 15, 14, 17}
t = 0.8819, p = 0.4071
(13.1594, 15.8406)
z = 0.9428, p = 0.3458
chi2 = 2, p = 0.5724
```

يستخدمان `ttest(بيانات, mu0)` و`tinterval(بيانات, مستوى)` الانحراف
المعياري للعينة (n−1)؛ ويحتاجان `ztest(بيانات, mu0, sigma)` و
`zinterval(بيانات, sigma, مستوى)` إلى sigma معلوم. و`chisq_gof(ملاحظ,
متوقع)` هو اختبار جودة المطابقة بدرجات حرية k−1. النتائج نصوص عرض:
قابلة للقراءة والنسخ، لكن الحساب لا يلمسها.

اختباران آخران بالشكل نفسه. **anova(قائمة1, قائمة2, …)** هو تحليل التباين أحادي العامل على مجموعتين أو أكثر (الأطوال غير المتساوية مقبولة) ويلبي F مع قيمة p:

```epher
anova({1, 2, 3}, {4, 5, 6}, {7, 8, 9})
```

```text
F = 27, p = 0.001
```

**ttestpaired(a, b)** هو اختبار t المزدوج: يأخذ فروقات قائمتين متساويتي الطول ويختبرها ضد 0، اختبار "قبل وبعد" الصفي:

```epher
ttestpaired({80, 85, 90}, {82, 84, 91})
```

```text
t = -0.7559, p = 0.5286
```

### 1.23 مخططات البيانات

تقبل عائلة الرسم قوائم أيضًا: مخطط التشتت والمدرج التكراري ومخطط
الصندوق. يحتكر مخطط البيانات اللوحة مثل النظام الشمسي — أحدث أمر
يفوز، و`graph clear` يفرغه.

```epher
x = {1, 2, 3, 4, 5}
y = {2.1, 4.2, 5.8, 8.1, 9.9}
graph scatter(x, y)
```

```epher
graph histogram({1, 2, 2, 3, 3, 3, 4, 5})
```

```epher
graph boxplot({1, 2, 2, 3, 3, 3, 9})
```

**scatter(xs, ys)** يرسم النقاط، وعند وجود نقطتين أو أكثر يرسم خط
المربعات الصغرى، بعنوان `y = a*x + b (r = …)` في المفتاح.
**histogram(بيانات[, خانات])** يرسم مدرجًا تكراريًا؛ عدد الخانات
اختياري (قاعدة ستيرجس افتراضيًا) ويجب أن يكون عددًا صحيحًا بين 1 و50.
**boxplot(بيانات)** يرسم مخطط الصندوق والشارب: الحد الأدنى وQ1
والوسيط وQ3 والحد الأقصى، بشاربين حتى الطرفين. تفتح النافذة ملائمة للبيانات — كلمتا النطاق `from a to b` لا تزالان غير منطبقتين — وبمجرد رسمه يتقرّب المخطط تماماً مثل مخطط المنحنيات: عجلة الفأرة والإيماءة بالقرص ومنزلق التقريب كلها تعمل، ويحفظ التصدير ما يعرضه اللوحة.

كلمة ثالثة اختيارية تختار النموذج: `graph scatter(xs, ys, quadreg)` (أو expreg أو powreg أو logreg) يرسم تلك الملاءمة بدلًا من المستقيم.
### 1.24 أعداد عشوائية

`random()` يسحب عددًا منتظمًا في `[0, 1)`، و`random(a, b)` واحدًا في `[a, b)`،
و`randint(a, b)` عددًا صحيحًا من الفترة المغلقة `[a, b]` — رمية نرد:

```epher
randseed(7)
randint(1, 6)
```

```text
7
3
```

التسلسل قابل لإعادة الإنتاج: يعيد `randseed(n)` بذر المولّد بـ `n` ويعرضه،
لذلك نفس البذرة تعيد نفس السحوبات في كل جلسة وكل واجهة.

**randn(mu, sigma)** يسحب من التوزيع الطبيعي بمتوسط mu وانحراف معياري sigma (يسمّيه Desmos ‏randomNormal ويسمّيه TI ‏randNorm):

```epher
randseed(7)
randn(0, 1)
```

```text
7
1.36499229746
```

البذرة نفسها تعيد السحبات نفسها، تمامًا كالموحدة.

### 1.25 الوحدات والتحويل

الرقم الذي يتبعه وحدة يصبح *كمية*: القيمة بوحدات SI بالإضافة إلى أبعادها.
يغطي جدول الوحدات وحدات SI الأساسية والمشتقة (`m`, `s`, `kg`, `A`, `K`,
`mol`, `cd`, `Hz`, `N`, `Pa`, `J`, `W`, `C`, `V`, `F`, `ohm`, `S`,
`Wb`, `T`, `H`, `lm`, `lx`, `Bq`, `Gy`, `Sv`)، والوحدات اليومية (`min`,
`hr`, `d`, `yr`, `L`, `t`, `bar`, `atm`, `torr`, `psi`, `eV`, `mile`,
`yd`, `ft`, `inch`, `nmi`, `lb`, `oz`, `gal`, `qt`, `pt`, `mph`,
`knot`)، ولواحق الفلك من القسم 1.29. الوحدات المركبة تتسلسل: `60
mile/hr` و`5 m/s^2` وحدتان مفردتان.

```epher
60 mile/hr
```

```text
60 mile/hr
```

سوابق SI تدرّج أيًا منها: `k M G T m µ n p` هي كيلو وميجا وجيجا وتيرا
وميلي وميكرو ونانو وبيكو — `5 km` و`3 MPa` و`1 GHz` تعمل جميعًا،
و`2 kg` هو الكيلوجرام نفسه.

تُفحص الأبعاد: جمع أو مقارنة كميات بوحدات مختلفة ينتج خطأً بدل خلط
الأمتار والثواني:

```epher
5 m + 3 s
```

```text
error: dimension error: cannot add 5 m and 3 s
```

الحساب يركّب الأبعاد: `5 m * 3 m` هي `15 m^2`، و`(3 m)^2` هي `9 m^2`،
و`sqrt(4 m^2)` هي `2 m`، والتعبير الكامل الذي تتلاشى أبعاده يعود عددًا
عاديًا (`5 m / 5 m` هو `1`). تفضّل النتائج الاسم المشتق الدقيق عندما
تطابقه الأبعاد — `5 kg * 3 m / 1 s^2` يجيب `15 N`.

**التحويل.** `expr in وحدة` (أو `expr -> وحدة`) يعرض كمية بالوحدة
المذكورة؛ يجب أن تتطابق الأبعاد. `in` أضعف الروابط بين العوامل، لذا
`5 m + 3 m in km` يحوّل المجموع كاملًا:

```epher
72 km/hr in m/s
```

```text
20 m/s
```

```epher
2 m^2 in cm^2
```

```text
20000 cm^2
```

مقاييس الحرارة (سلسيوس، فهرنهايت) ليست وحدات هنا — الكلفن وحدة، و`K`
تعمل مثل أي وحدة أخرى.


### 1.26 العمليات الثنائية

الرموز الأساسية من القسم 1.15 مصنوعة لهذا: `0b101` و`0o17` و`0xFF`. عوامل
البت تعمل على الأعداد الصحيحة وتجيب بأعداد صحيحة دقيقة:

```epher
0xFF & 0x0F
```

```text
15
```

| العامل | المعنى |
|---|---|
| `a & b` | و على مستوى البت |
| `a \| b` | أو على مستوى البت |
| `a xor b` | أو الحصري على مستوى البت |
| `~a` | نفي على مستوى البت (متمم الثنائي) |
| `a << n` | إزاحة يسار (ضرب في 2^n) |
| `a >> n` | إزاحة يمين حسابية (قسمة على 2^n، تقريب لأسفل) |

النتائج أعداد صحيحة `big` دقيقة، لذا `1 << 60` يحتفظ بكل رقم. حجم الكلمة
الافتراضي 64 بت: تُقرأ النتائج كمتمم ثنائي مع الإشارة، لذا `~0` هو -1
و`1 << 100` يلتف إلى 0. يغيّر `bits(n)` حجم الكلمة إلى 8 أو 16 أو 32 أو
64، و`bits()` يعرضه:

```epher
bits(8)
~0
```

```text
8
-1
```

الإزاحة بمقدار سالب تعكس الاتجاه (`8 << -1` هو `4`). يحتفظ `and` و`or`
المنطقيان بمعناهما؛ `&` و`|` هما كتابتا البت.


### 1.27 العلاقات الضمنية

المعادلة ذات المجهولين تُرسم كمنحنى: عائلة الرسوم تأخذ عينات من العلاقة بطريقة المربعات المتحركة وترسم محيط الصفر. الدائرة والقطع المكافئ والخط الرأسي — كل واحد بأمر واحد:

```epher
graph x^2 + y^2 == 1
```

```epher
graph y == x^2
```

```epher
graph x == 2
```

تُؤخذ عينات العلاقة على مربع `from a to b` (أو النافذة الافتراضية)، لذا `graph x^2 + y^2 == 1 from -2 to 2` يلائم نافذة الدائرة. كل ما يفعله المنحنى ينطبق: وسيلة الإيضاح تعرض المعادلة، والمنزلقات تحرك ثوابتها، والصورة تُكبَّر وتُحرَّك وتُصدَّر مثل أي رسم. تعبئات المتباينات (`y < …`, `y > …`) تبقى منحنيات مظللة؛ العلاقة لا نقاط اهتمام لها.


### 1.28 المصفوفات

المصفوفة شبكة أرقام، تُكتب كصفوف من القوائم: `[[1, 2], [3, 4]]` هي
مصفوفة 2×2. `+` و`-` عنصر بعنصر (بأشكال متطابقة)، و`*` هو حاصل ضرب
المصفوفات، والعدد يدرّج عنصرًا بعنصر، و`^` هو الأس الصحيح للمصفوفة
(`A ^ 0` هي المحايدة، لذا الأس يحتاج مصفوفات مربعة). `M[2][1]` هو عنصر
الصف 2، العمود 1 — الصفوف تُفهرس كقوائم، ابتداءً من 1.

```epher
[[1, 2], [3, 4]] * [[5, 6], [7, 8]]
```

```text
[[19, 22], [43, 50]]
```

تغطي دوال المصفوفات أرضية الصف: `det(M)` (مربعة فقط)، و`inv(M)`
(المفردة خطأ)، و`transpose(M)`، و`trace(M)` (مربعة)، و`dim(M)` (قائمة
`{صفوف، أعمدة}`)، و`ref(M)` مع `rref(M)` للاختزال. تُحل الأنظمة الخطية
عبر rref على المصفوفة الموسعة:

```epher
rref([[2, 1, 5], [1, -1, 1]])
```

```text
[[1, 0, 2], [0, 1, 1]]
```

تُقرأ الصفوف `x = 2`, `y = 1` — العمود الأخير من المصفوفة الموسعة
المختزلة. تظهر الكسور الدقيقة داخل المصفوفات كما في القوائم، لذا
`inv([[1, 2], [3, 4]])` يعرض `[[-2, 1], [3/2, -1/2]]`.


### 1.29 الفلك والمجموعة الشمسية

يتحدث epher لغة الفلك: لواحق الوحدات، والثوابت الفيزيائية، ودوال التقويم
والوقت، وفلك حي للشمس والقمر والكواكب وبلوتو. وكل ذلك يعمل دون اتصال.

**وحدات تتحدث الفلك.** اكتب رقمًا يتبعه لاحقة وحدة وسيحوّله epher فورًا إلى
وحدات النظام الدولي:

| اللاحقة | الوحدة | تُحوِّل إلى |
|---|---|---|
| `AU` أو `au` | الوحدة الفلكية | أمتار |
| `pc` | فرسخ فلكي | أمتار |
| `ly` | سنة ضوئية | أمتار |
| `deg` | درجة | راديان |
| `arcmin`, `arcsec` | دقيقة وثانية قوسية | راديان |
| `min`, `hr`, `d`, `yr` | دقيقة، ساعة، يوم، سنة يوليوسية | ثوانٍ |
| `Jy` | جانسكي | واط/م²/هرتز |

```epher
3.2 AU in m
```

```text
478713186240 m
```

```epher
sin(30 deg)
```

```text
0.5
```

مثال جاهز للنسخ لكل لاحقة:

| اللاحقة | جرّب | النتيجة |
|---|---|---|
| `AU`, `au` | `1 au in km` | `149597870.7 km` |
| `pc` | `pc / au` | `206264.806247` (au per parsec) |
| `ly` | `ly / au` | `63241.0770843` (au per light year) |
| `deg` | `90 deg in rad` | `1.57079632679` |
| `arcmin` | `sin(1 arcmin)` | `0.000290888204563` |
| `arcsec` | `sin(1 arcsec)` | `0.00000484813681108` |
| `min` | `1 min in s` | `60 s` |
| `hr` | `1 hr in min` | `60 min` |
| `d` | `1 d in hr` | `24 hr` |
| `yr` | `1 yr in d` | `365.25 d` |
| `Jy` | `mag2jy(0) Jy` | `3631 Jy` |

اللواحق جزء من قواعد اللغة، فلا يمكن لأي ثابت من المستخدم أن يغيّر معنى
`3.2 AU`، ويبقى `h` ثابت بلانك: تُكتب الساعات `hr`. تعيد الدوال عددًا بوحدات
طبيعية؛ واللاحقة تحوّل العدد إلى النظام الدولي، فـ `mag2jy(20)` عدد بالجانسكي
و`mag2jy(20) Jy` هو التدفق نفسه بالواط لكل متر مربع لكل هرتز.

**الثوابت الفلكية.** تعمل `au` و`pc` و`ly` و`c` و`g` و`h` و`h_bar` و`k_b`
و`sigma_sb` و`m_sun` و`r_sun` و`l_sun` و`m_earth` و`r_earth` و`m_moon` و`r_moon` مثل `pi`، ويمكنك
تظليلها بثوابتك الخاصة.

مثال جاهز للنسخ لكل ثابت:

| الثابت | الكمية | جرّب | النتيجة |
|---|---|---|---|
| `au` | وحدة فلكية | `1 au in km` | `149597870.7 km` |
| `pc` | فرسخ فلكي | `pc / au` | `206264.806247` |
| `ly` | سنة ضوئية | `ly / au` | `63241.0770843` |
| `c` | سرعة الضوء (m/s) | `c / 1000` | `299792.458` (km/s) |
| `g` | جاذبية معيارية (m/s²) | `g` | `9.80665` |
| `h` | ثابت بلانك (J·s) | `h` | `0.000000000000000000000000000000000662607015` |
| `h_bar` | ثابت بلانك المختزل | `h_bar` | `0.000000000000000000000000000000000105457181765` |
| `k_b` | ثابت بولتزمان (J/K) | `k_b` | `0.00000000000001380649` |
| `sigma_sb` | ثابت ستيفان-بولتزمان | `sigma_sb * 5772 ^ 4` | `12902411456/205` (تدفق سطح الشمس, نحو 62.9 MW/m²) |
| `m_sun` | كتلة الشمس (kg) | `m_sun / m_earth` | `332954.355179` |
| `r_sun` | نصف قطر الشمس (m) | `r_sun / r_earth` | `109.197928112` |
| `l_sun` | إشراق الشمس (W) | `l_sun / (4 * pi * (1 au) ^ 2)` | `1361.16646541` (الثابت الشمسي) |
| `m_earth` | كتلة الأرض (kg) | `m_earth / m_moon` | `81.342958322` |
| `r_earth` | نصف قطر الأرض (m) | `r_earth / r_moon` | `3.66697363877` |
| `m_moon` | كتلة القمر (kg) | `m_moon / m_earth` | `0.0122936271391` |
| `r_moon` | نصف قطر القمر (m) | `r_earth / r_moon` | `3.66697363877` |


**التواريخ والوقت.** تحوّل `jd(y, m, d [, hr])` و`mjd(...)` تاريخًا ميلاديًا
إلى تاريخ يوليوسي، وتقرأ `now()` اللحظة الحالية:

```epher
jd(2000, 1, 1, 12)
```

```text
2451545
```

و`delta_t(jd)` هي تصحيح TT - UT1، و`lst(jd, lon)` هي الوقت النجمي المحلي

```epher
mjd(2000, 1, 1, 12)
```

```text
51544.5
```

```epher
delta_t(jd(2026, 1, 1))
```

```text
69.1
```

```epher
lst(jd(2026, 6, 21), 0)
```

```text
17.9472349955
```

**قراءة اليوم الجولياني مجددًا.** اليوم الجولياني مجرد عدد أيام; يحتاج ترجمة قبل أن يُقرأ كتاريخ. التحويل المعياري لدى فلكيّي المجتمع (Meeus, «Astronomical Algorithms», الفصل 7; روتين `jd2cal` في IAU SOFA) يحول JD إلى **تاريخ التقويم الغريغوري** و**وقت الساعة بتوقيت UT**; والترميز هو تاريخ-وقت **ISO 8601** الذي يطبعه JPL Horizons وastropy. في epher الأشكال الثلاثة دوالًا مدمجة: `date(jd)` يعطي `{year, month, day}`, و`time(jd)` يعطي الساعة `{hour, minute, second}` بتوقيت UT, و`iso(jd)` يكتب اللحظة كاملة كنص `YYYY-MM-DDTHH:MM:SS`:

```epher
iso(jd(2000, 1, 1, 12))
```

```text
2000-01-01T12:00:00
```

```epher
date(jd(2000, 1, 1, 12))
```

```text
{2000, 1, 1}
```

```epher
time(jd(2000, 1, 1, 12))
```

```text
{12, 0, 0}
```

الثلاثة هي العكس التام لـ`jd()`: تطبيق `jd()` على ما يعيده `date()` يعطي JD الأصلي من جديد. تبديل التقويم عند 1582-10-15 معالج: قيم JD من `2299160.5` فصاعدًا تُقرأ تواريخ غريغورية, وما قبلها جوليانية, و`iso(0)` هو عصور البداية نفسها `-4712-01-01T12:00:00`. `time()` يقرّب إلى أقرب ثانية; و`iso()` يسمح لنصف ثانية بالانتقال إلى اليوم التالي ليبقى التاريخ-وقت صادقًا مع اللحظة. قيم JD دون `0` خارج نطاق التقويم وتسبب خطأ نطاق.
بالساعات لخط طول شرقي بالدرجات.

**الساعات والدقائق والثواني.** تحوّل `hms2deg(h, m, s)` المطلوع المستقيم إلى
درجات، و`dms2deg(d, m, s)` زاوية سكساجينيسيمية، و`deg2hms(x)` / `deg2dms(x)`
تكتبان الزاوية نصًا:

```epher
deg2hms(90)
```

```text
6h 0m 0s
```

```epher
hms2deg(6, 42, 14)
```

```text
12067/120
```

```epher
dms2deg(-23, 26, 0)
```

```text
-703/30
```

```epher
deg2dms(-23.4333)
```

```text
-23° 26' 0"
```

**السماء بأرقام.** أعطِ كل دالة وصول رقمًا للجرم: عطارد 1، الزهرة 2، المريخ 4،
المشتري 5، زحل 6، أورانوس 7، نبتون 8، بلوتو 9، الشمس 10، القمر 11 (الأرض هي 3،
المرصِد، وليست هدفًا أبدًا).

| الدالة | المعنى |
|---|---|
| `ra(b, jd)`, `decl(b, jd)` | المطلوع المستقيم والميل المركزي-الأرضي (درجات) |
| `dist(b, jd)` | المسافة بالوحدات الفلكية |
| `alt(b, jd, lat, lon)`, `az(b, jd, lat, lon)` | الارتفاع والسمت الأعلى-المركزيان (درجات، حقيقيان) |
| `rise(b, jd, lat, lon)`, `set(...)`, `transit(...)` | أحداث اليوم الشمسي المحلي، كتواريخ يوليوسية |
| `mag(b, jd)` | القدر الظاهري |
| `phase(b, jd)`, `illum(b, jd)` | زاوية الطور (درجات) والجزء المضيء |
| `diam(b, jd)` | القطر الزاوي (درجات) |
| `satx(5, s, jd)`، `saty(5, s, jd)`، `satz(5, s, jd)` | قمر من أقمار المشتري (s 1-4: Io وEuropa وGanymede وCallisto) أو زحل (s 1-8: Mimas وEnceladus وTethys وDione وRhea وTitan وHyperion وIapetus)، بأنصاف أقطار الكوكب (x غربًا، y شمالًا، z نحوك) |
| `satsep(5, s, jd)` | بُعد القمر عن مركز الكوكب، بالثواني القوسية |
| `satphen(5, s, jd)` | حالة القمر: 0 مرئي، 1 عبور، 2 مخفي خلف الكوكب، 3 في الظل، 4 عبور الظل |

```epher
decl(10, jd(2000, 6, 21, 1.8))
```

```text
23.437882351
```

بقية الدوال الواصلة في العمل, مثال لكل منها (التواريخ لغرينتش, 51.4779 شمالًا, 0 شرقًا):

```epher
ra(10, jd(2000, 1, 1, 12))
```

```text
281.278205832
```

```epher
dist(4, jd(2026, 6, 1))
```

```text
2.18405254195
```

```epher
alt(10, jd(2026, 6, 21, 11.97), 51.4779, 0)
```

```text
61.950168
```

```epher
az(10, jd(2026, 6, 21, 11.97), 51.4779, 0)
```

```text
178.2365282
```

```epher
mag(11, jd(2026, 6, 21))
```

```text
-9.66497374724
```

```epher
phase(11, jd(2026, 6, 21))
```

```text
100.83
```

```epher
illum(11, jd(2026, 6, 21))
```

```text
0.4061
```

```epher
diam(11, jd(2026, 6, 21))
```

```text
0.519678138889
```

دوال rise وset وtransit تجيب باليوم الجولياني; اقرأها مجددًا بـ`iso()` (أعلاه):

```epher
iso(rise(10, jd(2026, 6, 21), 51.4779, 0))
```

```text
2026-06-21T03:42:47
```

```epher
iso(transit(10, jd(2026, 6, 21), 51.4779, 0))
```

```text
2026-06-21T12:01:49
```

```epher
iso(set(10, jd(2026, 6, 21), 51.4779, 0))
```

```text
2026-06-21T20:20:50
```

خطوط العرض والطول بالدرجات، والشرق موجب. المواقع مركزية-الأرضية ما لم يُعطَ
مرصد. بلوتو يسير على مدار تقريبي صادق إلى نحو دقيقة قوسية، وهو أقل بكثير من
دقة الأجرام الأخرى؛ الكسوف والخسوف والبحث عن الاقترانات غير متضمنة.

**البصريات والضوء.** `kepler(M, e)` تحل معادلة كيبلر، و`airmass(alt)` هي كتلة
الهواء sec(z)، و`dawes(d)` قوة التمييز لفتحة بقطر d ملم بالثواني القوسية،
و`dist_mod(mu)` تحوّل معامل المسافة إلى فراسخ فلكية.

`mag2jy(m)` و`jy2mag(f)` يحولان بين القدر وكثافة التدفق:

```epher
kepler(0.3, 0.1)
```

```text
0.333333124406
```

```epher
airmass(30)
```

```text
2
```

```epher
dawes(100)
```

```text
1.16
```

```epher
dist_mod(10)
```

```text
1000
```

```epher
mag2jy(0)
```

```text
3631
```

```epher
jy2mag(363.1)
```

```text
2.5
```

**الفصول.** تعيد `march_equinox(year)` و`june_solstice(year)`
و`september_equinox(year)` و`december_solstice(year)` التاريخ اليوليوسي لكل
حدّ فصلي:

```epher
march_equinox(2000)
```

```text
1012520636/413
```

```epher
june_solstice(2000)
```

```text
2451716.57464
```

```epher
september_equinox(2000)
```

```text
2451810.22761
```

```epher
iso(december_solstice(2000))
```

```text
2000-12-21T13:37:50
```

**المجموعة الشمسية في ثلاثة أبعاد.** يرسم الأمر `solar3d` النظام كاملًا:
كل مدار منحنى، وكل جرم نقطة تحمل اسمه، مع أثر يبيّن من أين أتى:

```epher
solar3d jd(2020, 7, 1)
```

اجعل الوقت ثابتًا واضغط زر التشغيل لترى الكواكب تتحرك:
`const t = now(); solar3d t`. اسحب أو استخدم مفاتيح الأسهم للدوران،
و`clear` للإفراغ، و`solar3d save file.svg` للتصدير.

يُحسب الفلك بواسطة حزمة solar-ephemeris (github.com/Protonmatter/sol)،
المتحققة مقابل JPL Horizons؛ الشكر لمؤلفها. الدقة من رتبة الثانية القوسية
للشمس والقمر والكواكب على نحو 5000 سنة حول الحاضر.
### 1.30 المالية

يتحدث epher لغة المال كما يتحدث لغة الفلك: حَلّال القيمة الزمنية
للمال، وإطفاء القروض، والفوائد، وتحليل التدفقات النقدية، كل ذلك دون
اتصال. كل ما في هذا القسم يعيد أعدادًا مجردة — لا نصوص عملة — فالنتائج
محايدة تجاه العملة وتعود مباشرة إلى الحساب. المعدلات دائمًا لكل فترة
وبصورة كسر: `0.08/12` معدل سنوي 8% يُحتسب شهريًا، و`0.01` يساوي 1%
(ولاحقة `%` من 1.2 تعمل أيضًا: `6 * 100%` تساوي 0.06).

**اتفاقية الإشارات.** يتبع الحَلّال معيار الآلات الحاسبة (TI): المال
الذي تدفعه سالب، والمال الذي تتلقاه موجب. في قرضٍ حصلت عليه، الصرف
سالب والأقساط موجبة؛ وفي خطة ادخار، الإيداعات سالبة والمُدَّخر الذي
تجمعه موجب. أي مجموعة متسقة من الحقول الخَمسة تجعل الرصيد صفرًا:

```text
pv*(1+i)^n + pmt*(1+i*begin)*((1+i)^n - 1)/i + fv = 0
```

وإذا قلبتَ الإشارات (القرض والأقساط سالبة معًا) فمعنى ذلك أن «المال
لا يتوازن أبدًا»، فيجيب الحَلّال بخطأ نطاق بدل عدد لا معنى له.

**حَلّال القيمة الزمنية للمال.** خمس دوال يحسب كل منها حقلًا واحدًا
متى دُهلت الحقول الأربعة الأخرى. `n` عدد الفترات، `i` المعدل لكل
فترة، `pv` القيمة الحالية، `pmt` القسط، `fv` القيمة النهائية:

| الدالة | تحسب |
|---|---|
| `tvm_pmt(n, i, pv, fv)` | القسط |
| `tvm_n(i, pv, pmt, fv)` | عدد الفترات |
| `tvm_i(n, pv, pmt, fv)` | المعدل لكل فترة |
| `tvm_pv(n, i, pmt, fv)` | القيمة الحالية |
| `tvm_fv(n, i, pv, pmt)` | القيمة النهائية |

الرهن الكلاسيكي بفائدة 8%: 360 قسطًا شهريًا بقيمة 733.76 على قرض
قدره 100,000:

```epher
tvm_pmt(360, 0.08/12, -100000, 0)
```

```text
733.764573879
```

الفائدة المدفوعة على كامل المدة = القسط × عددها ناقص القرض:

```epher
tvm_pmt(360, 0.08/12, -100000, 0) * 360 - 100000
```

```text
164155.246597
```

`tvm_i` يقرأ المعدل عائدًا من قسط معلَن (أقل قليلًا من 8%/12 لأن
733.76 مقرَّبة):

```epher
tvm_i(360, -100000, 733.76, 0)
```

```text
0.00666661199068
```

`tvm_n` يجيب عن «كم من الوقت». الدفع بـ900 شهريًا بدل الحد الأدنى:

```epher
tvm_n(0.08/12, -100000, 900, 0)
```

```text
203.163223431
```

نحو 203 شهرًا بدل 360. وعرض البنك يفحص نفسه بنفسه: قرض 100,000
بفائدة 5% وقسط 536.82 شهريًا هو فعلًا 30 سنة:

```epher
tvm_n(0.05/12, 100000, -536.82, 0)
```

```text
360.002521488
```

(لاحظ انقلاب الإشارات: هنا القرض مال مُتلقّى، فهو موجب، والأقساط
سالبة.)

`tvm_fv` ينمّي خطة ادخار: 200 شهريًا لمدة 40 سنة بفائدة 6%:

```epher
tvm_fv(480, 0.06/12, 0, -200)
```

```text
398298.146866
```

`tvm_pv` يُسعّر تدفّق مدفوعات: ما يجب أن يحمله صندوق اليوم ليدفع
1,500 شهريًا لعشرين سنة بفائدة 5%:

```epher
tvm_pv(240, 0.05/12, 1500, 0)
```

```text
-227287.969611
```

الإجابة سالبة لأن شراء الصندوق مال خارج اليوم. وفي الاتجاه الآخر —
كم تدَّخر شهريًا للوصول إلى هدف — اسأل `tvm_pmt` وضع الهدف في `fv`:
بلوغ 50,000 في عشر سنوات بفائدة 5% يكلف 322 شهريًا:

```epher
tvm_pmt(120, 0.05/12, 0, -50000)
```

```text
321.994242862
```

كل دالة من الخَمس تقبل وسيطًا أخيرًا اختياريًا `begin`: القيمة 0 تعني
أن الدفع في نهاية كل فترة (الافتراضي)، والقيمة 1 في بدايتها (دفع
مقدَّم — الإيجار ومعظم الرواتب). المدفوعات في بداية الفترة تكسب
فائدة لفترة إضافية، فينخفض القسط قليلًا:

```epher
tvm_pmt(360, 0.08/12, -100000, 0, 1)
```

```text
728.90520584
```

يغطي البحث عن المعدلات حتى 100% لكل فترة، والبحث عن المدد حتى عشرة
ملايين فترة؛ وأي مسألة خارج هذين الحدين (أو نمط إشارات لا يتوازن
أبدًا) تعيد خطأ نطاق يذكر ما جرى تجريبه.

**الإطفاء.** `amort(p, r, n, k)` هو الرصيد المتبقي بعد k قسطًا من قرض
قيمته p بمعدل r على n فترة — عند k يساوي 0 يكون أصلًا، وعند k يساوي
n يكون صفرًا:

```epher
amort(100000, 0.08/12, 360, 120)
```

```text
87724.7039064
```

بعد عشر سنوات من رهن الـ8% يبقى 87,725 مستحقًا. وحلقة (1.10) تحوّل
ذلك إلى جدول إطفاء، سطر لكل خمس سنوات:

```epher
for k in 0 to 360 step 60 do amort(100000, 0.08/12, 360, k)
```

```text
{100000, 95069.8567174, 87724.7039064, 76781.5595143, 60477.9628062, 36188.1192209, 0}
```

**الفوائد.** `simple_interest(p, r, t)` هي `p*r*t` و
`compound_interest(p, r, n)` هي `p*(1+r)^n - p` — كلتاهما تعيد
الفائدة المكتسبة، لا الرصيد:

```epher
simple_interest(1000, 0.05, 2)
```

```text
100
```

```epher
compound_interest(1000, 0.05, 2)
```

```text
102.5
```

أما الرصيد نفسه فهو حساب بسيط — وهذه هي فائدة الأعداد المجردة:

```epher
1000 * 1.05 ^ 2
```

```text
1102.5
```

معدلان من الحياة اليومية، مبنيان بالطريقة نفسها. المعدل السنوي
الفعلي لمعدل اسمي 6% يُركَّب شهريًا، وزمن التضاعف بقاعدة الـ72 عند
6%:

```epher
(1 + 0.06/12) ^ 12 - 1
```

```text
0.0616778118645
```

```epher
72 / (6 * 100%)
```

```text
12
```

**تحليل التدفقات النقدية.** `npv(r, flows)` تخصم قائمة تدفقات نقدية
بالمعدل r: `flows[1]` هو المصروف اليوم، والبقية تصل كل فترة. أما
`irr(flows)` فتجد المعدل الذي تنعدم عنده القيمة الحالية الصافية.
ادفع 100 اليوم واحصل على 60 في كل من السنتين التاليتين:

```epher
npv(0.1, {-100, 60, 60})
```

```text
500/121
```

```epher
irr({-100, 60, 60})
```

```text
0.130662386292
```

الاستثمار يدر 13.07%. (`500/121` هي عرض epher بالكسر الدقيق، 1.14:
قيمة عشريتها متكرر فتُعرض كسرًا؛ `dec(500/121)` تكتب 4.13223140496.)
قاعدة القرار: اقبل المشروع عندما تكون `npv` بمعدل الحد الأدنى عندك
موجبة — أي عندما تتفوق `irr` على الحد الأدنى. عند حد أدنى 15% يفشل
هذا المشروع:

```epher
npv(0.15, {-100, 60, 60})
```

```text
-1300/529
```

قائمة تدفقات لا تغيّر حدودها إشارتها أبدًا ليس لها معدل ذو معنى؛ وفي
هذه الحالة تعيد `irr` خطأ نطاق.

**سطور يومية.** معدل النمو السنوي المركّب لاستثمار صار من 1,000 إلى
2,400 في خمس سنوات، والعائد الحقيقي لعائد اسمي 7% مقابل تضخم 2%
(وهو العرض بالكسر الدقيق من جديد؛ `dec(5/102)` تساوي
0.0490196078431):

```epher
(2400/1000) ^ (1/5) - 1
```

```text
0.191357898167
```

```epher
1.07 / 1.02 - 1
```

```text
5/102
```

تضم مجموعة السكربتات 42 سكربت مالي جاهزًا مبنيًا على هذه الدوال
العشر — أربعة مجلدات: الفوائد (interest)، والاستثمار (investing)،
والقروض (loans)، والادخار (savings) — كل منها مع نسخة مُتحقَّق منها
ضد المحرك (scripts.html يسردها بملخصات من سطر واحد).

## 2. تطبيق الويب (PWA)

### 2.1 فتحه

تطبيق الويب موجود على:

```text
https://epher.org/pwa/
```

لا حاجة للتثبيت. يعمل في أي متصفح حديث على الحاسوب أو الهاتف أو الجهاز اللوحي.

هذا الدليل مدمج أيضاً في التطبيق: افتح **Help → In-app user guide** في شريط القوائم (المس **☰** على الهاتف) لقراءته داخل التطبيق، بلغة التطبيق الحالية. المس أي مثال في ذلك الدليل لتحميله في حقل الإدخال. يفتح **المساعدة → الثوابت** مستعرض الثوابت: كل ثابت مدمج في مجموعات (رياضيات، فلك، فيزياء، كيمياء)، كل واحد بقيمته ووصف مختصر؛ المس واحدًا لإدراج اسمه في حقل الإدخال، ويصفّي صندوق البحث القائمة.

### 2.2 أول عملية حساب لك

1. انقر حقل النص (يكون مركَّزاً بالفعل عند تحميل الصفحة).
2. اكتب تعبيراً، مثلاً `2 + 3 * 4`.
3. اضغط **Enter** أو انقر زر **=**.

تظهر النتيجة بخط كبير أسفل الحقل. كل ما في الفصل 1 يعمل هنا، بما في ذلك المتغيرات والدوال والنصوص البرمجية. الإجابة القصيرة المنفردة تبقى في ذلك السطر. والإجابة الأطول - مخرجات نص برمجي عدة إجابات، أو جدول، أو رقم طويل - تظهر في لوحة النتائج، اللوحة التي تعرض الرسوم البيانية أيضاً، بإجابة واحدة في كل سطر؛ وعلى الهاتف تنزلق اللوحة إلى العرض من تلقاء نفسها. على يسار الإجابة أيقونة نسخ - اضغطها فتنتقل قيم الإجابة إلى الحافظة، إجابة في كل سطر؛ والإجابات الطويلة في لوحة النتائج تُنسخ بالطريقة نفسها.

أثناء كتابة اسم، تظهر قائمة اقتراحات أسفل الحقل: الأسهم تحرك التحديد، و**Enter** أو **Tab** يقبل، و**Esc** يغلق، والنقرة تقبل دون مغادرة لوحة المفاتيح. كل اقتراح يحمل وصفاً موجزاً للدالة أو الثابت. و**F1** يعرض الوصف نفسه للكلمة عند المؤشر في شريط التلميحات فوق لوحة المفاتيح. وإذا كان أول ما تكتبه في حقل فارغ عاملًا (`+ - * / ^ % !`)، يضيف epher‏ `ans` تلقائياً، فيكمل السطر من الإجابة السابقة.

لوحة المفاتيح على الشاشة تكتب عنك، تماماً كما لو ضغطت المفاتيح بنفسك. مجموعاتها تغطي اللغة كلها: `123` أرقام وعوامل، و`trig`، و`ƒ` دوال، و`nΣ` أعداد وإحصاء، و`data` مصفوفات وقوائم ونصوص (المجموعة التي تكتب `[[1, 2], [3, 4]]` و`{1, 2, 3}` و"text")، و`0x` تحويلات وأساسات، و`dist` عائلات الانحدار والتوزيعات، و`$` مالية، و`π∇` ثوابت وأوامر رسم، و`☉` فلك (المجموعة الوحيدة التي تمرر). كل دالة في هذا الدليل على مفتاح ما. فعّل **key hints** (زر **?**) لتسمية كل مفتاح، أو استقر على مفتاح لقراءة ما يفعله.

تحتوي قائمة **الإعدادات** (أيقونة الترس، أو **☰ → الإعدادات** على الهاتف) على ثلاث مجموعات. **المظهر** و**اللغة** يفعلان ما يعنيه اسماهما. **النتائج** تشكل عرض الإجابات: كسور مضبوطة (مفعلة افتراضياً، لذا يظهر `1 / 3` كـ`1/3`)، الترميز (تلقائي أو علمي أو هندسي)، وفواصل الآلاف. هذه إعدادات عرض فقط؛ تبقى القيم تحتها أعداداً عادية.

### 2.3 السجل

تُضاف كل عملية حساب إلى قائمة السجل أسفل النتيجة، لتتمكن من التمرير للخلف ورؤية ما فعلته. تظهر أحدث الإدخالات في الأعلى، وأيقونة سلة المهملات إلى يسار عنوان **السجل** تفرغها (في الطرفية، Ctrl+L أو نقرة على الأيقونة نفسها). يبقى السجل ما دامت الصفحة مفتوحة.

يقع كل عنصر بين خطوط رفيعة: التعبير ذو السطر الواحد هو صف واحد، والنص البرمجي متعدد الأسطر هو عنصر واحد يعرض كل أسطره. انقر على عنصر لإعادته إلى حقل الإدخال وتشغيله من جديد.

### 2.4 الرسوم البيانية

اكتب `graph` متبوعاً بتعبير، ثم اضغط **Enter**:

```epher
graph x ^ 2
```

يرسم epher المنحنى y = f(x) من x = −10 إلى x = 10 أسفل حقل الإدخال، على شبكة ذات محاور معنونة. يمكنك رسم أي تعبير، بما في ذلك دوالك الخاصة:

```epher
def f(x) = x ^ 3
graph f(x)
```

كل سطر `graph` يضيف منحنى آخر إلى المخطط نفسه، لكلٍّ لونه الخاص. المنحنيات كلها مصمتة، والوسيلة الإيضاحية والتسميات
هي ما يميّزها دون لون. `graph clear` يفرغ المخطط، وزر **Clear graph** أعلى لوحة الرسم البياني يفعل الشيء نفسه للمنحنيات والأسطح 3D معاً. تحتفظ واجهة الطرفية بالأمر في قائمة **Graph** الخاصة بها.

في أعلى لوحة الرسم البياني، بجانب **Clear graph** و**Copy SVG** و**Save PNG**، يتيح شريط الأدوات إخفاء قائمة نقاط الاهتمام والنقاط المميزة المرسومة على الرسم نفسه. فوق كل رسم مباشرة يوجد شريط من المنزلقات المعنونة بالأيقونات، والكلمات في تلميح كل واحد منها: سماكة الخط (من 0 إلى 4 بخطوات 0.1 للمنحنيات 2D، ومن 0 إلى 0.4 بخطوات 0.05 للرسوم 3D، و0.2 افتراضياً - لا يُعرض إلا منزلق النوع المعروض، وكل نوع يحفظ قيمته الخاصة)، إضافة إلى التقريب على كل رسم، وفي العرضين 3D والشمسية سرعة الدوران الأفقي والرأسي. الضغط على أيقونة منزلق يعيده إلى قيمته الافتراضية. أمام كل مدخل في الوسيلة الإيضاحية خانة اختيار، محددة افتراضياً: إلغاء تحديدها يخفي منحنى أو سطحاً 3D أو منحنى فضائياً أو جرمًا شمسيًا من الرسم ومن نقاط اهتمامه ومن تصدير SVG.

```epher
graph x ^ 2
graph x ^ 3
```

النقاط التي لا يكون للتعبير فيها قيمة (القسمة على صفر مثلاً) تُتجاوز، تاركة فجوة في المنحنى. والقفزة التي هي في الحقيقة خط تقارب رأسي لا تُرسم أبداً كخط واصل.

#### 2.4.1 ما يمكنك رسمه

مجال من اختيارك:

```epher
graph sin(x) from 0 to 2*pi
```

منحنيات وسيطية (تمتد t من 0 إلى 2π):

```epher
graph param 2*cos(t), 3*sin(t)
```

منحنيات قطبية:

```epher
graph polar 1 + cos(theta)
```

مناطق: `y <` يظلّل المنطقة تحت المنحنى، و`y >` يظلّل فوقه:

```epher
graph y < x ^ 2
```
#### 2.4.2 قراءة المخطط

**التتبع:** حرّك المؤشر فوق المخطط، أو ركّز عليه واضغط مفاتيح الأسهم. فيُعلَّم أقرب نقطة على المنحنى، وتُعرض إحداثياتها أسفل المخطط.

**نقاط الاهتمام:** بعد كل أمر graph يجد epher جذور كل منحنى ونقاط تحوّله وتقاطعات المنحنيات، يعلّمها على المخطط، ويعرضها أسفله:

```text
root (-1, 0)   minimum (0, 0)   root (1, 0)
```

**الجداول:** أمر `table` يطبع جدول قيم (الصفوف التي لا قيمة للتعبير فيها تبقى فارغة):

إضافة اختيارية `derivative <تعبير>` تضيف عمودًا ثالثًا، وهو المشتقة
العددية لذلك التعبير عند كل x:

```epher
table x ^ 2 from -2 to 2 points 5 derivative x ^ 2
```

```text
         x           y          y'
        -2           4          -4
        -1           1          -2
         0           0           0
         1           1           2
         2           4           4
```

تتبع خلايا الجدول إعدادات النتائج: مع تشغيل الكسور الدقيقة
(الافتراضي)، تظهر القيمة التي هي كسر بسيط ككسر — يسرد `table x / 3
from 0 to 1 points 4` قيمة `1/3` بدلًا من `0.333`.
```epher
table x ^ 2 from -2 to 2 points 5
```

```text
         x           y
        -2           4
        -1           1
         0           0
         1           1
         2           4
```

بندان آخران يغطيان بقية العمل. `values <قائمة>` يأخذ عمود x من قائمة بيانات بدل الشبكة المتساوية، أعطِه بياناتك وشاهدها بعد التحويل:

```epher
d = {1, 2, 3, 4, 5}
table x ^ 2 values d
```

```text
         x           y
         1           1
         2           4
         3           9
         4          16
         5          25
```

و`exact` / `approx` يفرضان عرض الخلايا لجدول واحد: `exact` يعرض الكسور البسيطة حيث تنطبق، و`approx` يعرض الأعداد العشرية، كلٌّ منهما يتجاوز إعدادات النتائج لهذا الأمر وحده.

#### 2.4.3 المنزلقات والتصدير

عرّف ثابتاً واستخدمه في رسم بياني، فيظهر منزلق أسفل المخطط. اسحبه (أو حرّكه بمفاتيح الأسهم) ويُعاد رسم كل منحنى:

```epher
const a = 1
graph a * x ^ 2
```

كل رسم يتقرّب بالطريقة نفسها: مرر عجلة الفأرة فوقه (أو اضغط بإصبعين على شاشة لمس) للتقريب والإبعاد، ويتحرك منزلق التقريب فوق الرسم معك. الرسوم البيانية للبيانات - مخطط الانتشار والمدرج التكراري ومخطط الصندوق - تتقرّب تماماً مثل المنحنيات.

ينسخ **Copy SVG** المخطط الحالي كصورة SVG مستقلة للصقها في المستندات. الألوان مدمجة فيه والخلفية شفافة، فيناسب أي صفحة أو شريحة أو مستند. ويحفظ **حفظ PNG** الصورة نفسها كصورة نقطية بضعف الحجم، فتبقى المنحنيات واضحة، مع الخلفية الشفافة نفسها؛ يطلب تطبيق سطح المكتب مكان الحفظ، ويحفظ تطبيق المتصفح في مجلد التنزيلات (أو يطلب، حيث يتيح المتصفح ذلك). صفوف المزالق والثوابت المتحركة تقع مباشرة أسفل الرسم، فوق قائمة نقاط الاهتمام. المنزلق المتحرك أداة حقيقية: أمسك مؤشره واسحبه إلى قيمة جديدة (الإمساك يوقف التحريك أولاً)، أو حركه بمفاتيح الأسهم وهو في بؤرة التركيز.

#### 2.4.4 الأسطح 3D

يرسم `graph3d` سطحاً z = f(x, y) فوق مجال مربع (من −5 إلى 5، أو `from a to b` الذي تحدده):

```epher
graph3d x ^ 2 - y ^ 2
```

تُرسم خطوط الشبكة الأقرب إليك بشكل أسمك، فيتضح عمق الشكل. تتراكب عدة أسطر `graph3d` فوق بعضها، مثل المنحنيات، و`graph3d clear` يفرغ المخطط. دوّر المنظر بالسحب، أو ركّز على المخطط واستخدم مفاتيح الأسهم. ترسم واجهة الطرفية السطح نفسه كإطار سلكي ASCII، وتدوّره مفاتيح الأسهم.

تُرسم منحنيات الفضاء بـ `param`: `graph3d param cos(t), sin(t), t` يرسم حلزونًا على مدى t الذي تعطيه (`from 0 to 6.28318`)، أو من 0 إلى 2pi افتراضيًا. اللوحة نفسها، والدوران والتكبير والتحريك نفسها.
#### 2.4.5 التحريك

لكل منزلق زر تشغيل. يحرّك ثابته خطوة بخطوة عبر مدى المنزلق ثم يلتف ليعود إلى البداية: يجتاز المؤشر المسار كاملاً ويعود إلى البداية عند بلوغ النهاية. هذه هي الطريقة المعيارية التي تحرّك بها الآلات الحاسبة: تحرّك معاملاً، فيتحرك كل ما يستخدمه. اضغط الزر مرة أخرى للإيقاف المؤقت.

متغير "الوقت" هو مجرد ثابت تحرّكه:

```epher
const t = 0
graph sin(x - t)
```

تشغيل منزلق t يجعل الموجة تنتقل. تتحرك الأسطح 3D بالطريقة نفسها. حدّد ثابتاً أولاً، ثم شغّل منزلقَه:

```epher
const a = 1
graph3d sin(a * (x ^ 2 + y ^ 2)) from -3 to 3
```

في واجهة الطرفية، يبدأ شريط المسافة التحريك ويوقفه.

### 2.5 تثبيته واستخدامه دون اتصال

تطبيق الويب هو *تطبيق ويب تقدمي*: بعد زيارة واحدة يعمل بالكامل دون اتصال، ويمكنك تثبيته مثل تطبيق عادي.

- **Chrome أو Edge أو Android:** انقر أيقونة التثبيت في شريط العناوين (أو *Install app* في قائمة المتصفح)، ثم أكّد.
- **iPhone / iPad (Safari):** المس **Share** ← **Add to Home Screen**.
- **المتصفحات الأخرى:** ابحث عن *Install* أو *Add to Home Screen* في القائمة.

بمجرد تثبيته، شغّله من شاشتك الرئيسية أو قائمة التطبيقات. يفتح فوراً، حتى دون اتصال بالإنترنت.

### 2.6 ما لا يفعله تطبيق الويب

يحتفظ تطبيق الويب بعملك في الجلسة الحالية: يقيّم التعبيرات ويرسمها بيانياً (القسم 2.4) ويحتفظ بسجل. تعمل أوامر **save** و**save script** و**language** في إصدارات سطح المكتب وسطر الأوامر والطرفية (الفصول 3 و4 و5). في تطبيق الويب تجيب بملاحظة تفيد بأن الحفظ متاح هناك. يُتذكَّر نشاطك الأخير - السجل ومتغيراتك ودوالك وآخر إجابة تبقى بعد إغلاق التطبيق، محفوظة في هذا المتصفح فقط.

## 3. تطبيق سطح المكتب

تطبيق سطح المكتب هو نافذة عادية حول نفس تطبيق الويب. كل ما في الفصل 2 ينطبق؛ الفرق فقط في طريقة التثبيت والتشغيل.

### 3.1 التثبيت

نزّل مثبِّتاً واحداً لنظامك من موقع epher:

- **Windows:** شغّل `epher-windows-x86_64.exe`. يضع المثبِّت `epher` في PATH. افتح نافذة CMD أو PowerShell جديدة و`epher "2 + 2"` يعمل. لأن البناء غير موقَّع، اختر *More info* ← *Run anyway* عند أول تشغيل.
- **macOS:** افتح ملف `epher-macos-aarch64.dmg` واسحب epher إلى Applications. لأن البناء غير موقَّع، يحتاج أول تشغيل إلى النقر بالزر الأيمن ← **Open**.
- **Linux (Debian/Ubuntu):** حزمة `.deb`

```sh
sudo apt install ./epher-linux-x86_64.deb
```

- **Linux (Fedora/RHEL):** حزمة `.rpm`

```sh
sudo dnf install ./epher-linux-x86_64.rpm
```

- **Linux (أي توزيعة، بما فيها Arch):** AppImage. اجعله قابلاً للتنفيذ وشغّله:

```sh
chmod +x epher-linux-x86_64.AppImage
./epher-linux-x86_64.AppImage
```

**أو ثبِّت من مستودع أو متجر** — تصل التحديثات بعدها مع مدير الحزم لديك.
على ديبيان/أوبونتو:

```sh
curl -fsSL https://epher.org/apt/KEY.gpg | sudo tee /etc/apt/keyrings/epher.gpg >/dev/null
curl -fsSL https://epher.org/epher.sources | sudo tee /etc/apt/sources.list.d/epher.sources >/dev/null
sudo apt update && sudo apt install epher
```

على فيورا:

```sh
curl -fsSL https://epher.org/epher.repo | sudo tee /etc/yum.repos.d/epher.repo >/dev/null
sudo dnf install epher
```

وتتوفر نسخ ARM64 من ملفات `.deb` و`.rpm` وAppImage في صفحة التنزيل.

يحتوي كل مثبِّت على epher *كله*: تطبيق سطح المكتب وسطر الأوامر (الفصل 4) وواجهة الطرفية (الفصل 5)، بوصفها الأمر الواحد `epher`. على Linux، تضع الحزمة `epher` في `/usr/bin`.

### 3.2 الاستخدام

شغّل epher مثل أي تطبيق آخر. تحصل على نافذة بنفس واجهة تطبيق الويب: اكتب تعبيراً، واضغط **Enter** أو انقر **=**، واقرأ النتيجة. الرسوم البيانية تعمل هنا أيضاً. `graph x ^ 2` يرسم في النافذة (الفصل 2.4). يمكن تغيير حجم النافذة بحرية. يتضمن شريط القوائم **Help → In-app user guide**، الدليل نفسه الذي في هذه الصفحة، مع أمثلة تُحمَّل باللمس.

يمكنك أيضاً فتحه من الطرفية: `epher` دون وسائط (أو `epher gui`) يبدأ تطبيق سطح المكتب. على macOS، استخدم زر **Install the epher command** داخل التطبيق لوضع `epher` في PATH الطرفية.

### 3.3 التخزين: مخزن واحد مشترك مع CLI وTUI

يشارك تطبيق سطح المكتب تخزينه مع إصدارَي سطر الأوامر والطرفية. الدوال والثوابت والنصوص البرمجية والسجل وتفضيل اللغة تعيش في مكان واحد، `~/.epher` على حاسوبك (أو `EPHER_STORE_DIR`، الفصل 4.6)، وكل ما يُحفظ في إصدار متاح في البقية:

```text
def area(w, h) = w * h
save area
```

عرّف `area` في تطبيق سطح المكتب، احفظها بأمر `save`، أغلق النافذة. ثم افتح CLI وستجد `area(3, 4)` تعمل مباشرة. ويعمل العكس أيضاً: عند فتح نافذة سطح المكتب تكون الدوال والنصوص البرمجية المحفوظة في CLI أو TUI موجودة بالفعل، بما في ذلك المتغيرات التي تضعها النصوص المحفوظة. أوامر `save` و`save script` و`language` من الفصل 4 تعمل هنا تماماً بالطريقة نفسها.

الأوامر التي تكتبها في CLI أو REPL أو TUI أو تطبيق سطح المكتب تنضم جميعها إلى السجل نفسه، والجلسة تنتقل معك أيضًا: المتغيرات التي تعيّنها وقيمة `ans` تتبعك من إصدار إلى التالي. المخزن المشترك حي: عندما يكون إصداران مفتوحين معًا، ينعكس أي تغيير في أحدهما فورًا في الآخر (تطبيق سطح المكتب وواجهة الطرفية يراقبان المخزن ويحدّثان أنفسهما تلقائيًا).

> تطبيق الويب في المتصفح هو الإصدار الوحيد الذي لا يستخدم هذا المخزن. كل جلسة تعيش منفصلة (الفصل 2.6).

## 4. سطر الأوامر (CLI)

CLI هو الجانب النصي من برنامج `epher` نفسه الذي يمثله تطبيق سطح المكتب. له ثلاثة أوضاع: تقييم لمرة واحدة، ونصوص برمجية عبر الأنابيب، وجلسة تفاعلية للعمل الأطول.

للحصول على المساعدة في أي وقت، شغّل `epher --help` (كل الأوامر، مع أمثلة)
أو `epher help` (الدليل الكامل؛ في حزم لينكس هذه هي صفحة `man epher`).

كل مُثبِّت يحمل معه مجموعة السكربتات كاملة - السكربتات نفسها التي
تصفحها صفحة Scripts في الموقع - ويثبّتها بجانب البرنامج. أوامر الطرفية
القابلة للنسخ في الموقع وفي README السكربتات وفي الأسفل أدناه تشير جميعها
إلى ذلك المجلد المثبَّت، فهي تعمل بمجرد تثبيت epher:

```sh
# Debian, Ubuntu, Fedora (deb, rpm)
epher /usr/lib/epher/scripts/astronomy/moon/full-moons.epher

# Windows (PowerShell)
epher "$env:LOCALAPPDATA\epher\scripts\astronomy\moon\full-moons.epher"

# macOS
epher /Applications/epher.app/Contents/Resources/scripts/astronomy/moon/full-moons.epher
```

### 4.1 حسابات لمرة واحدة

مرِّر التعبير كوسيط:

```sh
epher "2 + 3 * 4"
```

```text
14
```

يمكنك فعل أي شيء في الفصل 1 يكون تعبيراً واحداً:

```sh
epher "if 3 > 2 then 10 else 20"
```

```text
10
```

التعبير الذي يبدأ بعلامة ناقص يعمل مباشرة:

```sh
epher "-2 + 5"
```

```text
3
```

وضع لمرة واحدة للنصوص البرمجية، من تعبير واحد وحتى برنامج كامل. تُطبع قيمة كل عبارة في سطرها الخاص:

```sh
epher "x = 10; x + 5"
```

```text
10
15
```

العبارات المربوطة بأسطر جديدة تعمل بالطريقة نفسها داخل الوسيط. كل ما في الفصل 1 متاح: المتغيرات والدوال والحلقات، كل شيء. والأسطر تتشارك جلسة واحدة، مثل نص عبر الأنابيب (القسم 4.2).

### 4.2 نصوص برمجية عبر الأنابيب

يقرأ `epher -` التعابير من الإدخال القياسي سطراً بسطر، كما تُستخدم لغات البرمجة النصية في الأنابيب:

```sh
# Linux and macOS (bash, zsh)
printf "x = 3\nx * 10\n" | epher -

# Windows PowerShell
"x = 3`nx * 10" | epher -
```

```text
= 3
= 30
```

كل ما في الفصل 1 يعمل، والأسطر تتشارك جلسة واحدة: دالة عُرِّفت في سطر مبكر متاحة لاحقاً، و`save` يكتب في المخزن نفسه كالعادة. تُطبع الأخطاء ويستمر النص. قد يضم السطر عدة عبارات مربوطة بـ `;`. الأسطر الجديدة و`;` تعني الشيء نفسه في كل مكان في epher.


يعمل الملف بالطريقة نفسها: `epher plots/sine.es` ينفّذ كل سطر من الملف بالترتيب ويطبع كل نتيجة. يُعامل الوسيط كملف عندما يسمّي ملفًا موجودًا ويحتوي على `.` أو `/` أو `\` - لذا يبقى `epher x` يقيّم الاسم `x`.
### 4.3 الجلسة التفاعلية (REPL)

ابدأها بـ `epher repl`:

```sh
epher repl
```

> `epher` دون وسائط يفتح تطبيق سطح المكتب (الفصل 3).

يعرض epher موجهه وينتظر:

```text
epher>
```

الآن اكتب أي شيء من الفصل 1، سطراً سطراً. تحتفظ المتغيرات بقيمها بين الأسطر:

```text
epher> x = 5
= 5
epher> x ^ 2
= 25
```

أمر `table` (القسم 2.4.2) يطبع جدول قيم هنا أيضاً:

```text
epher> table x ^ 2 from -2 to 2 points 5
         x           y
        -2           4
        -1           1
         0           0
         1           1
         2           4
```

يُعرض كل جواب بصيغة `تعمل سطور `graph` هنا أيضًا: تتراكم المنحنيات عبر السطور، ويكتب
`graph save plot.svg` نفس صورة SVG التي يعطيها زر **Copy SVG** في تطبيق
الويب. ويحفظ `graph3d save file.svg` سطحًا ثلاثي الأبعاد بالطريقة نفسها.
وتعمل السطور نفسها في التقييم المفرد وفي السكربتات المُمرَّرة:
`epher "graph sin(x); graph save plot.svg"` رسم كامل في أمر واحد.

= النتيجة`. للخروج اكتب `quit` (أو `exit`):

```text
epher> quit
```

يُتذكر سجلك: في المرة القادمة التي تشغّل فيها `epher repl`، ستجد أسطر الجلسة السابقة ما زالت موجودة.


ينفّذ الأمر `load` برنامجًا نصيًا - مسار ملف أو اسم برنامج حفظته بـ `save script` - سطرًا بسطر، تمامًا كأنك كتبته بنفسك:

```text
epher> load plots/sine.es
epher> load my_setup
```
### 4.4 حفظ الدوال والثوابت والنصوص البرمجية

عرّف دالة، ثم احفظها:

```text
epher> def fib(n) = if n <= 1 then n else fib(n - 1) + fib(n - 2)
epher> save fib
saved fib
```

يحفظ أمر `save fib` الدالة على القرص. في المرة القادمة التي تبدأ فيها الجلسة، تكون `fib` معرَّفة مسبقاً:

```text
epher> fib(10)
= 55
```

تُحفظ الثوابت بالطريقة نفسها. `save` مع اسم الثابت:

```text
epher> const tax = 0.2
= 0.2
epher> save tax
saved tax
```

لحفظ نص برمجي كامل (آخر سطر كتبته) استخدم `save script`:

```text
epher> x = 0; while x < 5 do x = x + 1; x
= 5
epher> save script count_to_five
saved script count_to_five
```

تُنفَّذ النصوص البرمجية المحفوظة تلقائياً عند بدء epher، لذا كل ما تعرّفه جاهز لك.


يمكنك أيضًا تحميل برنامج محفوظ متى شئت بـ `load count_to_five`، أو إبقائه ملفًا عاديًا وتشغيل `load count_to_five.es`؛ و`epher count_to_five.es` يشغّله مباشرة من سطر الأوامر (القسم 4.2).
### 4.5 تغيير لغة الواجهة

تُختار لغة الواجهة من اللغات المضبوطة على جهازك. لتغييرها، اكتب `language` متبوعاً بواحدة من: `en`، `zh-CN`، `hi`، `es`، `fr`، `ar`، `de`، `pt`:

```text
epher> language fr
language set to fr
```

يُتذكر الاختيار للمرة القادمة. لاحظ: اللغة التي *تكتب* بها، لغة التعبيرات، تبقى نفسها دائماً، بأي لغة واجهة.

### 4.6 أين توجد بياناتك

تُخزَّن الدوال والنصوص البرمجية والسجل واختيار اللغة في مجلد واحد على حاسوبك:

```text
~/.epher
```

احذف هذا المجلد لتبدأ من الصفر تماماً. لاستخدام موقع آخر، اضبط متغير البيئة `EPHER_STORE_DIR` قبل تشغيل epher:

```sh
EPHER_STORE_DIR=/tmp/my-epher epher repl
```

## 5. واجهة الطرفية (TUI)

TUI هو نسخة ملء الشاشة من الجلسة التفاعلية، داخل طرفيتك. هو جزء من برنامج `epher` نفسه. ابدأه بـ:

```sh
epher tui
```

### 5.1 الشاشة

تنقسم الشاشة إلى لوحات:

- **تعبير**: مربع الإدخال (أعلى). يبدأ Shift+Enter سطرًا جديدًا؛
  وتحرّك مفاتيح الأسهم أو نقرة الفأرة المؤشر داخل النص.
- **النتيجة** الحالية أسفله مباشرة.
- **السجل**: كل سطر كتبته، مع جوابه.
- **الرسم البياني**: مخطط أمر `graph` (أسفل).
- سطر تلميحات يعرض اختصارات لوحة المفاتيح.

### 5.2 المفاتيح

| المفتاح | الإجراء |
|---|---|
| الكتابة | إضافة إلى التعبير عند المؤشر |
| **Enter** | تقييم النص البرمجي كله (يدخل الإدخال متعدد الأسطر كعنصر واحد في السجل) |
| **Shift+Enter** | بدء سطر جديد |
| **← → ↑ ↓** | تحريك المؤشر (مع إدخال فارغ: تدوير العرض 3D) |
| **Esc** | مسح سطر الإدخال |
| **F1** | وصف الدالة عند المؤشر (في سطر النتيجة) |
| **Ctrl+C** | خروج |
| **q** | خروج (عندما يكون الإدخال فارغاً) |
| **Arrow keys** | تدوير العرض 3D (عندما يكون الإدخال فارغاً) |
| **Space** | تشغيل/إيقاف التحريك (عندما يكون الإدخال فارغاً) |
| **F9** | فتح القوائم (File، Edit، Graph، Settings، Help) |
| **Tab** | تركيز لوحة المفاتيح الظاهرة دائمًا (أو السجل، من لوحة المفاتيح)؛ تبديل مجموعاتها (**Esc** يعيد التركيز إلى الكتابة) |
| **الفأرة** | انقر القوائم وعناصرها، وخلايا لوحة المفاتيح وتبويباتها، وسطور السجل (تحمّل التعبير)؛ اسحب لوحة الرسم للتدوير (3D) أو التحريك (2D)، وعجلة الفأرة للتقريب، والنقر المزدوج يعيد ضبط العرض |
| **Ctrl+L** | مسح السجل |

يفتح **المساعدة** الدليل المدمج، ومساعدة مفاتيح لوحة المفاتيح، ومستعرض ثوابت: الثوابت في مجموعات، والأسهم تختار صفًا، و**Enter** يُدرج اسمه في التعبير عند المؤشر، و**Esc** يغلق.

تحتوي مجموعات لوحة المفاتيح على كل دالة وثابت وأمر تدعمه اللغة, بترتيب تبويبات لوحة الويب: **trig** و**fn** و**num** و**data** و**dist** و**fin** و**0x** و**var** و**astro** (مجموعة الأرقام هي **123**). تحمل مجموعة 0x التحويلات الدقيقة وتحويلات الأساس (`frac` و`dec` و`big` و`bin` و`oct` و`hex`) والمضروب `!`. أسهم الاتجاهات تحرك التحديد، و**Enter** يُدرج الرمز، و**Tab** يبدّل المجموعات. العامل في بداية سطر فارغ (أو المُدرج من لوحة المفاتيح) يضيف `ans` أولاً، فيكمل السطر من الإجابة السابقة.

تقدم قائمة **الإعدادات** نفس خيارات عرض النتائج التي يقدمها تطبيق الويب (كسور مضبوطة، ترميز، فواصل آلاف)، إلى جانب صفي المظهر واللغة.

### 5.3 الرسوم البيانية

اكتب `graph` متبوعاً بتعبير، ثم اضغط **Enter**:

```epher
graph x ^ 2
```

يأخذ epher عينات من المنحنى من x = −10 إلى x = 10 ويرسمه كمخطط ASCII في لوحة Graph؛ وسيلة الإيضاح فوق المخطط تسمّي ما يُرسم.

`graph clear` يفرغ المخطط، وقائمة **Graph** تفعل الشيء نفسه؛ قائمة **Help** تفتح هذا الدليل داخل واجهة الطرفية (مفاتيح الأسهم للتمرير، **Esc** للإغلاق). قائمة **Settings** يمكنها
إخفاء نقاط الاهتمام المدرجة أسفل الرسم البياني.

يمكنك رسم أي تعبير، بما في ذلك دوالك الخاصة. عرّف واحدة أولاً، ثم ارسمها:

```epher
def f(x) = x ^ 3
graph f(x)
```

كل سطر `graph` يضيف منحنى إلى المخطط، يُرسم برمزه الخاص (`o` أو `x` أو `+` أو `*`)؛ `graph clear` يفرغ المخطط. تنطبق القواعد نفسها كما في تطبيق الويب: مجال (`graph sin(x) from 0 to 2*pi`)، ومنحنيات وسيطية (`graph param 2*cos(t), 3*sin(t)`)، ومنحنيات قطبية (`graph polar 1 + cos(theta)`)، ومناطق (`graph y < x ^ 2` يظلّل المنطقة تحت المنحنى).

النقاط التي لا يكون للتعبير فيها قيمة (القسمة على صفر مثلاً) تُتجاوز ببساطة، تاركة فجوة في المخطط. بعد كل أمر graph يعرض TUI نقاط الاهتمام (الجذور ونقاط التحوّل والتقاطعات) تحت المخطط. أمر `table` (القسم 2.4.2) يعمل هنا أيضاً.

يرسم `graph3d x ^ 2 - y ^ 2` سطحاً 3D كإطار سلكي ASCII. دوّره بمفاتيح الأسهم ما دام الإدخال فارغًا، واضغط شريط المسافة لتحريك ثابت منزلق (القسم 2.4.5). يعرض سطر التلميحات أسفل الشاشة تلميحي الأسهم والمسافة فقط عند عرض سطح 3D أو منحنى قابل للتحريك.

يكتب `graph save plot.svg` المخطط الحالي كنفس صورة SVG التي يعطيها زر
**Copy SVG** في تطبيق الويب؛ ويحفظ `graph3d save file.svg` الإطار
السلكي ثلاثي الأبعاد من الزاوية التي تنظر إليها.

### 5.4 الحفظ والاستمرارية

يتشارك TUI تخزينه مع CLI: كل ما يُحفظ في أحدهما متاح في الآخر. تعيش الدوال والنصوص البرمجية والسجل وتفضيل اللغة في `~/.epher` (الفصل 4.6)، وأوامر `save` و`save script` و`language` نفسها تعمل هنا.

## 6. بياناتك وخصوصيتك

- **برنامج epher المثبَّت** (تطبيق سطح المكتب وCLI وTUI) يخزن الدوال والنصوص البرمجية والسجل واختيار اللغة محلياً في `~/.epher` (أو `EPHER_STORE_DIR`). لا يغادر شيء حاسوبك.
- **تطبيق الويب** لا يخزن شيئاً على القرص: السجل يدوم فقط ما دامت الصفحة مفتوحة. يمكن لتطبيق الويب العمل دون اتصال لأن متصفحك هو من يخزن الصفحة نفسها.

تنفذ الإصدارات الخمسة الحساب بالكامل على جهازك. لا يُرسَل أي شيء إلى أي مكان.

