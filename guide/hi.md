# epher उपयोगकर्ता गाइड

स्वागत है! epher एक प्रोग्राम करने योग्य, स्क्रिप्ट करने योग्य कैलकुलेटर है। आप इसका उपयोग त्वरित गणना के लिए कर सकते हैं, या अपने स्वयं के फ़ंक्शन और छोटे प्रोग्राम बना सकते हैं। सब कुछ आठ भाषाओं में उपलब्ध है।

यह गाइड पूर्ण शुरुआती लोगों के लिए है। यह सबसे सरल संभव गणना से शुरू होता है और भाषा की पूरी शक्ति तक क्रमशः आगे बढ़ता है। हर उदाहरण दिखाता है कि आप क्या टाइप करते हैं और epher क्या उत्तर देता है।

epher का उपयोग करने के पाँच तरीके हैं। जो आपको सबसे उपयुक्त लगे उसे चुनें:

| संस्करण | यह क्या है | कब चुनें |
|---|---|---|
| **कमांड लाइन** (CLI) | टर्मिनल में टेक्स्ट कमांड | आप टर्मिनल में रहते हैं और स्क्रिप्ट पसंद करते हैं |
| **REPL** | `epher>` प्रॉम्प्ट पर एक इंटरैक्टिव `epher` सत्र | टर्मिनल छोड़े बिना त्वरित आदान-प्रदान चाहते हैं |
| **टर्मिनल इंटरफ़ेस** (TUI) | टर्मिनल के अंदर एक फुल-स्क्रीन प्रोग्राम | ग्राफ़ और स्क्रीन पर इतिहास वाला टर्मिनल ऐप चाहते हैं |
| **डेस्कटॉप ऐप** | अपनी विंडो वाला एक सामान्य डेस्कटॉप प्रोग्राम | एक सामान्य एप्लिकेशन चाहते हैं |
| **वेब ऐप** (PWA) | आपके ब्राउज़र में चलता है, इंस्टॉल हो सकता है, ऑफ़लाइन काम करता है | सबसे तेज़ शुरुआत चाहते हैं; कोई इंस्टॉलेशन नहीं |

डेस्कटॉप ऐप, कमांड लाइन, REPL और टर्मिनल इंटरफ़ेस एक ही प्रोग्राम हैं: एक डाउनलोड से `epher` कमांड इंस्टॉल होता है, जो चारों काम करता है। वेब ऐप अपवाद है: उसे कोई डाउनलोड नहीं चाहिए।

पाँचों संस्करण बिल्कुल एक ही भाषा समझते हैं। एक बार सीखें, कहीं भी उपयोग करें।

## 1. epher भाषा

यह अध्याय epher के सभी संस्करणों में साझा भाषा सिखाता है। वेब ऐप या डेस्कटॉप ऐप में, एक व्यंजक टाइप करें और **Enter** दबाएँ (या **=** बटन पर क्लिक करें)। CLI में, सत्र `epher repl` से शुरू करें और `epher>` प्रॉम्प्ट के बाद टाइप करें। TUI में (`epher tui`), बस टाइप करें और **Enter** दबाएँ। CLI में आप एक व्यंजक सीधे मूल्यांकित करने के लिए `epher "व्यंजक"` भी लिख सकते हैं।

### 1.1 आपकी पहली गणना

यह टाइप करें:

```epher
2 + 3 * 4
```

epher उत्तर देता है:

```text
14
```

गुणा जोड़ से पहले होती है, बिल्कुल गणित की तरह। इस नियम को *ऑपरेटर प्राथमिकता* कहते हैं।

### 1.2 संक्रियाओं का क्रम

पूर्ण प्राथमिकता क्रम, सबसे मज़बूत से सबसे कमज़ोर:

1. `!` क्रमगुणित और `%` प्रतिशत (दोनों प्रत्यय)
2. `^` घात
3. `*` और `/` गुणा और भाग
4. `+` और `-` जोड़ और घटाव

क्रम बदलने के लिए कोष्ठक का उपयोग करें:

```epher
(2 + 3) * 4
```

```text
20
```

`^` ऑपरेटर घात निकालता है और दाएँ से बाएँ काम करता है:

```epher
2 ^ 10
```

```text
1024
```

```epher
2 ^ 3 ^ 2
```

```text
512
```

(`2 ^ 3 ^ 2` का अर्थ है `2 ^ (3 ^ 2)`, यानी `2 ^ 9` = 512.)

घात भिन्नात्मक हो सकती है। `2 ^ 0.5` मतलब 2 का वर्गमूल:

```epher
2 ^ 0.5
```

```text
1.41421356237
```

घटाव और भाग बाएँ से दाएँ काम करते हैं:

```epher
10 - 3 - 2
```

```text
5
```

`%` चिह्न एक प्रत्यय ऑपरेटर है जिसका अर्थ है "100 से भाग": `5%` यानी 0.05। यह आस-पास के ऑपरेटरों को कभी नहीं देखता, इसलिए `200 + 10%` 200.1 है। 200 को 10% बढ़ाने के लिए गुणा लिखें:

```epher
200 * (1 + 10%)
```

```text
220
```


### 1.3 विशेष संख्याएँ pi, e, tau और phi

प्रसिद्ध स्थिरांक पहले से मौजूद हैं:

```epher
pi
```

```text
3.14159265359
```

```epher
2 * pi
```

```text
6.28318530718
```

```epher
e
```

```text
2.71828182846
```

दो और: `tau` एक पूरा चक्र है (2 pi) और `phi` स्वर्ण अनुपात है:

```epher
tau
```

```text
6.28318530718
```

```epher
phi
```

```text
1.61803398875
```

### 1.4 तुलना और तर्क

आप संख्याओं की तुलना कर सकते हैं। परिणाम `true` (सत्य) या `false` (असत्य) होता है:

| तुलना | अर्थ |
|---|---|
| `a > b` | a, b से बड़ा है |
| `a < b` | a, b से छोटा है |
| `a >= b` | a, b से बड़ा या बराबर है |
| `a <= b` | a, b से छोटा या बराबर है |
| `a == b` | a, b के बराबर है (दोहरा `=` ध्यान दें) |
| `a != b` | a, b के बराबर नहीं है |

```epher
3 > 2
```

```text
true
```

```epher
1 != 2
```

```text
true
```

तुलनाओं को `and`, `or` और `not` से जोड़ें:

```epher
3 > 2 and 2 < 3
```

```text
true
```

```epher
not 3 > 2
```

```text
false
```

### 1.5 चर (variables)

एक `=` से किसी मान को नाम दें:

```epher
x = 5
```

```text
5
```

epher मान आपको दोहराकर दिखाता है। अब से `x` का उपयोग कहीं भी किया जा सकता है:

```epher
x ^ 2
```

```text
25
```

आप किसी चर को कभी भी बदल सकते हैं। यह अपना मान तब तक रखता है जब तक आप बदलते नहीं:

```epher
x = x + 1
```

```text
6
```

> नामों में अक्षर और अंडरस्कोर हो सकते हैं, जैसे `radius` या `my_total`। इनमें रिक्त स्थान नहीं हो सकते और ये संख्या से शुरू नहीं हो सकते।

एक नाम आरक्षित है: `i`, काल्पनिक इकाई (खंड 1.18)।

विशेष चर `ans` में हमेशा पिछला उत्तर रहता है, जैसे जेबी कैलकुलेटर की
`Ans` कुंजी, जुड़ी हुई गणनाओं के लिए उपयोगी:

```epher
2 + 3
ans * 2
```

```text
5
10
```

एक सूची एक ही बार में कई नाम भर सकती है — `{a, b} = list` सूची को बाएँ
से दाएँ अलग कर देता है (खंड 1.11 ऐसे फ़ंक्शन दिखाता है जो इस तरह एक से
अधिक उत्तर लौटाते हैं):

```epher
{a, b} = {10, 20}; a + b
```

```text
{10, 20}
30
```

जो स्थान नहीं चाहिए उसे `_` लिखा जाता है:

```epher
{x, _} = {7, 8}; x
```

```text
{7, 8}
7
```

### 1.6 स्थिरांक: कभी न बदलने वाले नाम

*स्थिरांक* एक ऐसा नाम है जिसका मान कभी नहीं बदलता, जैसे बिल्ट-इन `pi`,
लेकिन आपकी पसंद का। इसे `const` से परिभाषित करें:

```epher
const tax = 0.2
```

```text
0.2
```

जहाँ कोई संख्या चल सकती है, वहाँ इसका उपयोग करें:

```epher
100 * (1 + tax)
```

```text
120
```

मान निश्चित है: इसे `=` से बदलना एक त्रुटि है,

```epher
tax = 0.25
```

```text
error: cannot assign to constant tax
```

और उसी स्थिरांक को अलग मान के साथ फिर से परिभाषित करना भी:

```epher
const tax = 0.25
```

```text
error: constant already defined: tax
```

स्थिरांक चर से एक और तरह से अलग हैं: `pi` की तरह, वे आपके अपने फ़ंक्शन
के भीतर काम करते हैं।

```epher
const g = 9.81
```

```text
9.81
```

```epher
def weight(m) = m * g
```

```epher
weight(80)
```

```text
784.8
```

किसी स्थिरांक को आने वाले सत्रों के लिए सहेजने के लिए `save tax` करें, ठीक
फ़ंक्शन की तरह (अध्याय 4.4)।

> एक चर और एक स्थिरांक एक ही नाम नहीं रख सकते: `const tax = 0.2` के बाद,
> `tax = ...` हमेशा त्रुटि है। नया नाम चुनें या नया सत्र शुरू करें।

### 1.7 स्ट्रिंग और print

स्ट्रिंग दोहरे उद्धरण चिह्नों में लिखा पाठ है: `"hello"`। स्ट्रिंग `+` से जुड़ती हैं, `==` और `!=` से तुलना होती है, `<` और `>` से क्रम तय होता है (शब्दकोश क्रम), `len` से गिनती होती है, और सूचियों की तरह 1-आधारित अनुक्रमण होता है:

```epher
"hello" + " " + "world"
len("hello")
"hello"[1]
"abc" == "abd"
"apple" < "banana"
```

```text
hello world
5
h
false
true
```

स्ट्रिंग के भीतर बैकस्लैश एक **एस्केप अनुक्रम** शुरू करता है: `\n` नई
पंक्ति है, `\t` टैब है, और `\\` तथा `\"` स्वयं बैकस्लैश और उद्धरण चिह्न
हैं — इस तरह एक स्ट्रिंग में दोहरा उद्धरण चिह्न आ सकता है:

```epher
s = "say \"hi\""
len("a\nb")
```

```text
say "hi"
3
```

**str(x)** एक मान को वैसे लिखता है जैसे उत्तर पैनल लिखता, और
**print(a, b, …)** अपने तर्कों को रिक्त स्थान से जोड़कर एक पंक्ति
बनाता है:

```epher
print("x =", 42)
```

```text
x = 42
```

एक छोटी लाइब्रेरी रिपोर्ट लिखने का बाकी काम सँभालती है। **upper** और
**lower** अक्षरों की किस्म बदलते हैं, **trim** सिरों के रिक्त स्थान
हटाता है, **substr** एक टुकड़ा लेता है (हर अनुक्रमणिका की तरह
1-आधारित), **find** बताता है कि कोई पाठ कहाँ आता है (न होने पर 0),
**replace** एक पाठ की हर प्रति दूसरे से बदल देता है, **split** एक
विभाजक पर पाठ को सूची में तोड़ता है, **join** सूची को एक पाठ में जोड़
देता है, और **fixed** संख्या को ठीक उतने दशमलव स्थानों के साथ लिखता है
जितने आप कहें — रिपोर्ट जो शून्य चाहता है, वह बना रहता है:

```epher
upper("hello")
substr("2026-09-17", 1, 4)
find("hello world", "world")
replace("2026-09-17", "-", "/")
split("a,b,c", ",")
join({1, 2, 3}, "-")
fixed(3.1, 2)
```

```text
HELLO
2026
7
2026/09/17
{a, b, c}
1-2-3
3.10
```

### 1.8 if से निर्णय

`if` दो मानों के बीच चुनाव करता है:

```epher
if 3 > 2 then 10 else 20
```

```text
10
```

रूप हमेशा होता है `if शर्त then सत्य_वाला_मान else असत्य_वाला_मान`। `else` भाग अनिवार्य है।

चर के साथ एक अधिक उपयोगी उदाहरण:

```epher
price = 100
if price > 50 then 2 else 1
```

```text
100
2
```

> एक `if` अब स्ट्रिंग की तुलना भी कर सकता है (खंड 1.7): दोनों शाखाओं को बस
> एक ही किस्म के मान होना चाहिए: संख्याओं के साथ संख्याएँ, स्ट्रिंग के साथ
> स्ट्रिंग।

### 1.9 while से लूप

जब तक शर्त सत्य है, `while` एक कथन को दोहराता है:

```epher
x = 0; while x < 5 do x = x + 1; x
```

```text
0
5
```

इस स्क्रिप्ट को ऐसे पढ़ें: *x को 0 से शुरू करें; जब तक x 5 से छोटा है, x में 1 जोड़ें; फिर x दिखाएँ।* परिणाम 5 है क्योंकि लूप पाँच बार चला।

> **सुरक्षा जाल:** epher किसी भी लूप को 100 000 चरणों के बाद रोक देता है और `error: step limit exceeded` दिखाता है। यह आपको ऐसे लूपों से बचाता है जो कभी समाप्त नहीं होंगे। यदि यह दिखे, तो शायद आपकी शर्त कभी असत्य नहीं हुई।

एक लूप को जानबूझकर भी छोड़ा जा सकता है: अगला भाग `break` और `continue`
जोड़ता है, और एक फ़ंक्शन `return` (खंड 1.11)।

### 1.10 for से लूप

`for` प्रति मान एक बार कथन दोहराता है और शरीर के मानों को सूची में जमा करता है: `start to end` रेंज (समावेशी) पर वैकल्पिक `step` के साथ, या किसी सूची के तत्वों पर:

```epher
for i in 1 to 5 do i^2
for x in {2, 3, 4} do 10*x
for i in 0 to 1 step 0.5 do i
```

```text
{1, 4, 9, 16, 25}
{20, 30, 40}
{0, 0.5, 1}
```

लूप चर का क्षेत्र केवल लूप तक है: इसके बाद नाम वैसा ही वैसा ही अर्थ रखता है जैसा पहले था (`i` पर लूप सामान्य इकाई को नहीं डीखता), जबकि शरीर के भीतर अन्य नामों को किए गए आबंटन बने रहते हैं। print के साथ, लूप पढ़ने योग्य पंक्तियाँ लिखता है:

```epher
for i in 1 to 3 do print("line", i)
```

```text
{line 1, line 2, line 3}
```

वही 100,000-चरण की सुरक्षा एक for लूप को भी बाँधती है जितनी while को।

**लूप को समय से पहले छोड़ना।** `break` लूप को वहीं रोक देता है; अब तक
जमा किए गए मान ही लूप का उत्तर हैं। `continue` चक्कर का बाकी हिस्सा
छोड़ कर अगले मान पर चला जाता है। दोनों कथन हैं, इसलिए वे एक `if` के
पीछे बैठते हैं:

```epher
for k in 1 to 6 do if mod(k, 2) == 1 then k
for k in 1 to 10 do if k == 4 then break else k
total = 0
for k in 1 to 6 do if mod(k, 2) == 0 then continue else total = total + k
total
```

```text
{1, 3, 5}
{1, 2, 3}
0
{1, 4, 9}
9
```

बीच वाले को ऐसे पढ़ें: k को 1 से 10 तक, अगर k 4 है तो रुक जाओ; वरना k
वापस कर दो। लूप का उत्तर वे मान हैं जो उसने रुकने से पहले देखे। अंतिम
जोड़ी `continue` की गिनती दिखाती है: for पंक्ति की जमा सूची चलती हुई
कुल रकम है - 1, फिर 4, फिर 9 - और सम चक्कर छोड़ दिए गए थे, इसलिए
उन्होंने कुछ नहीं जोड़ा। अंतिम `total`, 9, विषम मानों का योग है। बिना
`else` वाला `if` जब उसकी शर्त असत्य हो तो कुछ नहीं देता — पहला लूप
इसी तरह केवल विषम मान रखता है, और एक लूप इसी तरह किसी सूची को बदलने
के साथ-साथ उसे *छान* भी सकता है।


### 1.11 def से अपने स्वयं के फ़ंक्शन

फ़ंक्शन नाम और पैरामीटर वाली एक गणना है:

```epher
def f(x) = x ^ 2
```

फिर इसे उपयोग करें:

```epher
f(7)
```

```text
49
```

फ़ंक्शन कई पैरामीटर ले सकते हैं:

```epher
def area(w, h) = w * h
area(3, 4)
```

```text
12
```

आप बिना पैरामीटर के भी फ़ंक्शन बना सकते हैं:

```epher
def answer() = 42
answer()
```

```text
42
```

**कई चरणों वाला शरीर।** जब एक व्यंजक काफ़ी न हो, फ़ंक्शन को `do … end`
शरीर दें। कथन क्रम से एक के बाद एक चलते हैं, और अंतिम कथन का मान ही
उत्तर है:

```epher
def hyp(a, b) do
  c = a ^ 2 + b ^ 2
  sqrt(c)
end
hyp(3, 4)
```

```text
5
```

बीच का नाम `c` कॉल के भीतर रहता है; बाहर से दिखता नहीं, और दो कॉल
एक-दूसरे का `c` नहीं देखते।

**return: उत्तर अभी।** `return value` तुरंत उत्तर देता है और शरीर का
बाकी हिस्सा छोड़ देता है — जल्दी निकास वाले चुनाव का सहज रूप:

```epher
def grade(s) do
  if s >= 90 then return "A"
  if s >= 80 then return "B"
  "C"
end
grade(95); grade(85); grade(40)
```

```text
A
B
C
```

`return` तलाश करते लूप को भी उसी क्षण छोड़ देता है जिस क्षण उसे मिल
जाए:

```epher
def firstsq(xs) do
  for x in xs do
    if x ^ 0.5 == floor(x ^ 0.5) then return x
  0
end
firstsq({3, 5, 9, 11}); firstsq({3, 5, 7})
```

```text
9
0
```

एक नियम ब्लॉकों को पढ़ने योग्य रखता है: `end` हमेशा फ़ंक्शन के do को
बंद करता है — `if`, `for` या `while` को कोई `end` नहीं मिलता। बिना
उत्तर के खत्म होने वाला शरीर (मान लीजिए, हर रास्ता कुछ नहीं लौटाता)
एक त्रुटि है, चुप्पी नहीं: epher यह कहकर बताता है और फ़ंक्शन का नाम
लेता है।

> **एक से अधिक उत्तर?** सूची लौटाएँ — और खंड 1.5 के डिस्ट्रक्चरिंग से
> एक ही बारी में नाम दें:
> `{mean, sd} = {4, 1.6}`।

### 1.12 पुनरावृत्ति: अपने आप को बुलाने वाला फ़ंक्शन

सबसे प्रसिद्ध उदाहरण हैं फ़िबोनाची संख्याएँ:

```epher
def fib(n) = if n <= 1 then n else fib(n - 1) + fib(n - 2)
```

```epher
fib(10)
```

```text
55
```

`fib(10)` दसवीं फ़िबोनाची संख्या है। फ़ंक्शन अपने आप को छोटे तर्कों के साथ बुलाता है जब तक `n <= 1` नहीं हो जाता। यह इसलिए काम करता है क्योंकि `if ... then ... else ...` रूप केवल उसी शाखा की गणना करता है जिसकी ज़रूरत है।

> फ़ंक्शन का शरीर `=` के बाद एक व्यंजक होता है, या कई कथनों वाला एक
> `do … end` ब्लॉक जिसमें जल्दी निकास के लिए `return` हो (खंड 1.11)।
> जब फ़ंक्शन की ज़रूरत न हो, तो कई गणनाएँ जोड़ने के लिए स्क्रिप्ट बनाएँ
> (अगला भाग)।

### 1.13 स्क्रिप्ट: एक साथ कई कथन

*स्क्रिप्ट* कई कथन होते हैं जो `;` से जुड़े होते हैं या नई पंक्तियों से, जिनका अर्थ बिल्कुल वही होता है, और एक के बाद एक चलते हैं:

```epher
x = 10; y = x + 5; x + y
```

```text
10
15
25
```

स्क्रिप्ट छोटे प्रोग्राम बनाने का तरीका है: चर तैयार करें, लूप चलाएँ, और अंतिम परिणाम दिखाएँ।

नई पंक्तियाँ और `;` एक ही विभाजक हैं, और आप उन्हें स्वतंत्र रूप से मिला सकते हैं। बहु-पंक्ति वाले उदाहरण के ऊपर **कॉपी करें** बटन पूरी स्क्रिप्ट कॉपी करता है, और आप उसे सीधे epher में चिपका सकते हैं: वेब ऐप और डेस्कटॉप ऐप का एंट्री फ़ील्ड, टर्मिनल इंटरफ़ेस और `epher repl`, ये सभी हर पंक्ति को क्रम से चलाते हैं, ठीक वैसे ही जैसे आपने उन्हें एक-एक करके टाइप किया हो। कई कथनों को `;` से एक पंक्ति में जोड़ना भी हर जगह काम करता है, एक-बार कमांड लाइन सहित (धारा 4.1)।


स्क्रिप्ट में **टिप्पणियाँ** हो सकती हैं - आपके लिए लिखी टिप्पणियाँ जिन्हें epher छोड़ देता है, PHP की शैली में। `//` या `#` पंक्ति के अंत तक टिप्पणी करता है, और `/* ... */` एक ब्लॉक को टिप्पणित करता है। किसी कथन से जुड़ी टिप्पणी उसके अंत में, `//` के बाद आती है, कथन के भीतर कभी नहीं:

```epher
// a small script with notes
r = 3 # radius in metres
area = pi * r ^ 2 // pi r squared
area
```

A block comment may also span several lines. A script file or a pasted
program arrives as one whole program, so its block comments may span
lines:

```epher
/* the area of a circle
of radius 3 */
pi * 3 ^ 2
```

Input that arrives line by line - typing into the REPL or the terminal
UI, or a piped script - takes one line at a time, so there a block
comment opens and closes on the same line.
### 1.14 सटीक परिणाम: frac, dec और big

आम तौर पर epher पॉकेट कैलकुलेटर की तरह दशमलव संख्याओं से गणना करता है, और परिणाम पॉकेट कैलकुलेटर की तरह बारह सार्थक अंकों तक पूर्णांकित होते हैं: `0.1 + 0.2` = `0.3`, कभी `0.30000000000000004` नहीं। सटीक भिन्न डिफ़ॉल्ट रूप से चालू हैं — छोटे हर वाला अच्छा भिन्न जिसका दशमलव दोहराता है, वैसे ही दिखता है। `1 / 3` बिना पूछे `1/3` दिखता है:

```epher
1 / 3
```

```text
1/3
```

परिणाम सेटिंग्स (अध्याय 2.2) में **सटीक भिन्न बंद** करने पर वही भाग `0.333333333333` दिखाता है। **frac(n, d)** एक सटीक भिन्न बनाता है जो गणनाओं में सटीक बनी रहती है:

```epher
frac(1, 3)
```

```text
1/3
```

भिन्न गणनाओं में सटीक बनी रहती हैं:

```epher
frac(1, 3) * 3
```

```text
1
```

**dec(x)** एक सटीक दशमलव बनाता है। `0.1 + 0.2` दोनों स्थितियों में
`0.3` दिखाता है — अंतर अंकगणित में है:

```epher
0.1 * 3 - 0.3
dec(0.1) * 3 - dec(0.3)
```

```text
0.0000000000000000555111512313
0.0
```

फ़्लोट परिणाम में वह छोटी पूर्णांकन त्रुटि होती है जो हर कंप्यूटर
दशमलव संख्याओं से करता है; `dec()` गणना को सटीक रखता है।

**big(x)** एक सटीक पूर्णांक बनाता है, उन मानों के लिए जो पॉकेट कैलकुलेटर की सीमा से बड़े हैं:

```epher
big(10 ^ 20)
```

```text
100000000000000000000
```

**संख्या आधार** पूर्णांकों को वैसे ही लिखते हैं जैसे गणित समुदाय लिखता है:
`0b` द्विआधारी के लिए, `0o` अष्टाधारी के लिए, `0x` षोडशाधारी के लिए
(उपसर्ग केवल वर्तनी बदलता है, मान कभी नहीं):

```epher
0b1010 + 0xFF
```

```text
265
```

**bin(x)**, **oct(x)** और **hex(x)** से वापस बदलें। ये एक पूर्ण संख्या की
उपसर्ग-सहित वर्तनी देते हैं, जिसे सीधे दोबारा इस्तेमाल किया जा सकता है:

```epher
hex(255)
bin(10)
```

```text
0xff
```
0b1010
```

**exact(x)** दशमलव परिणाम के पीछे की यथार्थ भिन्न को पुनर्निर्मित करता है: छोटे हर वाली अच्छी भिन्न वाला हर मान ऐसे दिखता है। यह वही पुनर्निर्माण है जो ऐप डिफ़ॉल्ट रूप से उपयोग करते हैं, इसलिए `1 / 3` आमतौर पर सीधे `1/3` दिखता है:

```epher
exact(0.3333333333333333)
exact(0.30000000000000004)
```

```text
1/3
3/10
```

`pi` जैसा अपरिमेय मान कोई अच्छी भिन्न नहीं रखता, इसलिए `exact()` उसे वैसे ही छोड़ देता है।

प्रदर्शन क्रियाएँ किसी संख्या को दूसरे संकेतन में लिखती हैं। **scientific(x)** घातांक से पहले एक अंक रखता है, **engineering(x)** घातांक तीन-तीन के चरणों में (मेंटिसा 1 और 1000 के बीच रहता है), और **grouped(x)** हजार विभाजक के रूप में पतले स्थान जोड़ता है:

```epher
scientific(12345)
engineering(12345)
engineering(0.5)
grouped(1234567.89)
```

```text
1.2345e4
12.345e3
500e-3
1 234 567.89
```

वेब ऐप और TUI भी इन्हें प्रदर्शन सेटिंग के रूप में देते हैं (अध्याय 2.2 और 5.2 देखें): यथार्थ भिन्न चालू/बंद, ऑटो/वैज्ञानिक/इंजीनियरिंग संकेतन और हजार विभाजक। सेटिंग केवल परिणाम दिखाने का तरीका बदलती हैं; मान नीचे सामान्य दशमलव संख्याएँ ही रहते हैं।

### 1.15 अंतर्निर्मित फ़ंक्शन

epher में वैज्ञानिक कैलकुलेटर के फ़ंक्शन हैं, परिवार के अनुसार समूहबद्ध।

त्रिकोणमिति रेडियन में काम करती है। बदलने के लिए `deg` और `rad` का उपयोग करें:

| फ़ंक्शन | अर्थ | उदाहरण | परिणाम |
|---|---|---|---|
| `sin(x)`, `cos(x)`, `tan(x)` | त्रिकोणमितीय फ़ंक्शन | `sin(pi / 2)` | `1` |
| `asin(x)`, `acos(x)`, `atan(x)` | प्रतिलोम त्रिकोणमिति | `atan(1)` | `0.7853981633974483` |
| `atan2(y, x)` | बिंदु (x, y) का कोण | `atan2(1, 1)` | `0.7853981633974483` |
| `deg(x)` | रेडियन → डिग्री | `deg(pi)` | `180` |
| `rad(x)` | डिग्री → रेडियन | `rad(180)` | `3.14159265359` |
| `sinh(x)`, `cosh(x)`, `tanh(x)` | अतिपरवलयिक फ़ंक्शन | `sinh(1)` | `1.1752011936438014` |
| `asinh(x)`, `acosh(x)`, `atanh(x)` | प्रतिलोम अतिपरवलयिक | `acosh(1)` | `0` |

घात, मूल और लघुगणक (कैलकुलेटर पर `log` आधार 10 होता है):

| फ़ंक्शन | अर्थ | उदाहरण | परिणाम |
|---|---|---|---|
| `sqrt(x)` | वर्गमूल | `sqrt(16)` | `4` |
| `cbrt(x)` | घनमूल | `cbrt(-27)` | `-3` |
| `root(n, x)` | nवाँ मूल | `root(3, 8)` | `2` |
| `exp(x)` | e की x घात | `exp(1)` | `2.71828182846` |
| `ln(x)` | प्राकृतिक लघुगणक | `ln(e)` | `1` |
| `log(x)` | आधार-10 लघुगणक | `log(100)` | `2` |
| `log2(x)` | आधार-2 लघुगणक | `log2(8)` | `3` |
| `logb(b, x)` | आधार b में लघुगणक | `logb(2, 8)` | `3` |
| `hypot(a, b)` | कर्ण | `hypot(3, 4)` | `5` |
| `5!` (या `fact(n)`) | क्रमगुणित | `5!` | `120` |

पूर्णांकन, चिह्न और पूर्णांक:

| फ़ंक्शन | अर्थ | उदाहरण | परिणाम |
|---|---|---|---|
| `abs(x)` | निरपेक्ष मान | `abs(-3)` | `3` |
| `floor(x)` / `ceil(x)` | नीचे / ऊपर पूर्णांकित | `floor(2.7)` | `2` |
| `round(x)` | निकटतम, आधा शून्य से दूर | `round(2.5)` | `3` |
| `trunc(x)` | दशमलव भाग हटाएँ | `trunc(-2.9)` | `-2` |
| `sign(x)` | -1, 0 या 1 | `sign(-5)` | `-1` |
| `ncr(n, r)` | संचय | `ncr(52, 5)` | `2598960` |
| `npr(n, r)` | क्रमचय | `npr(5, 2)` | `20` |
| `gcd(a, b)` / `lcm(a, b)` | सार्व भाजक और सार्व गुणज | `gcd(12, 18)` | `6` |
| `mod(a, b)` | शेषफल | `mod(7, 3)` | `1` |

अभाज्य संख्याएँ और भाजक पूर्ण संख्याओं पर काम करते हैं:

| फ़ंक्शन | अर्थ | उदाहरण | परिणाम |
|---|---|---|---|
| `isprime(n)` | सही जब n अभाज्य हो | `isprime(97)` | `true` |
| `nextprime(n)` / `prevprime(n)` | निकटतम अभाज्य संख्याएँ | `nextprime(10)` | `11` |
| `factors(n)` | अभाज्य गुणनखंडन | `factors(360)` | `2^3 * 3^2 * 5` |
| `totient(n)` | यूलर टोशेंट | `totient(12)` | `4` |
| `ndivisors(n)` | भाजकों की संख्या | `ndivisors(360)` | `24` |
| `modpow(b, e, m)` | b की घात e, mod m, यथार्थ | `modpow(2, 10, 1000)` | `24` |


सांख्यिकी कितने भी तर्क लेती है:

| फ़ंक्शन | अर्थ | उदाहरण | परिणाम |
|---|---|---|---|
| `sum(...)` / `product(...)` | कुल | `sum(1, 2, 3)` | `6` |
| `mean(...)` | औसत | `mean(1, 2, 3)` | `2` |
| `median(...)` | मध्य मान | `median(1, 2, 3, 4)` | `2.5` |
| `min(...)` / `max(...)` | सबसे छोटा / सबसे बड़ा | `max(4, 1, 3)` | `4` |
| `variance(...)` / `stdev(...)` | मानों का प्रसार | `stdev(2, 4)` | `1` |

भाग 1.12 की सटीक परतें यथावत् हैं:

| फ़ंक्शन | अर्थ | उदाहरण | परिणाम |
|---|---|---|---|
| `frac(n, d)` | सटीक भिन्न | `frac(1, 3)` | `1/3` |
| `dec(x)` | सटीक दशमलव | `dec(0.1)` | `0.1` |
| `big(x)` | सटीक पूर्णांक | `big(10 ^ 20)` | `100000000000000000000` |
| द्विआधारी, अष्टाधारी, षोडशाधारी | `0b…`, `0o…`, `0x…` | `0xFF + 0b1` |
| आधार वर्तनी | `bin(x)`, `oct(x)`, `hex(x)` | `hex(255)` |
| अभाज्य | `isprime(n)`, `factors(n)`, … | `factors(360)` |
| `bin(x)` / `oct(x)` / `hex(x)` | आधार 2 / 8 / 16 में उपसर्ग-सहित वर्तनी | `hex(255)` | `0xff` |

ये बाकी सब चीज़ों की तरह संयोजित होते हैं:

```epher
min(sqrt(16), 5)
```

```text
4
```

भौतिक नियतांक SI इकाइयों में हैं, जैसे 1.16 अनुभाग के खगोलीय नियतांक:

| नाम | अर्थ | मान |
|---|---|---|
| `G` | न्यूटन का गुरुत्वाकर्षण नियतांक | 6.6743e-11 |
| `gamma` | यूलर-माशेरोनी नियतांक | 0.577215664902 |
| `q_e` | मूल आवेश | 1.602176634e-19 |
| `ev` | इलेक्ट्रॉनवोल्ट, जूल में | 1.602176634e-19 |
| `eps_0` | निर्वात की विद्युतशीलता | 8.8541878128e-12 |
| `mu_0` | निर्वात की चुंबकशीलता | 1.25663706212e-6 |
| `z_0` | निर्वात की प्रतिबाधा | 376.730313668 |
| `m_e` | इलेक्ट्रॉन का द्रव्यमान | 9.1093837139e-31 |
| `m_p` | प्रोटॉन का द्रव्यमान | 1.67262192595e-27 |
| `m_n` | न्यूट्रॉन का द्रव्यमान | 1.67492750056e-27 |
| `m_u` | परमाणु द्रव्यमान इकाई | 1.66053906892e-27 |
| `a_0` | बोर त्रिज्या | 5.29177210544e-11 |
| `alpha` | सूक्ष्म संरचना नियतांक | 0.0072973525643 |
| `r_inf` | रिडबर्ग नियतांक | 10973731.568160 |
| `mu_b` | बोर मैग्नेटॉन | 9.2740100783e-24 |
| `n_a` | एवोगाद्रो नियतांक | 6.02214076e23 |
| `faraday` | फैराडे नियतांक, C/mol | 96485.33212 |
| `r_gas` | मोलर गैस नियतांक | 8.31446261815 |
| `atm` | मानक वायुमंडल, पास्कल में | 101325 |
| `wien` | वीन तरंगदैर्घ्य नियतांक | 0.002897771955 |
| `phi_0` | चुंबकीय फ्लक्स क्वांटम | 2.067833848e-15 |
| `m_P` | Planck-Masse | 2.176434e-8 |
| `l_P` | Planck-Länge | 1.616255e-35 |
| `t_P` | Planck-Zeit | 5.391247e-44 |
| `r_e` | klassischer Elektronenradius | 2.8179403205e-15 |
| `lambda_c` | Compton-Wellenlänge | 2.42631023867e-12 |
| `mu_n` | Kernmagneton | 5.050783699e-27 |


### 1.16 त्रुटियाँ पढ़ना

जब कुछ गलत होता है, तो epher अनुमान लगाने के बजाय बताता है:

```epher
1 / 0
```

```text
error: division by zero
```

```epher
sqrt(-4)
```

```text
2i
```

```epher
unknown_name
```

```text
error: unknown name: unknown_name
```

```epher
foo(1)
```

```text
error: unknown name: foo
```

आखिरी उदाहरण महत्वपूर्ण है: epher बताता है कि वह कौन सा नाम नहीं जानता, ताकि आप अपना व्यंजक ठीक कर सकें।

### 1.17 त्वरित संदर्भ

| क्या | सिंटैक्स | उदाहरण |
|---|---|---|
| जोड़, घटाव, गुणा, भाग | `+ - * /` | `7 / 2` |
| घात | `^` (दाएँ से बाएँ) | `2 ^ 10` |
| क्रमगुणित | `!` (प्रत्यय) | `5!` |
| प्रतिशत | `%` (प्रत्यय) | `200 * (1 + 10%)` |
| कोष्ठक | `( )` | `(2 + 3) * 4` |
| स्थिरांक | `pi`, `e`, `tau`, `phi` | `2 * pi` |
| वैज्ञानिक संकेतन | `2.5e-3` | `6.02e23` |
| तुलना | `> < >= <= == !=` | `3 >= 2` |
| तर्क | `and or not` | `a > 1 and a < 10` |
| चर | `name = value` | `x = 5` |
| स्थिरांक | `const name = value` | `const tax = 0.2` |
| निर्णय | `if c then a else b` | `if x > 0 then 1 else -1` |
| कथन चुनें | `if c then कथन [else कथन]` | `if k == 4 then break` |
| लूप | `while c do statement` | `while x < 5 do x = x + 1` |
| for लूप | `for i in a to b step s do कथन` | `for i in 1 to 5 do i^2` |
| लूप छोड़ें / चक्कर छोड़ें | `break`, `continue` | `if k == 4 then break` |
| फ़ंक्शन | `def name(params) = expr` या `do … end` | `def f(x) = x ^ 2` |
| उत्तर अभी | `return value` | `if ok then return x` |
| कई नाम | `{a, b} = list` (`_` छोड़ता है) | `{m, sd} = stats(d)` |
| स्ट्रिंग | `"..."`, `+` जोड़ता है, `s[i]`, `==`, `<` | `"a" + "b"` |
| स्ट्रिंग लाइब्रेरी | `upper` `lower` `trim` `substr` `split` `join` `find` `replace` `fixed` | `split("a,b", ",")` |
| Print | `print(a, b, ...)` | `print("x =", 42)` |
| स्क्रिप्ट | `;` या नई पंक्तियों से जुड़े कथन | `x = 1; x + 1` |
| सटीक भिन्न | `frac(n, d)` | `frac(1, 3)` |
| सटीक दशमलव | `dec(x)` | `dec(0.1) + dec(0.2)` |
| सटीक पूर्णांक | `big(x)` | `big(10 ^ 20)` |
| भिन्न पुनर्निर्मित करें | `exact(x)` | `exact(0.3333333333333333)` |
| वैज्ञानिक, इंजीनियरिंग, समूहित | `scientific(x)` `engineering(x)` `grouped(x)` | `engineering(12345)` |
| काल्पनिक इकाई | `i`, या एक लिटरल `4i` | `sqrt(-1)` |
| सम्मिश्र के अंग | `re(z)` `im(z)` `arg(z)` `conj(z)` `abs(z)` | `re(3 + 4i)` |
| समीकरण हल करें | `solve lhs == rhs` | `solve x^2 == 9` |
| संख्यात्मक अवकलज | `derivative(expr, x)` | `derivative(x^2, 3)` |
| निश्चित समाकल | `integral(expr, a, b)` | `integral(x^2, 0, 3)` |
| द्विआधारी, अष्टाधारी, षोडशाधारी | `0b…`, `0o…`, `0x…` | `0xFF + 0b1` |
| आधार वर्तनी | `bin(x)`, `oct(x)`, `hex(x)` | `hex(255)` |
| अभाज्य | `isprime(n)`, `factors(n)`, … | `factors(360)` |
| सूची शाब्दिक | `{…}` | `{1, 2, 3}` |
| सूची तत्व | `list[i]` (1-आधारित) | `{5, 6}[2]` |
| सूची सांख्यिकी | `mean(सूची)`, `median(सूची)`, … | `stdev(d)` |
| सूची आकार | `len(s)`, `sort(s)`, `mode(s)`, `range(s)`, `quartile(s, k)` | `quartile(d, 1)` |
| रेखीय प्रतिगमन | `linreg(xs, ys)` | `linreg(x, y)` |
| प्रतिगमन परिवार | `quadreg` `expreg` `powreg` `logreg` | `quadreg(xs, ys)` |
| सामान्य परिवार | `normpdf` `normcdf` `invnorm` | `invnorm(0.975)` |
| t परिवार | `tpdf` `tcdf` `invt` | `invt(0.975, 10)` |
| काई-वर्ग परिवार | `chi2pdf` `chi2cdf` `invchi2` | `chi2cdf(3.84, 1)` |
| असतत परिवार | `binompdf` `binomcdf` `poissonpdf` `poissoncdf` | `binomcdf(2, 10, 0.5)` |
| परीक्षण और अंतराल | `ztest` `ttest` `zinterval` `tinterval` `chisq_gof` | `tinterval(d, 0.95)` |
| ANOVA और युग्मित t | `anova(सूचियाँ...)`, `ttestpaired(a, b)` | `anova(g1, g2, g3)` |
| डेटा प्लॉट | `graph scatter(xs, ys)` `histogram(data)` `boxplot(data)` | `graph boxplot(d)` |
| यादृच्छिक संख्याएँ | `random()`, `random(a, b)`, `randint(a, b)`, `randseed(n)` | `randint(1, 6)` |
| प्रसामान्य खींच | `randn(mu, sigma)` | `randn(0, 1)` |
| स्थिरांक ब्राउज़र | सहायता → स्थिरांक: हर अंतर्निर्मित स्थिरांक, समूहों में | सहायता → स्थिरांक |
| राशि | `5 m`, `60 mile/hr`, `1 km` | `2 m^2` |
| बदलें | `expr in इकाई` या `expr -> इकाई` | `72 km/hr in m/s` |
| उपसर्ग | `k M G T m µ n p` किसी इकाई को स्केल करते हैं | `5 km`, `3 MPa`, `1 GHz` |
| बिटवाइज़ और, या | `a & b`, `a \| b` | `0xFF & 0x0F` |
| बिटवाइज़ अनन्य या | `a xor b` | `5 xor 3` |
| बिटवाइज़ नहीं | `~a` | `~0` |
| शिफ़्ट | `a << n`, `a >> n` | `1 << 8` |
| शब्द आकार | `bits(n)` — 8, 16, 32, 64 के लिए | `bits(8)` |
| अंतर्निहित संबंध | `graph lhs == rhs` | `graph x^2 + y^2 == 1` |
| मैट्रिक्स शाब्दिक | `[[1, 2], [3, 4]]` | `[[1, 2], [3, 4]] * [[5, 6], [7, 8]]` |
| मैट्रिक्स फ़ंक्शन | `det` `inv` `transpose` `trace` `dim` `ref` `rref` | `rref([[2, 1, 5], [1, -1, 1]])` |
| TVM सॉल्वर | `tvm_n` `tvm_i` `tvm_pv` `tvm_pmt` `tvm_fv` | `tvm_pmt(360, 0.08/12, -100000, 0)` |
| NPV और IRR | `npv(rate, flows)` `irr(flows)` | `irr({-100, 60, 60})` |
| परिशोधन | `amort(p, r, n, k)` | `amort(1000, 0.01, 12, 6)` |
| ब्याज | `simple_interest` `compound_interest` | `compound_interest(1000, 0.05, 2)` |

### 1.18 सम्मिश्र संख्याएँ

epher सम्मिश्र संख्याओं से स्वचालित रूप से गणना करता है। काल्पनिक इकाई **i** है, बिल्कुल `pi` की तरह:

```epher
i ^ 2
sqrt(-1)
```

```text
-1
i
```

बाकी सब अंतर्निहित नामों के विपरीत, `i` दूसरे काम में नहीं लिया जा सकता: `i = 5` अस्वीकृत है, इसलिए काल्पनिक इकाई कभी छाया नहीं बन सकती - न असाइनमेंट से, न पुराने सहेजे सत्र से।

सम्मिश्र संख्या `i` प्रत्यय के साथ लिखें, गुणा चिह्न की आवश्यकता नहीं: `3 + 4i` एक लिटरल है, `2.5i` काम करता है, और आधार लिटरल भी (`0xFFi`)। सामान्य अंकगणित विस्तारित होता है: जोड़, घटाव, गुणा, भाग और घात सब काम करते हैं, और `i` सामान्य पूर्वता का पालन करता है (`i ^ 2` किसी भी घात की तरह जुड़ता है)।

वास्तविक फ़ंक्शन भी विस्तारित होते हैं। सम्मिश्र तर्क मिलने पर वे सम्मिश्र तल में गणना करते हैं; वास्तविक डोमेन से बाहर के वास्तविक तर्क पर वे त्रुटि के बजाय मुख्य सम्मिश्र परिणाम देते हैं:

```epher
ln(-1)
asin(2)
exp(i * pi)
```

```text
3.14159265359i
1.57079632679-1.31695789692i
-1+0.000000000000000122464679915i
```

(`exp(i * pi)` बिल्कुल `-1` है; अंतिम अंक कंप्यूटर की अंकगणित में `sin(pi)` का शोर हैं।)

चार फ़ंक्शन सम्मिश्र संख्या के अंग पढ़ते हैं, और `abs()` उसका परिमाण है:

```epher
re(3 + 4i)
im(3 + 4i)
arg(-1)
conj(3 - 4i)
abs(3 + 4i)
```

```text
3
4
3.14159265359
3+4i
5
```

केवल पूर्णांक फ़ंक्शन (`fact`, `gcd`, `floor`, `isprime`, ...) सम्मिश्र तर्कों को प्रकार त्रुटि के साथ अस्वीकार करते हैं।

### 1.19 समीकरण हल करना

**solve** एक चर वाले समीकरण के मूल खोजता है। समीकरण `==` उपयोग करता है:

```epher
solve x^2 == 5*x + 6
```

```text
x = -1, x = 6
```

बहुपदीय समीकरण (`+ - * ^` और स्थिरांकों से बने) हर मूल देते हैं, वास्तविक और सम्मिश्र दोनों:

```epher
solve x^2 == -1
solve x^2 + 2*x + 5 == 0
solve (x - 1)^2 == 0
```

```text
x = -i, x = i
x = -1-2i, x = -1+2i
x = 1
```

हल किया जाने वाला चर `x` है जब वह आता है, वरना एकमात्र दूसरा चर। स्थिरांक और बंधे चर पैरामीटर की तरह काम करते हैं:

```epher
const k = 3
solve k*x == 12
```

```text
3
x = 4
```

कोई भी अन्य समीकरण -100..100 पर संख्यात्मक रूप से स्कैन किया जाता है: मूल चिह्न परिवर्तनों से घेरे जाते हैं, इसलिए `solve sin(x) == 0.5` उस सीमा का हर मूल सूचीबद्ध करता है। दो ईमानदार सीमाएँ: जहाँ फ़ंक्शन केवल शून्य को छूता है वहाँ का मूल (जैसे संख्यात्मक मार्ग से `x^2 == 0`) छूट सकता है, और कई अबंधित चरों वाला समीकरण एक त्रुटि है।

### 1.20 कैलकुलस: अवकलज और समाकल

**derivative(expr, p)** `p` पर `expr` का संख्यात्मक अवकलज है। पहला तर्क एक व्यंजक ही रहता है, और उसका मुक्त चर वही है जिसका अवकलन होता है:

```epher
derivative(x^2, 3)
derivative(sin(t), 0)
```

```text
6
1
```

क्योंकि तर्क व्यंजक ही रहता है, अवकलज ग्राफ़ किया जा सकता है: `graph derivative(x^3 - x, x)` ढलान वक्र खींचता है।

**integral(expr, a, b)** `a` से `b` तक का निश्चित समाकल है, जो अनुकूली सिम्पसन चतुर्थांश से परिकलित होता है:

```epher
integral(x^2, 0, 3)
integral(sin(x), 0, pi)
```

```text
9
2
```

`integral(x^2, 3, 0)` `-9` है (चिह्न सहित समाकल), और ग्राफ़ योग्य ऊपरी सीमा काम करती है: `graph integral(x^2, 0, x)`।

दोनों संख्यात्मक हैं; व्यंजक सीमा में वास्तविक मान देने चाहिए, और कई चरों वाला व्यंजक एक त्रुटि है।

### 1.21 डेटा: सूचियाँ, सांख्यिकी और प्रतिगमन

सूची घुंघराले कोष्ठकों में संख्याओं का एक स्तंभ है: `{1, 2, 3}`।
तत्व व्यंजक हैं, खाली सूची `{}` मान्य है, और सूची किसी भी मान की
तरह एक नाम से बंधती है:

```epher
d = {12, 15, 14, 16, 13, 15, 14, 17}
d[2]
len(d)
```

`list[i]` i-वाँ तत्व है, कैलकुलेटर की तरह 1-आधारित; सूची से बाहर का
सूचकांक एक त्रुटि है। कोष्ठक `^` से अधिक कसकर बंधता है, इसलिए
`d[2]^2` का अर्थ `(d[2])^2` है।

सूची पर अंकगणित तत्व-वार होता है, और एक अकेली संख्या हर तत्व पर
लागू होती है:

```epher
{1, 2, 3} * 2
{1, 2, 3} + 10
```

`+ - * / ^` के लिए दो सूचियों की लंबाई समान होनी चाहिए। `==` और
`!=` पूरी सूचियों की तुलना करते हैं; क्रम तुलना सूचियों को अस्वीकार
करती है।

सांख्यिकी फलन एक सूची को एकमात्र तर्क के रूप में लेते हैं (बहु-तर्क
रूप बना रहता है — `mean(1, 2, 3)` अब भी काम करता है): `sum product
mean median mode variance stdev min max range`। नए आकार फलन हैं
`len(सूची)`, `sort(सूची)` (आरोही प्रति), `mode(सूची)` (सबसे बार-बार
आने वाला मान, बराबरी पर सबसे छोटा), `range(सूची)` (सबसे बड़ा घटा
सबसे छोटा) और `quartile(सूची, k)` जहाँ k = 1..3 (TI-शैली चतुर्थक,
आधे-आधे का माध्यिका):

```epher
mean(d)
median(d)
quartile(d, 1)
```

**linreg(xs, ys)** दो समान-लंबाई सूचियों में न्यूनतम वर्ग रेखा फिट
करता है और सहसंबंध गुणांक r के साथ बताता है:

```epher
linreg({1, 2, 3, 4}, {2.1, 4.2, 5.8, 8.1})
```

फिट की गई रेखा एक प्रदर्शन है, जैसे solve के मूल; फिट की तस्वीर
स्कैटर प्लॉट में दिखती है (अनुभाग 1.22)।

प्रतिगमन परिवार का बाकी हिस्सा वे मॉडल फ़िट करता है जिन तक कैलकुलेटर बढ़ते हैं: **quadreg** `y = a*x^2 + b*x + c` फ़िट करता है (कम से कम 3 बिंदु), **expreg** `y = a*e^(b*x)` फ़िट करता है (y > 0), **powreg** `y = a*x^b` फ़िट करता है (x और y > 0), और **logreg** `y = a + b*ln(x)` फ़िट करता है (x > 0)। हर एक अपने r के साथ मॉडल बताता है:

```epher
quadreg({1, 2, 3, 4}, {1, 4.1, 8.9, 16.2})
expreg({1, 2, 3}, {2.7, 7.4, 20.1})
```

```text
y = 1.05*x^2 + -0.21*x + 0.2 (r = 0.9999)
y = 0.9911*e^(1.0037*x) (r = 1)
```

एक रूपांतरित फ़िट का r रैखिकीकृत जोड़ी का सहसंबंध है: वही संख्या जो TI और NumWorks बताते हैं। हर मॉडल स्कैटर पर स्वयं भी बन सकता है: `graph scatter(xs, ys, quadreg)` (या expreg, powreg, logreg) बिंदुओं के साथ उस मॉडल की वक्र रेखा खींचता है।

### 1.22 वितरण और परिकल्पना परीक्षण

प्रायिकता फलन मानक सामान्य, स्टूडेंट t, काई-वर्ग, द्विपद और पॉइसन
परिवारों को कवर करते हैं। सामान्य परिवार एक या तीन तर्क लेता है —
एक तर्क मानक सामान्य है:

```epher
normcdf(1.96)
invnorm(0.975)
normcdf(12, 10, 2)
```

`normpdf(x[, mu, sigma])`, `normcdf(x[, mu, sigma])`, `invnorm(p[,
mu, sigma])`; `tpdf(x, df)`, `tcdf(x, df)`, `invt(p, df)`;
`chi2pdf(x, df)`, `chi2cdf(x, df)`, `invchi2(p, df)`;
`binompdf(k, n, p)`, `binomcdf(k, n, p)`; `poissonpdf(k, lambda)`,
`poissoncdf(k, lambda)`। `inv*` फलन उल्टा प्रश्न पूछते हैं:
`invt(0.975, 10)` वह t मान है जिसके नीचे 97.5% द्रव्यमान है।

परीक्षण एक डेटा सूची लेते हैं और आँकड़ा और दो-पक्षीय p-मान एक
प्रदर्शन पाठ के रूप में बताते हैं; अंतराल आपके नामित स्तर पर
`(निचला, ऊपरी)` बताते हैं:

```epher
d = {12, 15, 14, 16, 13, 15, 14, 17}
ttest(d, 14)
tinterval(d, 0.95)
ztest(d, 14, 1.5)
chisq_gof({20, 30, 25, 25}, {25, 25, 25, 25})
```

```text
{12, 15, 14, 16, 13, 15, 14, 17}
t = 0.8819, p = 0.4071
(13.1594, 15.8406)
z = 0.9428, p = 0.3458
chi2 = 2, p = 0.5724
```

`ttest(डेटा, mu0)` और `tinterval(डेटा, स्तर)` नमूना मानक विचलन
(n−1) उपयोग करते हैं; `ztest(डेटा, mu0, sigma)` और `zinterval(डेटा,
sigma, स्तर)` को ज्ञात sigma चाहिए। `chisq_gof(प्रेक्षित, अपेक्षित)`
k−1 स्वतंत्रता-कोटि वाला अनुकूलता परीक्षण है। परिणाम प्रदर्शन पाठ
हैं: पठनीय और कॉपी-योग्य, पर अंकगणित उन्हें छू नहीं सकता।

दो और परीक्षण उसी रूप में हैं। **anova(सूची1, सूची2, …)** दो या अधिक समूहों पर एक-मार्ग विचलन विश्लेषण है (असमान लंबाईयाँ चलती हैं) और अपने p-मान के साथ F बताता है:

```epher
anova({1, 2, 3}, {4, 5, 6}, {7, 8, 9})
```

```text
F = 27, p = 0.001
```

**ttestpaired(a, b)** युग्मित t-परीक्षण है: यह दो समान-लंबाई सूचियों के अंतर लेकर उन्हें 0 के विरुद्ध परखता है, कक्षा का "पहले और बाद" परीक्षण:

```epher
ttestpaired({80, 85, 90}, {82, 84, 91})
```

```text
t = -0.7559, p = 0.5286
```

### 1.23 डेटा प्लॉट

ग्राफ़ परिवार सूचियाँ भी लेता है: स्कैटर, हिस्टोग्राम और बॉक्स
प्लॉट। एक डेटा प्लॉट पैन पर अकेला होता है जैसे सौर मंडल — सबसे
नया आदेश जीतता है, और `graph clear` उसे खाली करता है।

```epher
x = {1, 2, 3, 4, 5}
y = {2.1, 4.2, 5.8, 8.1, 9.9}
graph scatter(x, y)
```

```epher
graph histogram({1, 2, 2, 3, 3, 3, 4, 5})
```

```epher
graph boxplot({1, 2, 2, 3, 3, 3, 9})
```

**scatter(xs, ys)** बिंदु बनाता है और, दो या अधिक बिंदुओं पर,
न्यूनतम वर्ग रेखा, लेजेंड में `y = a*x + b (r = …)` शीर्षक के
साथ। **histogram(डेटा[, bins])** आवृत्ति हिस्टोग्राम बनाता है;
bin गिनती वैकल्पिक है (डिफ़ॉल्ट स्टर्जेस नियम) और 1 से 50 के बीच
पूर्ण संख्या होनी चाहिए। **boxplot(डेटा)** बॉक्स-एंड-व्हिस्कर
बनाता है: न्यूनतम, Q1, माध्यिका, Q3, अधिकतम, छोर तक व्हिस्कर।
खिड़की डेटा के अनुसार खुलती है — `from a to b` डोमेन कीवर्ड अब भी लागू नहीं होते — और बनने के बाद चित्र वक्र प्लॉट की तरह ही ज़ूम होता है: माउस व्हील, पिंच और ज़ूम स्लाइडर सब काम करते हैं, और निर्यात वही सहेजता है जो पैन दिखाता है।

एक वैकल्पिक तीसरा शब्द मॉडल चुनता है: `graph scatter(xs, ys, quadreg)` (या expreg, powreg, logreg) रेखा के बजाय वह फ़िट खींचता है।
### 1.24 यादृच्छिक संख्याएँ

`random()` एक समान यादृच्छिक संख्या निकालता है `[0, 1)` में, `random(a, b)` एक
`[a, b)` में, और `randint(a, b)` बंद परास `[a, b]` से एक पूर्णांक — पासा फेंकना:

```epher
randseed(7)
randint(1, 6)
```

```text
7
3
```

क्रम प्रतिलिपि योग्य है: `randseed(n)` जनरेटर को `n` से फिर से सीड करता है और
उसे दिखाता है, इसलिए वही सीड हर सत्र और हर फ्रंटएंड में वही निकाल दोहराती है।

**randn(mu, sigma)** माध्य mu और मानक विचलन sigma वाले प्रसामान्य वितरण से खींचता है (Desmos इसे randomNormal कहता है, TI इसे randNorm):

```epher
randseed(7)
randn(0, 1)
```

```text
7
1.36499229746
```

वही बीज वही खींच दोहराता है, बिल्कुल एकसमान वालों की तरह।

### 1.25 इकाइयाँ और रूपांतरण

संख्या के बाद की इकाई एक *राशि* बनाती है: SI इकाइयों में मान और उसके
आयाम। इकाई तालिका में SI मूल और व्युत्पन्न इकाइयाँ हैं (`m`, `s`, `kg`,
`A`, `K`, `mol`, `cd`, `Hz`, `N`, `Pa`, `J`, `W`, `C`, `V`, `F`,
`ohm`, `S`, `Wb`, `T`, `H`, `lm`, `lx`, `Bq`, `Gy`, `Sv`), रोज़मर्रा
की इकाइयाँ (`min`, `hr`, `d`, `yr`, `L`, `t`, `bar`, `atm`, `torr`,
`psi`, `eV`, `mile`, `yd`, `ft`, `inch`, `nmi`, `lb`, `oz`, `gal`,
`qt`, `pt`, `mph`, `knot`), और खंड 1.29 के खगोल प्रत्यय। मिश्रित इकाइयाँ
जुड़ती हैं: `60 mile/hr` और `5 m/s^2` एक ही इकाई हैं।

```epher
60 mile/hr
```

```text
60 mile/hr
```

SI उपसर्ग उनमें से किसी को भी स्केल करते हैं: `k M G T m µ n p` किलो,
मेगा, गीगा, टेरा, मिली, माइक्रो, नैनो, पिको हैं — `5 km`, `3 MPa`,
`1 GHz` सब चलते हैं, और `2 kg` खुद किलोग्राम है।

आयाम जाँचे जाते हैं: अलग इकाइयों वाली राशियों को जोड़ना या तुलना करना
गलती देता है, मीटर और सेकंड मिलाने के बजाय:

```epher
5 m + 3 s
```

```text
error: dimension error: cannot add 5 m and 3 s
```

अंकगणित आयामों को जोड़ता है: `5 m * 3 m` = `15 m^2`, `(3 m)^2` =
`9 m^2`, `sqrt(4 m^2)` = `2 m`, और जिस व्यंजक के आयाम पूरी तरह कट जाते
हैं वह फिर एक साधारण संख्या है (`5 m / 5 m` = `1`)। जब आयाम किसी
व्युत्पन्न नाम से मेल खाते हैं तो परिणाम वही दिखाता है — `5 kg * 3 m / 1
s^2` का उत्तर `15 N` है।

**रूपांतरण।** `expr in इकाई` (या `expr -> इकाई`) राशि को नामित इकाई में
दिखाता है; आयाम मेल खाने चाहिए। `in` सबसे ढीला बंधता है, इसलिए
`5 m + 3 m in km` पूरे योग को बदलता है:

```epher
72 km/hr in m/s
```

```text
20 m/s
```

```epher
2 m^2 in cm^2
```

```text
20000 cm^2
```

तापमान पैमाने (सेल्सियस, फ़ारेनहाइट) यहाँ इकाइयाँ नहीं हैं — केल्विन है,
और `K` बाकियों की तरह काम करता है।


### 1.26 बिटवाइज़ संक्रियाएँ

खंड 1.15 के आधार शाब्दिक इसी के लिए बने हैं: `0b101`, `0o17`, `0xFF`।
बिटवाइज़ ऑपरेटर पूर्णांकों पर काम करते हैं और सटीक पूर्णांक उत्तर देते हैं:

```epher
0xFF & 0x0F
```

```text
15
```

| ऑपरेटर | अर्थ |
|---|---|
| `a & b` | बिटवाइज़ और |
| `a \| b` | बिटवाइज़ या |
| `a xor b` | बिटवाइज़ अनन्य या |
| `~a` | बिटवाइज़ नहीं (दो का पूरक) |
| `a << n` | बायाँ शिफ़्ट (2^n से गुणा) |
| `a >> n` | दायाँ शिफ़्ट, अंकगणितीय (2^n से भाग, नीचे पूर्णांक) |

परिणाम सटीक `big` पूर्णांक हैं, इसलिए `1 << 60` हर अंक रखता है। कार्यशील
शब्द आकार डिफ़ॉल्ट रूप से 64 बिट है: परिणाम साइन्ड टूज़-कॉम्प्लिमेंट के
रूप में पढ़े जाते हैं, इसलिए `~0` = -1 है और `1 << 100` 0 पर लपेटता है।
`bits(n)` शब्द आकार 8, 16, 32 या 64 करता है, और `bits()` उसे बताता है:

```epher
bits(8)
~0
```

```text
8
-1
```

ऋणात्मक मात्रा से शिफ़्ट दिशा उलट देता है (`8 << -1` = `4`)। बूलियन
`and` और `or` अपने अर्थ रखते हैं; `&` और `|` बिटवाइज़ लेखन हैं।


### 1.27 अंतर्निहित संबंध

दो अज्ञातों वाला समीकरण वक्र के रूप में प्लॉट होता है: ग्राफ़ परिवार मार्चिंग स्क्वेयर से संबंध का नमूना लेकर उसकी शून्य रूपरेखा खींचता है। वृत्त, परवलय और ऊर्ध्वाधर रेखा — हर एक एक ही कमांड:

```epher
graph x^2 + y^2 == 1
```

```epher
graph y == x^2
```

```epher
graph x == 2
```

संबंध `from a to b` (या डिफ़ॉल्ट विंडो) के वर्ग पर नमूना लिया जाता है, इसलिए `graph x^2 + y^2 == 1 from -2 to 2` वृत्त की विंडो में फिट होता है। वक्र की हर क्षमता लागू होती है: लेजेंड समीकरण को कैप्शन करता है, स्लाइडर उसके स्थिरांक को एनिमेट करते हैं, और चित्र ज़ूम, पैन और निर्यात होता है। असमानता भरण (`y < …`, `y > …`) छायांकन वाले वक्र ही रहते हैं; संबंध में रुचि बिंदु नहीं होते।


### 1.28 मैट्रिक्स

मैट्रिक्स संख्याओं का ग्रिड है, जो पंक्तियों की सूचियों के रूप में लिखा
जाता है: `[[1, 2], [3, 4]]` एक 2×2 मैट्रिक्स है। `+` और `-` तत्व-वार हैं
(आकार मेल खाने चाहिए), `*` मैट्रिक्स गुणन है, संख्या तत्व-वार स्केल करती
है, और `^` पूर्णांक घात है (`A ^ 0` तत्समक है, इसलिए घात को वर्ग मैट्रिक्स
चाहिए)। `M[2][1]` पंक्ति 2, स्तंभ 1 का तत्व है — पंक्तियाँ सूचियों की
तरह 1-आधारित इंडेक्स होती हैं।

```epher
[[1, 2], [3, 4]] * [[5, 6], [7, 8]]
```

```text
[[19, 22], [43, 50]]
```

मैट्रिक्स फ़ंक्शन कक्षा का फ़र्श कवर करते हैं: `det(M)` (केवल वर्ग),
`inv(M)` (एकवचन मैट्रिक्स त्रुटि है), `transpose(M)`, `trace(M)` (वर्ग),
`dim(M)` (`{पंक्तियाँ, स्तंभ}` सूची), और `ref(M)` के साथ `rref(M)`।
रेखीय समीकरण संवर्धित मैट्रिक्स पर rref से हल होते हैं:

```epher
rref([[2, 1, 5], [1, -1, 1]])
```

```text
[[1, 0, 2], [0, 1, 1]]
```

पंक्तियाँ `x = 2`, `y = 1` पढ़ती हैं — घटे हुए संवर्धित मैट्रिक्स का
अंतिम स्तंभ। सटीक भिन्न सूचियों की तरह मैट्रिक्स में दिखते हैं, इसलिए
`inv([[1, 2], [3, 4]])` दिखाता है `[[-2, 1], [3/2, -1/2]]`।


### 1.29 खगोल और सौर मंडल

epher खगोल बोलता है: इकाई प्रत्यय, भौतिक स्थिरांक, कैलेंडर और समय के फलन,
और सूर्य, चंद्रमा, ग्रहों और प्लूटो के लिए सजीव ग्रहपत्रिका (एफेमेरिस)।
सब कुछ ऑफ़लाइन काम करता है।

**खगोल बोलने वाली इकाइयाँ।** संख्या के बाद इकाई प्रत्यय लिखें, और epher उसे
तुरंत SI इकाइयों में बदल देता है:

| प्रत्यय | इकाई | बदलता है |
|---|---|---|
| `AU` या `au` | खगोलीय इकाई | मीटर |
| `pc` | पारसेक | मीटर |
| `ly` | प्रकाश वर्ष | मीटर |
| `deg` | डिग्री | रेडियन |
| `arcmin`, `arcsec` | आर्कमिनट, आर्कसेकंड | रेडियन |
| `min`, `hr`, `d`, `yr` | मिनट, घंटा, दिन, जूलियन वर्ष | सेकंड |
| `Jy` | जांस्की | W m-2 Hz-1 |

```epher
3.2 AU in m
```

```text
478713186240 m
```

```epher
sin(30 deg)
```

```text
0.5
```

कॉपी करने के लिए, प्रत्येक प्रत्यय का एक उदाहरण:

| प्रत्यय | आज़माएँ | परिणाम |
|---|---|---|
| `AU`, `au` | `1 au in km` | `149597870.7 km` |
| `pc` | `pc / au` | `206264.806247` (au per parsec) |
| `ly` | `ly / au` | `63241.0770843` (au per light year) |
| `deg` | `90 deg in rad` | `1.57079632679` |
| `arcmin` | `sin(1 arcmin)` | `0.000290888204563` |
| `arcsec` | `sin(1 arcsec)` | `0.00000484813681108` |
| `min` | `1 min in s` | `60 s` |
| `hr` | `1 hr in min` | `60 min` |
| `d` | `1 d in hr` | `24 hr` |
| `yr` | `1 yr in d` | `365.25 d` |
| `Jy` | `mag2jy(0) Jy` | `3631 Jy` |

प्रत्यय व्याकरण का हिस्सा हैं: कोई भी उपयोगकर्ता स्थिरांक `3.2 AU` का अर्थ
नहीं बदल सकता, और `h` प्लांक स्थिरांक ही रहता है; घंटे `hr` लिखे जाते हैं।
फलन प्राकृतिक इकाइयों में गिनती लौटाते हैं; प्रत्यय गिनती को SI में बदलता
है, इसलिए `mag2jy(20)` जांस्की गिनती है और `mag2jy(20) Jy` वही फ्लक्स
वाट प्रति वर्ग मीटर हर्ट्ज़ में है।

**खगोलीय स्थिरांक।** `au`, `pc`, `ly`, `c`, `g`, `h`, `h_bar`, `k_b`,
`sigma_sb`, `m_sun`, `r_sun`, `l_sun`, `m_earth`, `r_earth`, `m_moon`, `r_moon` `pi` की तरह
काम करते हैं, और आप उन्हें अपने स्थिरांकों से छायांकित कर सकते हैं।

कॉपी करने के लिए, प्रत्येक स्थिरांक का एक उदाहरण:

| स्थिरांक | मात्रा | आज़माएँ | परिणाम |
|---|---|---|---|
| `au` | खगोलीय इकाई | `1 au in km` | `149597870.7 km` |
| `pc` | पारसेक | `pc / au` | `206264.806247` |
| `ly` | प्रकाश वर्ष | `ly / au` | `63241.0770843` |
| `c` | प्रकाश की चाल (m/s) | `c / 1000` | `299792.458` (km/s) |
| `g` | मानक गुरुत्व (m/s²) | `g` | `9.80665` |
| `h` | प्लैंक स्थिरांक (J·s) | `h` | `0.000000000000000000000000000000000662607015` |
| `h_bar` | घटा हुआ प्लैंक स्थिरांक | `h_bar` | `0.000000000000000000000000000000000105457181765` |
| `k_b` | बोल्ट्ज़मान स्थिरांक (J/K) | `k_b` | `0.00000000000001380649` |
| `sigma_sb` | स्टेफ़न-बोल्ट्ज़मान स्थिरांक | `sigma_sb * 5772 ^ 4` | `12902411456/205` (सूर्य की सतह का प्रवाह, लगभग 62.9 MW/m²) |
| `m_sun` | सूर्य द्रव्यमान (kg) | `m_sun / m_earth` | `332954.355179` |
| `r_sun` | सूर्य त्रिज्या (m) | `r_sun / r_earth` | `109.197928112` |
| `l_sun` | सूर्य दीप्तिमानता (W) | `l_sun / (4 * pi * (1 au) ^ 2)` | `1361.16646541` (सौर स्थिरांक) |
| `m_earth` | पृथ्वी द्रव्यमान (kg) | `m_earth / m_moon` | `81.342958322` |
| `r_earth` | पृथ्वी त्रिज्या (m) | `r_earth / r_moon` | `3.66697363877` |
| `m_moon` | चंद्र द्रव्यमान (kg) | `m_moon / m_earth` | `0.0122936271391` |
| `r_moon` | चंद्र त्रिज्या (m) | `r_earth / r_moon` | `3.66697363877` |


**तारीख़ और समय।** `jd(y, m, d [, hr])` और `mjd(...)` कैलेंडर तारीख़ को
जूलियन तिथि में बदलते हैं; `now()` वर्तमान क्षण पढ़ता है:

```epher
jd(2000, 1, 1, 12)
```

```text
2451545
```

`delta_t(jd)` TT - UT1 संशोधन है, और `lst(jd, lon)` देशांतर (डिग्री में,

```epher
mjd(2000, 1, 1, 12)
```

```text
51544.5
```

```epher
delta_t(jd(2026, 1, 1))
```

```text
69.1
```

```epher
lst(jd(2026, 6, 21), 0)
```

```text
17.9472349955
```

**जूलियन दिवस को वापस पढ़ना।** जूलियन दिवस केवल दिनों की गिनती है; उसे तारीख की तरह पढ़ने से पहले अनुवाद चाहिए। खगोल विज्ञान का मानक रूपांतरण (Meeus, "Astronomical Algorithms", अध्याय 7; IAU SOFA की `jd2cal` routine) JD को **ग्रेगोरी कैलेंडर तिथि** और **UT घड़ी समय** में बदलता है; संकेतन वह **ISO 8601** दिनांक-समय है जो JPL Horizons और astropy छापते हैं। epher में तीनों रूप अंतर्निहित हैं: `date(jd)` `{year, month, day}` देता है, `time(jd)` UT में `{hour, minute, second}` घड़ी देता है, और `iso(jd)` पूरा क्षण `YYYY-MM-DDTHH:MM:SS` पाठ के रूप में लिखता है:

```epher
iso(jd(2000, 1, 1, 12))
```

```text
2000-01-01T12:00:00
```

```epher
date(jd(2000, 1, 1, 12))
```

```text
{2000, 1, 1}
```

```epher
time(jd(2000, 1, 1, 12))
```

```text
{12, 0, 0}
```

तीनों `jd()` का हूबहू उलटा हैं: `date()` से मिले मान पर `jd()` फिर वही मूल JD देता है। 1582-10-15 का कैलेंडर परिवर्तन संभाला गया है: `2299160.5` से आगे के JD ग्रेगोरी तिथियाँ हैं, पीछे के जूलियन, और `iso(0)` स्वयं युग है, `-4712-01-01T12:00:00`। `time()` निकटतम सेकंड तक गोल करता है; `iso()` आधे सेकंड को अगले दिन ले जाने देता है ताकि दिनांक-समय क्षण के सच रहे। `0` से नीचे के JD कैलेंडर की सीमा से बाहर हैं और डोमेन त्रुटि देते हैं।
पूर्व धनात्मक) के लिए स्थानीय नाक्षत्र समय है (घंटों में)।

**घंटे, मिनट और सेकंड।** `hms2deg(h, m, s)` विषुवत रेखांश को डिग्री में
बदलता है, `dms2deg(d, m, s)` षाष्ट्यंशीय कोण को, और `deg2hms(x)` /
`deg2dms(x)` कोण को पाठ के रूप में लिखते हैं:

```epher
deg2hms(90)
```

```text
6h 0m 0s
```

```epher
hms2deg(6, 42, 14)
```

```text
12067/120
```

```epher
dms2deg(-23, 26, 0)
```

```text
-703/30
```

```epher
deg2dms(-23.4333)
```

```text
-23° 26' 0"
```

**आकाश, संख्याओं में।** हर एक्सेसर को एक पिंड संख्या दें: बुध 1, शुक्र 2,
मंगल 4, बृहस्पति 5, शनि 6, अरुण 7, वरुण 8, प्लूटो 9, सूर्य 10, चंद्रमा 11
(पृथ्वी 3 है, प्रेक्षक, कभी लक्ष्य नहीं)।

| फलन | अर्थ |
|---|---|
| `ra(b, jd)`, `decl(b, jd)` | भूकेंद्रित विषुवत रेखांश और क्रांतिवृत्तांश (डिग्री) |
| `dist(b, jd)` | दूरी (खगोलीय इकाइयों में) |
| `alt(b, jd, lat, lon)`, `az(b, jd, lat, lon)` | शीर्षकेंद्रित ऊँचाई और दिगंश (डिग्री, सत्य) |
| `rise(b, jd, lat, lon)`, `set(...)`, `transit(...)` | स्थानीय सौर दिन की घटनाएँ, जूलियन तिथियों के रूप में |
| `mag(b, jd)` | दृश्य कांतिमान |
| `phase(b, jd)`, `illum(b, jd)` | कला कोण (डिग्री) और प्रदीप्त भाग |
| `diam(b, jd)` | कोणीय व्यास (डिग्री) |
| `satx(5, s, jd)`, `saty(5, s, jd)`, `satz(5, s, jd)` | बृहस्पति का चंद्रमा (s 1-4: Io, Europa, Ganymede, Callisto) या शनि का (s 1-8: Mimas, Enceladus, Tethys, Dione, Rhea, Titan, Hyperion, Iapetus), ग्रह की त्रिज्याओं में (x पश्चिम, y उत्तर, z आपकी ओर) |
| `satsep(5, s, jd)` | चंद्रमा की ग्रह के केंद्र से दूरी, आर्कसेकंड में |
| `satphen(5, s, jd)` | चंद्रमा की स्थिति: 0 दृश्यमान, 1 पारगमन, 2 छिपा हुआ, 3 ग्रहण में, 4 छाया पारगमन |

```epher
decl(10, jd(2000, 6, 21, 1.8))
```

```text
23.437882351
```

बाकी एक्सेसर कार्यरत, हर एक का एक उदाहरण (तिथियाँ ग्रीनविच, 51.4779 N, 0 E के लिए हैं):

```epher
ra(10, jd(2000, 1, 1, 12))
```

```text
281.278205832
```

```epher
dist(4, jd(2026, 6, 1))
```

```text
2.18405254195
```

```epher
alt(10, jd(2026, 6, 21, 11.97), 51.4779, 0)
```

```text
61.950168
```

```epher
az(10, jd(2026, 6, 21, 11.97), 51.4779, 0)
```

```text
178.2365282
```

```epher
mag(11, jd(2026, 6, 21))
```

```text
-9.66497374724
```

```epher
phase(11, jd(2026, 6, 21))
```

```text
100.83
```

```epher
illum(11, jd(2026, 6, 21))
```

```text
0.4061
```

```epher
diam(11, jd(2026, 6, 21))
```

```text
0.519678138889
```

rise, set और transit जूलियन दिवस में उत्तर देते हैं; `iso()` (ऊपर) उन्हें वापस पढ़ता है:

```epher
iso(rise(10, jd(2026, 6, 21), 51.4779, 0))
```

```text
2026-06-21T03:42:47
```

```epher
iso(transit(10, jd(2026, 6, 21), 51.4779, 0))
```

```text
2026-06-21T12:01:49
```

```epher
iso(set(10, jd(2026, 6, 21), 51.4779, 0))
```

```text
2026-06-21T20:20:50
```

अक्षांश और देशांतर डिग्री में हैं, पूर्व धनात्मक। बिना प्रेक्षक के स्थिति
भूकेंद्रित होती है। प्लूटो लगभग एक आर्कमिनट तक ईमानदार निकट-कक्षा पर चलता
है, जो अन्य पिंडों की सटीकता से बहुत नीचे है; ग्रहण और युति खोज शामिल नहीं
हैं।

**प्रकाशिकी और प्रकाश।** `kepler(M, e)` केप्लर समीकरण हल करता है,
`airmass(alt)` sec(z) वायुमंडल है, `dawes(d)` d-मिलीमीटर द्वार की विभेदन
क्षमता है (आर्कसेकंड में), और `dist_mod(mu)` दूरी मापांक को पारसेक में
बदलता है।

`mag2jy(m)` और `jy2mag(f)` कांतिमान और प्रवाह घनत्व के बीच बदलते हैं:

```epher
kepler(0.3, 0.1)
```

```text
0.333333124406
```

```epher
airmass(30)
```

```text
2
```

```epher
dawes(100)
```

```text
1.16
```

```epher
dist_mod(10)
```

```text
1000
```

```epher
mag2jy(0)
```

```text
3631
```

```epher
jy2mag(363.1)
```

```text
2.5
```

**ऋतुएँ।** `march_equinox(year)`, `june_solstice(year)`,
`september_equinox(year)` और `december_solstice(year)` हर ऋतु सीमा की
जूलियन तिथि लौटाते हैं:

```epher
march_equinox(2000)
```

```text
1012520636/413
```

```epher
june_solstice(2000)
```

```text
2451716.57464
```

```epher
september_equinox(2000)
```

```text
2451810.22761
```

```epher
iso(december_solstice(2000))
```

```text
2000-12-21T13:37:50
```

**3D में सौर मंडल।** `solar3d` कमांड पूरा सौर मंडल बनाता है: हर कक्षा एक
वक्र के रूप में, हर पिंड एक लेबल वाले बिंदु के रूप में, और साथ में एक रास्ता
जो दिखाता है कि वह अभी कहाँ था:

```epher
solar3d jd(2020, 7, 1)
```

समय को एक स्थिरांक के रूप में दें और ग्रहों को चलते देखने के लिए प्ले बटन
दबाएँ: `const t = now(); solar3d t`। घुमाने के लिए खींचें या तीर
कुंजियाँ दबाएँ, `clear` से खाली करें, और `solar3d save file.svg` से निर्यात
करें।

ग्रहपत्रिका की गणना solar-ephemeris क्रेट (github.com/Protonmatter/sol)
करता है, जो JPL Horizons से सत्यापित है; इसके लेखक को धन्यवाद। वर्तमान के
आसपास लगभग 5000 वर्षों में सूर्य, चंद्रमा और ग्रहों के लिए सटीकता
आर्कसेकंड-श्रेणी की है।
### 1.30 वित्त

epher पैसे की भाषा उतनी ही अच्छी बोलता है जितनी खगोल की: मूलधन-समय
मूल्य हल कर्ता, ऋण का परिशोधन, ब्याज और नकदी-प्रवाह विश्लेषण, सब
ऑफ़लाइन। इस खंड की हर उत्तर सादी संख्या है — मुद्रा-स्ट्रिंग नहीं —
इसलिए उत्तर मुद्रा-निरपेक्ष हैं और सीधे गणित में वापस जुड़ जाते हैं।
दरें हमेशा प्रति अवधि और भिन्न के रूप में होती हैं: `0.08/12` मतलब
8% वार्षिक दर मासिक लगत, और `0.01` मतलब 1% (1.2 का `%` प्रत्यय भी
चलता है: `6 * 100%` = 0.06)।

**चिह्न संविदा।** हल कर्ता कैलकुलेटर-मानक (TI) का पालन करता है: आपके
द्वारा दिया गया पैसा ऋणात्मक, मिला हुआ पैसा धनात्मक। लिए गए ऋण में
किश्त ऋणात्मक और भुगतान धनात्मक; बचत योजना में जमा ऋणात्मक और जुटाया
गया भंडार धनात्मक। पाँच फ़ील्ड का सुसंगत समूह शेष को शून्य कर देता है:

```text
pv*(1+i)^n + pmt*(1+i*begin)*((1+i)^n - 1)/i + fv = 0
```

चिह्न उलटे कर दें (ऋण और भुगतान दोनों ऋणात्मक) तो "पैसा कभी बैलेंस
नहीं होता", और हल कर्ता बेतुकी संख्या की जगह डोमेन त्रुटि देता है।

**मूलधन-समय मूल्य हल कर्ता।** पाँच फलन, दिए हुए चार फ़ील्ड से पाँचवाँ
हल करते हैं। `n` अवधियों की संख्या, `i` प्रति-अवधि दर, `pv` वर्तमान
मूल्य, `pmt` भुगतान, `fv` अंतिम मूल्य:

| फलन | उत्तर |
|---|---|
| `tvm_pmt(n, i, pv, fv)` | भुगतान |
| `tvm_n(i, pv, pmt, fv)` | अवधियों की संख्या |
| `tvm_i(n, pv, pmt, fv)` | प्रति-अवधि दर |
| `tvm_pv(n, i, pmt, fv)` | वर्तमान मूल्य |
| `tvm_fv(n, i, pv, pmt)` | अंतिम मूल्य |

क्लासिक 8% गृह-ऋण: 100,000 के ऋण पर 733.76 की 360 मासिक किश्तें:

```epher
tvm_pmt(360, 0.08/12, -100000, 0)
```

```text
733.764573879
```

पूरी अवधि का ब्याज = किश्त × किश्तों की संख्या − ऋण:

```epher
tvm_pmt(360, 0.08/12, -100000, 0) * 360 - 100000
```

```text
164155.246597
```

`tvm_i` बताई गई किश्त से दर पलट कर पढ़ता है (8%/12 से थोड़ा कम,
क्योंकि 733.76 पूर्णांकित है):

```epher
tvm_i(360, -100000, 733.76, 0)
```

```text
0.00666661199068
```

`tvm_n` "कितना समय" बताता है। न्यूनतम की जगह हर माह 900 चुकाने पर:

```epher
tvm_n(0.08/12, -100000, 900, 0)
```

```text
203.163223431
```

360 की जगह लगभग 203 महीने। और बैंक का अपना कोटेशन खुद जाँच लेता
है: 100,000 का ऋण, 5% दर, 536.82 मासिक — सचमुच 30 साल:

```epher
tvm_n(0.05/12, 100000, -536.82, 0)
```

```text
360.002521488
```

(चिह्न पलटे हुए देखें: यहाँ ऋण मिला हुआ पैसा है, इसलिए धनात्मक, और
भुगतान ऋणात्मक हैं।)

`tvm_fv` बचत योजना बढ़ाता है: हर माह 200, 40 साल, 6% पर:

```epher
tvm_fv(480, 0.06/12, 0, -200)
```

```text
398298.146866
```

`tvm_pv` भुगतानों की धारा की क़ीमत बताता है: 5% पर 20 साल तक हर
माह 1,500 देने के लिए फंड में आज कितना पूँजी चाहिए:

```epher
tvm_pv(240, 0.05/12, 1500, 0)
```

```text
-227287.969611
```

उत्तर ऋणात्मक है क्योंकि फंड ख़रीदना आज का व्यय है। दूसरी दिशा — किसी
लक्ष्य के लिए हर माह कितना अलग रखें — `tvm_pmt` से पूछें, लक्ष्य को
`fv` रख कर: 5% पर दस साल में 50,000 के लिए हर माह 322:

```epher
tvm_pmt(120, 0.05/12, 0, -50000)
```

```text
321.994242862
```

पाँचों में से हर एक वैकल्पिक अंतिम तर्क `begin` लेता है: 0 का अर्थ
भुगतान अवधि के अंत में (डिफ़ॉल्ट), 1 का अर्थ शुरुआत में (अग्रिम
वार्षिकी — किराया, अधिकांश वेतन)। अवधि की शुरुआत में भुगतान एक अवधि
अधिक ब्याज कमाते हैं, इसलिए गृह-ऋण की किश्त थोड़ी कम होती है:

```epher
tvm_pmt(360, 0.08/12, -100000, 0, 1)
```

```text
728.90520584
```

दर खोज प्रति अवधि 100% तक और अवधि खोज एक करोड़ अवधियों तक जाती है;
इन सीमाओं से बाहर की समस्या (या चिह्नों का ऐसा संयोजन जो कभी बैलेंस
नहीं होता) डोमेन त्रुटि देती है, जिसमें लिखा होता है कि क्या आज़माया।

**परिशोधन।** `amort(p, r, n, k)` — दर r पर n अवधि के ऋण p की k
किश्तों के बाद शेष राशि — 0 अवधि पर मूलधन, पूरे n पर शून्य:

```epher
amort(100000, 0.08/12, 360, 120)
```

```text
87724.7039064
```

8% गृह-ऋण के दस साल बाद 87,725 बाक़ी। एक लूप (1.10) इसे परिशोधन
तालिका बना देता है, हर पाँच साल पर एक पंक्ति:

```epher
for k in 0 to 360 step 60 do amort(100000, 0.08/12, 360, k)
```

```text
{100000, 95069.8567174, 87724.7039064, 76781.5595143, 60477.9628062, 36188.1192209, 0}
```

**ब्याज।** `simple_interest(p, r, t)` = `p*r*t` और
`compound_interest(p, r, n)` = `p*(1+r)^n - p` — दोनों कमाए गए ब्याज
का उत्तर देते हैं, शेष का नहीं:

```epher
simple_interest(1000, 0.05, 2)
```

```text
100
```

```epher
compound_interest(1000, 0.05, 2)
```

```text
102.5
```

शेष स्वयं सादी अंकगणित है — सादी संख्याओं का यही तो फ़ायदा है:

```epher
1000 * 1.05 ^ 2
```

```text
1102.5
```

दो रोज़मर्रा की दरें, उसी तरह बनीं। 6% नाममात्र दर के मासिक चक्रवृद्धि
का प्रभावी वार्षिक दर, और 6% पर 72-नियम का दोगुना होने का समय:

```epher
(1 + 0.06/12) ^ 12 - 1
```

```text
0.0616778118645
```

```epher
72 / (6 * 100%)
```

```text
12
```

**नकदी-प्रवाह विश्लेषण।** `npv(r, flows)` नकदी-प्रवाह सूची को दर r
पर अप्रचलित करता है: `flows[1]` आज का व्यय है, बाक़ी हर एक अवधि के
अंतर पर आते हैं। `irr(flows)` वह दर ढूँढता है जहाँ शुद्ध वर्तमान
मूल्य शून्य हो। आज 100 चुकाएँ और अगले दो साल हर साल 60 पाएँ:

```epher
npv(0.1, {-100, 60, 60})
```

```text
500/121
```

```epher
irr({-100, 60, 60})
```

```text
0.130662386292
```

निवेश 13.07% कमाता है। (`500/121` epher की यथार्थ भिन्न प्रदर्शनी
है, 1.14: जिस मान का दशमलव दोहराता है; `dec(500/121)` लिखेगा
4.13223140496।) निर्णय नियम: अपनी न्यूनतम दर पर `npv` धनात्मक हो तो
परियोजना लें — यानी `irr` न्यूनतम दर को हरे तो। 15% की दर पर यह
असफल है:

```epher
npv(0.15, {-100, 60, 60})
```

```text
-1300/529
```

जिस प्रवाह-सूची के पद कभी चिह्न नहीं बदलते, उसकी कोई सार्थक दर नहीं
होती; ऐसे में `irr` डोमेन त्रुटि देता है।

**रोज़मर्रा के एक-पंक्ति सूत्र।** पाँच साल में 1,000 से 2,400 हुए
निवेश की चक्रवृद्धि वार्षिक वृद्धि दर, और 2% मुद्रास्फीति के मुक़ाबले
7% नाममात्र पर वास्तविक प्रतिफल (फिर वही यथार्थ भिन्न प्रदर्शनी;
`dec(5/102)` = 0.0490196078431):

```epher
(2400/1000) ^ (1/5) - 1
```

```text
0.191357898167
```

```epher
1.07 / 1.02 - 1
```

```text
5/102
```

स्क्रिप्ट संग्रह इन दस फलनों पर बने 42 तैयार वित्त-स्क्रिप्ट देता है —
चार फ़ोल्डर: ब्याज (interest), निवेश (investing), ऋण (loans) और
बचत (savings) — हर एक की इंजन के विरुद्ध सत्यापित प्रतिलेख के साथ
(scripts.html एक-पंक्ति सारांश के साथ उन्हें सूचीबद्ध करता है)।

## 2. वेब ऐप (PWA)

### 2.1 इसे खोलना

वेब ऐप इस पते पर है:

```text
https://epher.org/pwa/
```

किसी इंस्टॉलेशन की ज़रूरत नहीं। यह किसी भी आधुनिक ब्राउज़र में कंप्यूटर, फ़ोन या टैबलेट पर चलता है।

यह गाइड ऐप में भी बनी हुई है: मेनू बार में **Help → In-app user guide** खोलें (फ़ोन पर **☰** टैप करें) और इसे ऐप के अंदर, ऐप की मौजूदा भाषा में पढ़ें। उस गाइड में किसी भी उदाहरण पर टैप करें। वह एंट्री फ़ील्ड में लोड हो जाता है। **सहायता → स्थिरांक** स्थिरांक ब्राउज़र खोलता है: हर अंतर्निर्मित स्थिरांक समूहों में (गणित, खगोल विज्ञान, भौतिकी, रसायन विज्ञान), हर एक अपने मान और छोटे विवरण के साथ; किसी पर टैप करें और उसका नाम इनपुट फ़ील्ड में डाला जाता है, और खोज बॉक्स सूची को छांटता है।

### 2.2 आपकी पहली गणना

1. टेक्स्ट फ़ील्ड पर क्लिक करें (पेज लोड होते ही उसमें फ़ोकस होता है)।
2. एक व्यंजक टाइप करें, जैसे `2 + 3 * 4`।
3. **Enter** दबाएँ या **=** बटन पर क्लिक करें।

परिणाम फ़ील्ड के नीचे बड़े अक्षरों में दिखाई देता है। अध्याय 1 की हर चीज़ यहाँ काम करती है, जिसमें चर, फ़ंक्शन और स्क्रिप्ट शामिल हैं। छोटा एकल उत्तर उसी पंक्ति में रहता है। लंबा उत्तर - कई उत्तरों वाली स्क्रिप्ट की आउटपुट सूची, एक तालिका, एक लंबी संख्या - परिणाम फलक में दिखता है, उसी फलक में जो ग्राफ़ भी दिखाता है, प्रति पंक्ति एक उत्तर; फ़ोन पर फलक स्वयं ही दृश्य में सरक आता है। उत्तर के ठीक बाएँ एक कॉपी आइकन है - दबाने पर उत्तर के मान क्लिपबोर्ड पर चले जाते हैं, प्रति पंक्ति एक; परिणाम फलक के लंबे उत्तर भी इसी तरह कॉपी होते हैं।

नाम टाइप करते समय फ़ील्ड के नीचे एक सुझाव सूची दिखती है: तीर कुंजियाँ चयन हिलाती हैं, **Enter** या **Tab** स्वीकार करता है, **Esc** बंद करता है, और क्लिक भी बिना कीबोर्ड छोड़े स्वीकार करता है। हर सुझाव के साथ फ़ंक्शन या स्थिरांक का संक्षिप्त विवरण होता है। **F1** कीपैड के ऊपर संकेत पट्टी में कर्सर के नीचे के शब्द का वही विवरण दिखाता है। अगर खाली फ़ील्ड में सबसे पहले आप ऑपरेटर (`+ - * / ^ % !`) टाइप करते हैं, तो epher आपके लिए `ans` जोड़ देता है, और पंक्ति पिछले उत्तर से आगे बढ़ती है।

स्क्रीन कीपैड आपके लिए टाइप करता है, बिल्कुल वैसे जैसे आपने चाबियाँ दबाई हों। उसके समूह पूरी भाषा को ढकते हैं: `123` अंक और ऑपरेटर, `trig`, `ƒ` फ़ंक्शन, `nΣ` संख्याएँ और सांख्यिकी, `data` मैट्रिक्स, सूचियाँ और स्ट्रिंग (वह समूह जो `[[1, 2], [3, 4]]`, `{1, 2, 3}` और "text" टाइप करता है), `0x` रूपांतरण और आधार, `dist` रिग्रेशन और वितरण परिवार, `$` वित्त, `π∇` स्थिरांक और ग्राफ़ कमांड, और `☉` खगोल विज्ञान (एकमात्र स्क्रॉल होने वाला समूह)। इस गाइड का हर फ़ंक्शन किसी न किसी चाबी पर है। **key hints** (**?** बटन) चालू करें और हर चाबी पर कैप्शन आ जाएगा, या किसी चाबी पर ठहरकर पढ़ें कि वह क्या करती है।

**सेटिंग्स** मेनू (गियर आइकन, या फ़ोन पर **☰ → सेटिंग्स**) में तीन समूह हैं। **थीम** और **भाषा** वही करते हैं जो उनके नाम कहते हैं। **परिणाम** उत्तरों का प्रदर्शन तय करता है: यथार्थ भिन्न (डिफ़ॉल्ट रूप से चालू, इसलिए `1 / 3` `1/3` दिखता है), संकेतन (ऑटो, वैज्ञानिक या इंजीनियरिंग) और हजार विभाजक। ये केवल प्रदर्शन सेटिंग हैं; नीचे के मान सामान्य संख्याएँ ही रहते हैं।

### 2.3 इतिहास

हर गणना परिणाम के नीचे इतिहास सूची में जुड़ती है, ताकि आप पीछे देख सकें कि आपने क्या किया। नई प्रविष्टियाँ सबसे ऊपर दिखाई देती हैं, और **इतिहास** शीर्षक के बाईं ओर कचरा-आइकन इसे खाली कर देता है (टर्मिनल में Ctrl+L, या उसी आइकन पर क्लिक)। इतिहास तब तक रहता है जब तक पेज खुला है।

हर मद पतली रेखाओं के बीच बैठता है: एक-पंक्ति का व्यंजक एक कतार है, और बहु-पंक्ति की स्क्रिप्ट एक मद है जो अपनी सभी पंक्तियाँ दिखाता है। किसी मद पर क्लिक करके उसे वापस इनपुट फ़ील्ड में लाएँ और फिर से चलाएँ।

### 2.4 ग्राफ़िंग

`graph` टाइप करें और फिर एक व्यंजक, और **Enter** दबाएँ:

```epher
graph x ^ 2
```

epher वक्र y = f(x) को x = −10 से x = 10 तक लेबल वाले अक्षों वाले ग्रिड पर, इनपुट के नीचे खींचता है। आप किसी भी व्यंजक का ग्राफ़ बना सकते हैं, जिसमें आपके अपने फ़ंक्शन शामिल हैं:

```epher
def f(x) = x ^ 3
graph f(x)
```

हर `graph` पंक्ति उसी प्लॉट में एक और वक्र जोड़ती है, हर वक्र का अपना रंग होता है। सभी वक्र ठोस होते हैं, और लीजेंड तथा
कैप्शन ही उन्हें बिना रंग के अलग पहचानते हैं। `graph clear` प्लॉट खाली करता है, और ग्राफ़ पेन के ऊपर **Clear graph** बटन वक्रों और 3D सतहों के लिए एक साथ यही करता है। TUI इस कमांड को अपने **Graph** मेनू में रखता है।

ग्राफ़ पैन के ऊपरी भाग में, **Clear graph**, **Copy SVG** और **Save PNG** के बगल में, टूलबार से आप रुचि के बिंदुओं की सूची और ग्राफ़ पर दिखाए गए उजागर बिंदु छिपा सकते हैं। हर ग्राफ़ के ठीक ऊपर आइकन-नामित स्लाइडरों की एक पट्टी है, शब्द हर एक के टूलटिप में हैं: रेखा की मोटाई (2D वक्रों के लिए 0 से 4 तक 0.1 के चरणों में, 3D प्लॉटों के लिए 0 से 0.4 तक 0.05 के चरणों में, 0.2 डिफ़ॉल्ट - दिखता केवल वर्तमान प्रकार का स्लाइडर है, और हर प्रकार अपना मान याद रखता है), हर प्लॉट पर ज़ूम, और 3D व सौर दृश्यों में क्षैतिज व ऊर्ध्वाधर घूर्णन गति। किसी स्लाइडर का आइकन दबाने पर वह स्लाइडर अपने डिफ़ॉल्ट मान पर लौट जाता है। लीजेंड की हर प्रविष्टि के सामने एक चेकबॉक्स है, डिफ़ॉल्ट रूप से चेक किया हुआ: उसे खाली करने पर वक्र, 3D सतह, अंतरिक्ष वक्र या सौर पिंड प्लॉट, उसके रुचि बिंदुओं और SVG निर्यात से छिप जाता है।

```epher
graph x ^ 2
graph x ^ 3
```

जिन बिंदुओं पर व्यंजक का कोई मान नहीं है (जैसे शून्य से भाग) उन्हें छोड़ दिया जाता है, जिससे वक्र में खाली जगह रह जाती है। जो उछाल वास्तव में एक लंबवत अनंतस्पर्शी रेखा है उसे कभी जोड़ने वाली रेखा के रूप में नहीं खींचा जाता।

#### 2.4.1 आप क्या प्लॉट कर सकते हैं

अपनी पसंद का प्रांत:

```epher
graph sin(x) from 0 to 2*pi
```

पैरामीट्रिक वक्र (t 0 से 2π तक चलता है):

```epher
graph param 2*cos(t), 3*sin(t)
```

ध्रुवीय वक्र:

```epher
graph polar 1 + cos(theta)
```

क्षेत्र: `y <` वक्र के नीचे का क्षेत्र भरता है, `y >` ऊपर का भरता है:

```epher
graph y < x ^ 2
```
#### 2.4.2 प्लॉट पढ़ना

**ट्रेस:** पॉइंटर को प्लॉट पर ले जाएँ, या उस पर फ़ोकस करें और तीर कुंजियाँ दबाएँ। वक्र का सबसे नज़दीकी बिंदु चिह्नित हो जाता है, उसके निर्देशांक प्लॉट के नीचे दिखाए जाते हैं।

**रुचि के बिंदु:** हर graph कमांड के बाद epher हर वक्र के मूल और मोड़ बिंदु तथा वक्रों के बीच प्रतिच्छेदन ढूँढता है, उन्हें प्लॉट पर चिह्नित करता है, और उन्हें उसके नीचे सूचीबद्ध करता है:

```text
root (-1, 0)   minimum (0, 0)   root (1, 0)
```

**सारणियाँ:** `table` कमांड मानों की सारणी छापता है (जिन पंक्तियों में व्यंजक का कोई मान नहीं है वे खाली रहती हैं):

एक वैकल्पिक `derivative <व्यंजक>` खंड तीसरा स्तंभ जोड़ता है, हर x
पर उस व्यंजक का संख्यात्मक अवकलज:

```epher
table x ^ 2 from -2 to 2 points 5 derivative x ^ 2
```

```text
         x           y          y'
        -2           4          -4
        -1           1          -2
         0           0           0
         1           1           2
         2           4           4
```

तालिका कक्ष परिणाम सेटिंग का पालन करते हैं: सटीक भिन्न चालू होने
पर (डिफ़ॉल्ट), साधारण भिन्न वाला मान भिन्न के रूप में दिखता है —
`table x / 3 from 0 to 1 points 4` में `0.333` की जगह `1/3` आता है।
```epher
table x ^ 2 from -2 to 2 points 5
```

```text
         x           y
        -2           4
        -1           1
         0           0
         1           1
         2           4
```

दो और खंड बाकी काम करते हैं। `values <सूची>` समान जाल के बजाय डेटा सूची से x कॉलम लेता है: अपना डेटा दें और उसे बदलता देखें:

```epher
d = {1, 2, 3, 4, 5}
table x ^ 2 values d
```

```text
         x           y
         1           1
         2           4
         3           9
         4          16
         5          25
```

और `exact` / `approx` एक तालिका के लिए सेल प्रदर्शन थोपते हैं: `exact` जहाँ बने वहाँ सरल भिन्न दिखाता है, `approx` दशमलव दिखाता है; हर एक उस एक कमांड के लिए परिणाम सेटिंग को रद्द करता है।

#### 2.4.3 स्लाइडर और निर्यात

एक स्थिरांक परिभाषित करें और उसे ग्राफ़ में उपयोग करें, और प्लॉट के नीचे एक स्लाइडर दिखाई देता है। उसे खींचें (या तीर कुंजियों से हिलाएँ) और हर वक्र फिर से खिंच जाता है:

```epher
const a = 1
graph a * x ^ 2
```

हर प्लॉट उसी तरह ज़ूम होता है: उस पर माउस व्हील घुमाएँ (या टच स्क्रीन पर पिंच करें) ताकि ज़ूम इन और आउट हो, और प्लॉट के ऊपर का ज़ूम स्लाइडर आपके साथ चलता है। डेटा प्लॉट - स्कैटर, हिस्टोग्राम और बॉक्स-एंड-व्हिस्कर - बिल्कुल वक्रों की तरह ज़ूम होते हैं।

**Copy SVG** मौजूदा प्लॉट को स्वसंपूर्ण SVG छवि के रूप में कॉपी करता है, दस्तावेज़ों में चिपकाने के लिए। रंग अंदर ही बसे हैं और पृष्ठभूमि पारदर्शी है, इसलिए वह किसी भी पेज, स्लाइड या दस्तावेज़ पर फिट बैठता है। **Save PNG** वही तस्वीर दोगुने आकार में बिटमैप के रूप में सहेजता है, जिससे वक्र तेज रहें, उसी पारदर्शी पृष्ठभूमि के साथ; डेस्कटॉप ऐप पूछता है कि कहाँ रखना है, और ब्राउज़र ऐप इसे डाउनलोड में सहेजता है (या जहाँ ब्राउज़र पूछने की सुविधा देता है, वहाँ पूछता है)। स्लाइडर पंक्तियाँ और एनिमेटेड स्थिरांक प्लॉट के ठीक नीचे, रुचि बिंदुओं की सूची के ऊपर बैठते हैं। एनिमेटेड स्लाइडर एक वास्तविक नियंत्रण है: उसके संकेतक को पकड़कर नए मान पर खींचें (पकड़ते ही एनीमेशन रुक जाता है), या फ़ोकस होने पर तीर कुंजियों से हिलाएँ।

#### 2.4.4 3D सतहें

`graph3d` एक वर्गाकार प्रांत (−5 से 5, या आपका `from a to b`) पर सतह z = f(x, y) प्लॉट करता है:

```epher
graph3d x ^ 2 - y ^ 2
```

आपके नज़दीक की मेश रेखाएँ ज़्यादा गहरी खींची जाती हैं, इसलिए आकृति में गहराई दिखाई देती है। कई `graph3d` पंक्तियाँ वक्रों की तरह एक के ऊपर एक खिंचती हैं, और `graph3d clear` प्लॉट खाली करता है। खींचकर दृश्य घुमाएँ, या प्लॉट पर फ़ोकस करें और तीर कुंजियाँ इस्तेमाल करें। टर्मिनल इंटरफ़ेस उसी सतह को ASCII वायरफ़्रेम के रूप में खींचता है, जिसे तीर कुंजियाँ घुमाती हैं।

अंतरिक्ष वक्र `param` से बनते हैं: `graph3d param cos(t), sin(t), t` आपके दिए t रेंज पर (`from 0 to 6.28318`) एक हेलिक्स खींचता है, या डिफ़ॉल्ट रूप से 0 से 2pi। वही पैन, वही घुमाव, ज़ूम और एनीमेशन।
#### 2.4.5 एनिमेशन

हर स्लाइडर में एक प्ले बटन होता है। वह अपने स्थिरांक को स्लाइडर की सीमा में कदम-दर-कदम चलाता है और फिर वापस शुरुआत में घूम जाता है: संकेतक पूरी पटरी चलता है और अंत पर पहुँचकर शुरुआत में लौट जाता है। यह कैलकुलेटरों का एनिमेट करने का मानक तरीका है: आप एक पैरामीटर एनिमेट करते हैं, और उसका इस्तेमाल करने वाली हर चीज़ हिलती है। रोकने के लिए बटन दोबारा दबाएँ।

एक "समय" चर बस एक स्थिरांक है जिसे आप एनिमेट करते हैं:

```epher
const t = 0
graph sin(x - t)
```

t का स्लाइडर चलाने से तरंग आगे बढ़ती है। 3D सतहें भी इसी तरह एनिमेट होती हैं। पहले एक स्थिरांक परिभाषित करें, फिर उसका स्लाइडर चलाएँ:

```epher
const a = 1
graph3d sin(a * (x ^ 2 + y ^ 2)) from -3 to 3
```

टर्मिनल इंटरफ़ेस में, स्पेस बार एनिमेशन शुरू और बंद करता है।

### 2.5 इसे इंस्टॉल करना और ऑफ़लाइन उपयोग

वेब ऐप एक *प्रगतिशील वेब ऐप* है: एक बार देखने के बाद यह पूरी तरह ऑफ़लाइन काम करता है, और आप इसे सामान्य ऐप की तरह इंस्टॉल कर सकते हैं।

- **Chrome, Edge या Android:** एड्रेस बार में इंस्टॉल आइकन पर क्लिक करें (या ब्राउज़र मेनू में *Install app*), फिर पुष्टि करें।
- **iPhone / iPad (Safari):** **Share** → **Add to Home Screen** पर टैप करें।
- **अन्य ब्राउज़र:** मेनू में *Install* या *Add to Home Screen* देखें।

एक बार इंस्टॉल होने पर, इसे होम स्क्रीन या ऐप सूची से खोलें। यह बिना इंटरनेट कनेक्शन के भी तुरंत खुलता है।

### 2.6 वेब ऐप क्या नहीं करता

वेब ऐप आपके काम को मौजूदा सत्र तक रखता है: यह व्यंजकों का मूल्यांकन करता है, उनका ग्राफ़ बनाता है (धारा 2.4), और इतिहास रखता है। **save**, **save script** और **language** कमांड डेस्कटॉप, कमांड लाइन और टर्मिनल संस्करणों में काम करते हैं (अध्याय 3, 4 और 5)। वेब ऐप में वे एक नोट के साथ उत्तर देते हैं कि सहेजना वहाँ काम करता है। हाल की गतिविधि याद रखी जाती है - इतिहास, आपके चर और फ़ंक्शन और अंतिम उत्तर ऐप बंद करने पर भी बने रहते हैं, केवल इसी ब्राउज़र में सहेजे जाते हैं।

## 3. डेस्कटॉप ऐप

डेस्कटॉप ऐप उसी वेब ऐप के चारों ओर एक सामान्य विंडो है। अध्याय 2 की हर बात लागू होती है; फ़र्क केवल इंस्टॉल और शुरू करने के तरीके में है।

### 3.1 इंस्टॉल करना

epher वेबसाइट से अपने सिस्टम के लिए एक इंस्टॉलर डाउनलोड करें:

- **Windows:** `epher-windows-x86_64.exe` चलाएँ। इंस्टॉलर `epher` को आपके PATH पर रखता है। नई CMD या PowerShell विंडो खोलें और `epher "2 + 2"` चल जाता है। चूँकि बिल्ड हस्ताक्षरित नहीं है, पहली बार *More info* → *Run anyway* चुनें।
- **macOS:** `epher-macos-aarch64.dmg` खोलें और epher को Applications में खींचें। चूँकि बिल्ड हस्ताक्षरित नहीं है, पहली बार खोलने के लिए राइट-क्लिक → **Open** करें।
- **Linux (Debian/Ubuntu):** `.deb` पैकेज

```sh
sudo apt install ./epher-linux-x86_64.deb
```

- **Linux (Fedora/RHEL):** `.rpm` पैकेज

```sh
sudo dnf install ./epher-linux-x86_64.rpm
```

- **Linux (कोई भी डिस्ट्रो, Arch सहित):** AppImage। इसे चलाने योग्य बनाएँ और चलाएँ:

```sh
chmod +x epher-linux-x86_64.AppImage
./epher-linux-x86_64.AppImage
```

**या किसी रिपॉज़िटरी या स्टोर से इंस्टॉल करें** — अपडेट फिर आपके पैकेज
मैनेजर के साथ आते हैं। Debian/Ubuntu पर:

```sh
curl -fsSL https://epher.org/apt/KEY.gpg | sudo tee /etc/apt/keyrings/epher.gpg >/dev/null
curl -fsSL https://epher.org/epher.sources | sudo tee /etc/apt/sources.list.d/epher.sources >/dev/null
sudo apt update && sudo apt install epher
```

Fedora पर:

```sh
curl -fsSL https://epher.org/epher.repo | sudo tee /etc/yum.repos.d/epher.repo >/dev/null
sudo dnf install epher
```

`.deb`, `.rpm`, और AppImage के ARM64 बिल्ड डाउनलोड पेज पर हैं।

हर इंस्टॉलर में *पूरा* epher होता है: डेस्कटॉप ऐप, कमांड लाइन (अध्याय 4) और टर्मिनल इंटरफ़ेस (अध्याय 5), सब एक ही `epher` कमांड के रूप में। Linux पर, पैकेज `epher` को `/usr/bin` में रखता है।

### 3.2 उपयोग

epher को किसी भी अन्य एप्लिकेशन की तरह शुरू करें। आपको वेब ऐप जैसा ही इंटरफ़ेस वाली विंडो मिलती है: व्यंजक टाइप करें, **Enter** दबाएँ या **=** पर क्लिक करें, और परिणाम पढ़ें। ग्राफ़िंग यहाँ भी काम करती है। `graph x ^ 2` विंडो में खींचता है (अध्याय 2.4)। विंडो का आकार स्वतंत्र रूप से बदला जा सकता है। मेनू बार में **Help → In-app user guide** शामिल है, इस पेज जैसी ही गाइड, जिसके उदाहरण टैप करने पर लोड होते हैं।

इसे टर्मिनल से भी खोल सकते हैं: बिना तर्क वाला `epher` (या `epher gui`) डेस्कटॉप ऐप शुरू करता है। macOS पर, ऐप के अंदर **Install the epher command** बटन से `epher` आपके टर्मिनल PATH पर आ जाता है।

### 3.3 भंडारण: CLI और TUI के साथ एक ही स्टोर

डेस्कटॉप ऐप अपना भंडारण कमांड लाइन और टर्मिनल संस्करणों के साथ साझा करता है। फ़ंक्शन, स्थिरांक, स्क्रिप्ट, इतिहास और भाषा पसंद एक ही जगह रहते हैं, आपके कंप्यूटर पर `~/.epher` (या `EPHER_STORE_DIR`, अध्याय 4.6), और एक संस्करण में सहेजी गई हर चीज़ दूसरों में उपलब्ध है:

```text
def area(w, h) = w * h
save area
```

`area` को डेस्कटॉप ऐप में परिभाषित करें, `save` करें, विंडो बंद करें। फिर CLI खोलें और `area(3, 4)` बस काम करता है। उल्टा भी यही है: डेस्कटॉप विंडो खुलने पर CLI या TUI में सहेजे गए फ़ंक्शन और स्क्रिप्ट पहले से वहाँ होते हैं, सहेजी गई स्क्रिप्टों द्वारा सेट किए गए चर सहित। अध्याय 4 के `save`, `save script` और `language` कमांड यहाँ बिल्कुल उसी तरह काम करते हैं।

CLI, REPL, TUI या डेस्कटॉप ऐप में टाइप किए गए कमांड सभी एक ही इतिहास में जुड़ते हैं, और सत्र भी साथ चलता है: आपके द्वारा निर्धारित चर और `ans` मान एक संस्करण से दूसरे में आपका पीछा करते हैं। साझा स्टोर लाइव है: जब दो संस्करण एक साथ खुले हों, तो एक में बदलाव तुरंत दूसरे में दिखाई देता है (डेस्कटॉप ऐप और TUI स्टोर देखते हैं और अपने आप ताज़ा होते हैं)।

> ब्राउज़र में वेब ऐप ही एकमात्र संस्करण है जो इस स्टोर का उपयोग नहीं करता। हर सत्र अलग रहता है (अध्याय 2.6)।

## 4. कमांड लाइन (CLI)

CLI उसी `epher` प्रोग्राम का टेक्स्ट रूप है जो डेस्कटॉप ऐप है। इसके तीन मोड हैं: एक-बार मूल्यांकन, पाइप्ड स्क्रिप्ट, और लंबे काम के लिए इंटरैक्टिव सत्र।

किसी भी समय सहायता के लिए `epher --help` चलाएँ (सभी कमांड, उदाहरणों के साथ)
या `epher help` (पूरा मैनुअल; Linux पैकेजों पर यह `man epher` मैनुअल पेज है)।

हर इंस्टॉलर पूरा स्क्रिप्ट संग्रह अपने साथ लाता है - वही स्क्रिप्ट जिन्हें
वेबसाइट का Scripts पृष्ठ ब्राउज़ करता है - और उसे प्रोग्रम के पास इंस्टॉल
करता है। वेबसाइट पर, स्क्रिप्ट्स के README में और नीचे दिए कॉपी-पेस्ट
टर्मिनल कमांड सभी उसी इंस्टॉल किए गए फ़ोल्डर की ओर इशारा करते हैं, इसलिए
epher इंस्टॉल होते ही वे काम करते हैं:

```sh
# Debian, Ubuntu, Fedora (deb, rpm)
epher /usr/lib/epher/scripts/astronomy/moon/full-moons.epher

# Windows (PowerShell)
epher "$env:LOCALAPPDATA\epher\scripts\astronomy\moon\full-moons.epher"

# macOS
epher /Applications/epher.app/Contents/Resources/scripts/astronomy/moon/full-moons.epher
```

### 4.1 एक-बार गणनाएँ

व्यंजक को तर्क के रूप में दें:

```sh
epher "2 + 3 * 4"
```

```text
14
```

अध्याय 1 की कोई भी एकल व्यंजक वाली चीज़:

```sh
epher "if 3 > 2 then 10 else 20"
```

```text
10
```

ऋण चिह्न से शुरू होने वाला व्यंजक सीधे चलता है:

```sh
epher "-2 + 5"
```

```text
3
```

एक-बार मोड स्क्रिप्ट के लिए है, एक व्यंजक से लेकर पूरे प्रोग्राम तक। हर कथन का मान अपनी पंक्ति में छपता है:

```sh
epher "x = 10; x + 5"
```

```text
10
15
```

नई पंक्तियों से जुड़े कथन तर्क के अंदर उसी तरह काम करते हैं। अध्याय 1 की हर चीज़ उपलब्ध है: चर, फ़ंक्शन, लूप, सब कुछ। पंक्तियाँ एक ही सत्र साझा करती हैं, पाइप्ड स्क्रिप्ट की तरह (अनुभाग 4.2)।

### 4.2 पाइप्ड स्क्रिप्ट

`epher -` मानक इनपुट से एक-एक पंक्ति व्यंजक पढ़ता है, जैसे पाइपलाइनों में स्क्रिप्टिंग भाषाएँ चलती हैं:

```sh
# Linux and macOS (bash, zsh)
printf "x = 3\nx * 10\n" | epher -

# Windows PowerShell
"x = 3`nx * 10" | epher -
```

```text
= 3
= 30
```

अध्याय 1 की हर चीज़ काम करती है, और पंक्तियाँ एक ही सत्र साझा करती हैं: पहले परिभाषित फ़ंक्शन बाद में उपलब्ध है, और `save` हमेशा की तरह उसी स्टोर में लिखता है। त्रुटियाँ छपती हैं और स्क्रिप्ट चलती रहती है। एक पंक्ति कई कथनों को `;` से जोड़ सकती है। नई पंक्तियों और `;` का अर्थ epher में हर जगह एक जैसा है।


फ़ाइल भी वैसे ही काम करती है: `epher plots/sine.es` फ़ाइल की हर पंक्ति को क्रम से चलाता है और हर परिणाम दिखाता है। जब तर्क किसी मौजूदा फ़ाइल को नाम देता है और उसमें `.`, `/` या `\` होता है तो उसे फ़ाइल माना जाता है - `epher x` अब भी नाम `x` का मूल्यांकन करता है।
### 4.3 इंटरैक्टिव सत्र (REPL)

इसे `epher repl` से शुरू करें:

```sh
epher repl
```

> बिना तर्क वाला `epher` डेस्कटॉप ऐप खोलता है (अध्याय 3)।

epher अपना प्रॉम्प्ट दिखाता है और इंतज़ार करता है:

```text
epher>
```

अब अध्याय 1 की कोई भी चीज़ एक-एक पंक्ति में टाइप करें। चर अपना मान पंक्तियों के बीच रखते हैं:

```text
epher> x = 5
= 5
epher> x ^ 2
= 25
```

`table` कमांड (धारा 2.4.2) यहाँ भी मानों की सारणी छापता है:

```text
epher> table x ^ 2 from -2 to 2 points 5
         x           y
        -2           4
        -1           1
         0           0
        `graph` पंक्तियाँ यहाँ भी चलती हैं: वक्र पंक्तियों के साथ जुड़ते जाते हैं,
और `graph save plot.svg` वही SVG छवि लिखता है जो वेब ऐप का **Copy SVG**
बटन देता है। `graph3d save file.svg` उसी तरह 3D सतह सहेजता है। ये ही
पंक्तियाँ एक-बार के मूल्यांकन और पाइप की गई स्क्रिप्ट में भी चलती हैं:
`epher "graph sin(x); graph save plot.svg"` एक ही कमांड में पूरा प्लॉट है।

 1           1
         2           4
```

हर उत्तर `= परिणाम` के रूप में दिखता है। बाहर निकलने के लिए `quit` (या `exit`) टाइप करें:

```text
epher> quit
```

आपका इतिहास याद रखा जाता है: अगली बार `epher repl` चलाने पर पिछले सत्र की पंक्तियाँ अब भी मौजूद रहती हैं।


`load` कमांड एक स्क्रिप्ट चलाती है - फ़ाइल पथ, या `save script` से सहेजी गई स्क्रिप्ट का नाम - पंक्ति दर पंक्ति, बिल्कुल जैसे आपने उसे टाइप किया हो:

```text
epher> load plots/sine.es
epher> load my_setup
```
### 4.4 फ़ंक्शन, स्थिरांक और स्क्रिप्ट सहेजना

एक फ़ंक्शन परिभाषित करें, फिर सहेजें:

```text
epher> def fib(n) = if n <= 1 then n else fib(n - 1) + fib(n - 2)
epher> save fib
saved fib
```

`save fib` कमांड फ़ंक्शन को डिस्क पर संग्रहीत करता है। अगली बार सत्र शुरू करने पर `fib` पहले से परिभाषित होता है:

```text
epher> fib(10)
= 55
```

स्थिरांक उसी तरह सहेजे जाते हैं। स्थिरांक के नाम पर `save`:

```text
epher> const tax = 0.2
= 0.2
epher> save tax
saved tax
```

पूरी स्क्रिप्ट (आपकी आखिरी टाइप की गई पंक्ति) सहेजने के लिए `save script` उपयोग करें:

```text
epher> x = 0; while x < 5 do x = x + 1; x
= 5
epher> save script count_to_five
saved script count_to_five
```

सहेजी गई स्क्रिप्ट epher शुरू होने पर स्वतः चलती हैं, इसलिए उनमें परिभाषित सब कुछ आपके लिए तैयार रहता है।


आप सहेजी गई स्क्रिप्ट को `load count_to_five` से जब चाहें लोड कर सकते हैं, या उसे सादी फ़ाइल के रूप में रखकर `load count_to_five.es` चला सकते हैं; `epher count_to_five.es` उसे सीधे कमांड लाइन से चलाता है (धारा 4.2)।
### 4.5 इंटरफ़ेस भाषा बदलना

इंटरफ़ेस भाषा आपके डिवाइस पर सेट भाषाओं से चुनी जाती है। इसे बदलने के लिए `language` टाइप करें और फिर इनमें से एक: `en`, `zh-CN`, `hi`, `es`, `fr`, `ar`, `de`, `pt`:

```text
epher> language fr
language set to fr
```

चुनाव अगली बार के लिए याद रखा जाता है। ध्यान दें: जो भाषा आप *टाइप* करते हैं, व्यंजकों की भाषा, हमेशा एक जैसी रहती है, किसी भी इंटरफ़ेस भाषा में।

### 4.6 आपका डेटा कहाँ रहता है

फ़ंक्शन, स्क्रिप्ट, इतिहास और आपकी भाषा का चुनाव आपके कंप्यूटर के एक फ़ोल्डर में संग्रहीत होते हैं:

```text
~/.epher
```

पूरी तरह नई शुरुआत के लिए उस फ़ोल्डर को हटा दें। दूसरा स्थान उपयोग करने के लिए, epher शुरू करने से पहले `EPHER_STORE_DIR` पर्यावरण चर सेट करें:

```sh
EPHER_STORE_DIR=/tmp/my-epher epher repl
```

## 5. टर्मिनल इंटरफ़ेस (TUI)

TUI इंटरैक्टिव सत्र का फुल-स्क्रीन संस्करण है, जो आपके टर्मिनल के अंदर चलता है। यह उसी `epher` प्रोग्राम का हिस्सा है। इसे शुरू करें:

```sh
epher tui
```

### 5.1 स्क्रीन

स्क्रीन पैनलों में विभाजित है:

- **व्यंजक**: इनपुट बॉक्स (ऊपर)। Shift+Enter नई पंक्ति शुरू करता है;
  तीर कुंजियाँ या माउस क्लिक पाठ में कर्सर घुमाते हैं।
- उसके ठीक नीचे वर्तमान **परिणाम**।
- **इतिहास**: आपकी हर पंक्ति, उत्तर सहित।
- **ग्राफ़**: `graph` कमांड का प्लॉट (नीचे)।
- एक संकेत पंक्ति कीबोर्ड शॉर्टकट दिखाती है।

### 5.2 कुंजियाँ

| कुंजी | क्रिया |
|---|---|
| टाइप करें | कर्सर पर व्यंजक में जोड़ें |
| **Enter** | पूरी स्क्रिप्ट का मूल्यांकन करें (बहु-पंक्ति इनपुट एक इतिहास मद के रूप में चलता है) |
| **Shift+Enter** | नई पंक्ति शुरू करें |
| **← → ↑ ↓** | कर्सर घुमाएँ (खाली इनपुट पर: 3D दृश्य घुमाएँ) |
| **Esc** | इनपुट पंक्ति साफ़ करें |
| **F1** | कर्सर के नीचे के फ़ंक्शन का वर्णन (परिणाम पंक्ति में) |
| **Ctrl+C** | बाहर निकलें |
| **q** | बाहर निकलें (इनपुट खाली होने पर) |
| **Arrow keys** | 3D दृश्य घुमाएँ (इनपुट खाली होने पर) |
| **Space** | एनिमेशन शुरू/बंद करें (इनपुट खाली होने पर) |
| **F9** | मेनू खोलें (फ़ाइल, संपादन, ग्राफ़, सेटिंग्स, सहायता) |
| **Tab** | हमेशा दिखने वाले कीपैड पर फ़ोकस करें (या कीपैड से इतिहास पर); बैंक बदलें (**Esc** टाइपिंग पर लौटाता है) |
| **माउस** | मेनू और मेनू आइटम पर क्लिक करें, कीपैड सेल और बैंक टैब, इतिहास पंक्तियाँ (व्यंजक लोड करती हैं); ग्राफ़ पैनल खींचकर घुमाएँ (3D) या सरकाएँ (2D), व्हील ज़ूम करता है, डबल-क्लिक दृश्य रीसेट करता है |
| **Ctrl+L** | इतिहास साफ़ करें |

**सहायता** मेनू अंतर्निर्मित गाइड, कीपैड की-हेल्प, और एक स्थिरांक ब्राउज़र खोलता है: स्थिरांक समूहों में, तीर एक पंक्ति चुनते हैं, **Enter** उसका नाम कर्सर पर व्यंजक में डालता है, और **Esc** बंद करता है।

कीपैड के बैंक भाषा की हर फ़ंक्शन, स्थिरांक और कमांड रखते हैं, वेब कीपैड के टैब क्रम में: **trig**, **fn**, **num**, **data**, **dist**, **fin**, **0x**, **var** और **astro** (अंक समूह **123** है)। 0x बैंक में सटीक और आधार रूपांतरण (`frac`, `dec`, `big`, `bin`, `oct`, `hex`) तथा फ़ैक्टोरियल `!` हैं। तीर कुंजियाँ चयन घुमाती हैं, **Enter** टोकन जोड़ता है, और **Tab** बैंक बदलता है। खाली पंक्ति की शुरुआत में टाइपा (या कीपैड से जोड़ा) गया ऑपरेटर पहले `ans` जोड़ता है, और पंक्ति पिछले उत्तर से आगे बढ़ती है।

**सेटिंग्स** मेनू वेब ऐप जैसे ही परिणाम प्रदर्शन विकल्प देता है (यथार्थ भिन्न, संकेतन, हजार विभाजक), थीम और भाषा की पंक्तियों के साथ।

### 5.3 ग्राफ़िंग

`graph` टाइप करें और फिर एक व्यंजक, और **Enter** दबाएँ:

```epher
graph x ^ 2
```

epher वक्र को x = −10 से x = 10 तक नमूना करता है और Graph पैनल में ASCII प्लॉट के रूप में खींचता है; प्लॉट के ऊपर का लीजेंड बताता है कि क्या प्लॉट हुआ है।

`graph clear` प्लॉट खाली करता है, और **Graph** मेनू भी यही करता है; **Help** मेनू इस गाइड को TUI के अंदर खोलता है (तीर कुंजियाँ स्क्रॉल करती हैं, **Esc** बंद करता है)। **Settings** मेनू
प्लॉट के नीचे सूचीबद्ध रुचि के बिंदु छिपा सकता है।

आप किसी भी व्यंजक का ग्राफ़ बना सकते हैं, जिसमें आपके अपने फ़ंक्शन शामिल हैं। पहले एक परिभाषित करें, फिर ग्राफ़ करें:

```epher
def f(x) = x ^ 3
graph f(x)
```

हर `graph` पंक्ति प्लॉट में एक वक्र जोड़ती है, जो अपने प्रतीक (`o`, `x`, `+`, `*`) से खींचा जाता है; `graph clear` प्लॉट खाली करता है। वेब ऐप वाला व्याकरण यहाँ भी लागू होता है: प्रांत (`graph sin(x) from 0 to 2*pi`), पैरामीट्रिक वक्र (`graph param 2*cos(t), 3*sin(t)`), ध्रुवीय वक्र (`graph polar 1 + cos(theta)`), और क्षेत्र (`graph y < x ^ 2` वक्र के नीचे का क्षेत्र भरता है)।

जिन बिंदुओं पर व्यंजक का कोई मान नहीं है (जैसे शून्य से भाग) उन्हें बस छोड़ दिया जाता है, जिससे प्लॉट में खाली जगह रह जाती है। हर graph कमांड के बाद TUI प्लॉट के नीचे रुचि के बिंदुओं (मूल, मोड़ बिंदु और प्रतिच्छेदन) की सूची देता है। `table` कमांड (धारा 2.4.2) यहाँ भी काम करता है।

`graph3d x ^ 2 - y ^ 2` 3D सतह को ASCII वायरफ़्रेम के रूप में प्लॉट करता है। इनपुट खाली रहने पर इसे तीर कुंजियों से घुमाएँ, और किसी स्लाइडर स्थिरांक को एनिमेट करने के लिए स्पेस बार दबाएँ (धारा 2.4.5)। नीचे की संकेत पंक्ति तीर और स्पेस के संकेत केवल तब दिखाती है जब कोई 3D सतह या एनिमेट करने योग्य वक्र प्रदर्शित हो।

`graph save plot.svg` मौजूदा प्लॉट को वही SVG छवि लिखता है जो वेब ऐप का
**Copy SVG** बटन देता है; `graph3d save file.svg` 3D वायरफ़्रेम को उसी
कोण से सहेजता है जिससे आप उसे देख रहे हैं।

### 5.4 सहेजना और स्थायित्व

TUI अपना संग्रहण CLI के साथ साझा करता है: एक में सहेजा गया सब कुछ दूसरे में उपलब्ध है। फ़ंक्शन, स्क्रिप्ट, इतिहास और भाषा पसंद `~/.epher` (अध्याय 4.6) में रहते हैं, और वही `save`, `save script` और `language` कमांड यहाँ काम करते हैं।

## 6. आपका डेटा और गोपनीयता

- **इंस्टॉल किया गया epher प्रोग्राम** (डेस्कटॉप ऐप, CLI और TUI) फ़ंक्शन, स्क्रिप्ट, इतिहास और भाषा चुनाव को `~/.epher` (या `EPHER_STORE_DIR`) में स्थानीय रूप से संग्रहीत करता है। कुछ भी आपके कंप्यूटर से बाहर नहीं जाता।
- **वेब ऐप** डिस्क पर कुछ नहीं रखता: इतिहास केवल पेज खुले रहने तक चलता है। वेब ऐप ऑफ़लाइन चल सकता है क्योंकि पेज को आपका ब्राउज़र स्वयं संग्रहीत करता है।

पाँचों संस्करण गणना पूरी तरह आपके डिवाइस पर करते हैं। कुछ भी कहीं नहीं भेजा जाता।

